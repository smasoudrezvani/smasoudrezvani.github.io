<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://smasoudrezvani.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://smasoudrezvani.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-26T21:14:41+00:00</updated><id>https://smasoudrezvani.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal website of Masoud Rezvani (also known as Masoud Rezvaninejad) — Data Scientist, MBA, and Master&apos;s student at UvA. Research interests include AI, Deep Learning, LLMs, NLP, and MLOps. </subtitle><entry><title type="html">Part 11: Batch Processing</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-11-Batch-Processing/" rel="alternate" type="text/html" title="Part 11: Batch Processing"/><published>2026-06-26T00:00:00+00:00</published><updated>2026-06-26T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%2011%20-%20Batch%20Processing</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-11-Batch-Processing/"><![CDATA[<p>A junior engineer’s beautifully optimized script finishes crunching 10 million recommendations and writes them straight to the live production database — 10 million inserts per second. Two seconds later, the pager goes off. They’ve launched a self-inflicted distributed denial-of-service attack on their own company. Chapter 11 of DDIA is about why that happens, and how the tech giants process petabytes without destroying their systems.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 11 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-online-vs-batch-processing">1. Online vs. Batch Processing</h2> <p>Most early software is <strong>OLTP (Online Transaction Processing)</strong>: a user clicks a button, a request hits a server, a database updates, a response comes back. The metric is <strong>response time</strong> — measured in milliseconds, with user abandonment beginning around 2–3 seconds.</p> <p>Batch processing flips every one of those priorities:</p> <table> <thead> <tr> <th>Property</th> <th>Online (OLTP)</th> <th>Batch</th> </tr> </thead> <tbody> <tr> <td>Trigger</td> <td>User request</td> <td>Scheduled job</td> </tr> <tr> <td>Metric</td> <td>Latency</td> <td>Throughput</td> </tr> <tr> <td>Input data</td> <td>Mutable (UPDATE in place)</td> <td>Immutable (read-only)</td> </tr> <tr> <td>Output</td> <td>Overwrites live state</td> <td>New derived output</td> </tr> <tr> <td>Failure recovery</td> <td>Stressful (corrupted live data)</td> <td>Trivial (delete output, rerun)</td> </tr> </tbody> </table> <p>The last row is crucial. Because batch jobs read input and write output to a <em>different location</em>, the input is always intact. A bug that corrupts yesterday’s recommendations is fixed by patching the code and re-running — not by writing emergency recovery scripts against a live database. This <strong>human fault tolerance</strong> lowers the cost of experimentation dramatically.</p> <hr/> <h2 id="2-the-unix-philosophy-small-tools-and-pipes">2. The Unix Philosophy: Small Tools and Pipes</h2> <p>To understand how petabyte-scale batch processing works, start with a single laptop in the 1970s. Unix showed that complex data pipelines emerge from chaining simple, single-purpose tools:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat </span>access.log <span class="se">\</span>
  | <span class="nb">awk</span> <span class="s1">'{print $7}'</span> <span class="se">\</span>
  | <span class="nb">sort</span> <span class="se">\</span>
  | <span class="nb">uniq</span> <span class="nt">-c</span> <span class="se">\</span>
  | <span class="nb">sort</span> <span class="nt">-rn</span> <span class="se">\</span>
  | <span class="nb">head</span> <span class="nt">-5</span>
</code></pre></div></div> <p>This finds the 5 most popular URLs in an nginx access log. Each tool does exactly one thing:</p> <ul> <li><code class="language-plaintext highlighter-rouge">awk</code> extracts the URL column — filters noise</li> <li><code class="language-plaintext highlighter-rouge">sort</code> groups identical URLs into adjacent lines</li> <li><code class="language-plaintext highlighter-rouge">uniq -c</code> counts runs of identical adjacent lines — O(1) memory</li> <li><code class="language-plaintext highlighter-rouge">sort -rn</code> orders by count descending</li> <li><code class="language-plaintext highlighter-rouge">head -5</code> truncates to the answer</li> </ul> <p>The <strong>pipe</strong> is the architectural innovation: standard output of one program flows directly to standard input of the next. Data streams through without any program needing to see the whole dataset at once.</p> <h3 id="why-not-a-python-hash-table">Why Not a Python Hash Table?</h3> <p>A Python script can do the same thing — instantiate a dictionary, read line by line, increment counts. For a few million rows, it’s perfectly valid. For hundreds of millions, it hits a hard physical wall: <strong>RAM exhaustion</strong>. As the hash table grows beyond available memory, the OS begins swapping pages to disk. Because a hash table requires random access, the CPU ends up thrashing — spending 99% of its time moving data in and out of swap, effectively freezing the machine.</p> <p>Unix’s <code class="language-plaintext highlighter-rouge">sort</code> command avoids this with <strong>external merge sort</strong>.</p> <hr/> <h2 id="3-sorting-beyond-memory-merge-sort-on-disk">3. Sorting Beyond Memory: Merge Sort on Disk</h2> <p>External merge sort respects the physics of storage hardware:</p> <ol> <li>Read a chunk of data that fits comfortably in RAM</li> <li>Sort it in memory</li> <li>Write the sorted chunk sequentially to disk</li> <li>Repeat until all chunks are written</li> <li>Merge the sorted chunks using a streaming k-way merge</li> </ol> <p>The key insight: <strong>sequential I/O is orders of magnitude faster than random I/O</strong>. A spinning disk must physically seek the read head to each new location. Sequential reads pay the seek penalty once, then stream data at 150 MB/s+. Random reads pay it millions of times. Merge sort is designed in deep sympathy with hardware physics — it almost never seeks randomly.</p> <p>This lets a laptop with 8 GB of RAM sort a terabyte dataset. The constraint shifts from RAM size to disk size.</p> <hr/> <h2 id="4-distributed-storage-hdfs-and-object-stores">4. Distributed Storage: HDFS and Object Stores</h2> <p>When the dataset exceeds a single disk, you need distributed storage.</p> <h3 id="hdfs-shared-nothing-commodity-storage">HDFS: Shared-Nothing Commodity Storage</h3> <p>The Hadoop Distributed File System (HDFS) splits files into large blocks (default 128 MB) and distributes them across hundreds of commodity servers. A central <strong>NameNode</strong> maintains a metadata ledger in RAM mapping every block to its server locations.</p> <p>Why 128 MB blocks? Math. If you stored a 1 PB dataset in 4 KB blocks (like a local filesystem), you’d generate 250 billion blocks. At 150 bytes of metadata each, the NameNode would need 37 TB of RAM just for the index. At 128 MB blocks, 1 PB generates only ~8 million blocks — about 1.2 GB of metadata. Block size is a deliberate trade-off to protect the centralized index from memory exhaustion.</p> <p>HDFS replicates every block three times across different racks. If a hard drive fails, the NameNode detects the missing replica from heartbeat timeouts and spawns a background copy job to restore redundancy on a healthy node. Hardware failure becomes a routine operational event.</p> <h3 id="object-stores-infinite-flat-key-value">Object Stores: Infinite Flat Key-Value</h3> <p>S3, GCS, and Azure Blob Storage take a different approach: there are no directories. The file path <code class="language-plaintext highlighter-rouge">year=2026/month=06/data.parquet</code> is just a string key. The “folders” in the S3 UI are a fiction — a prefix search on the flat keyspace.</p> <p>The trade-off: metadata operations (renaming a “folder” of 10,000 files) require 10,000 individual copy + delete calls. But storage scales infinitely because there is no tree structure to lock or traverse.</p> <p><strong>Compute-storage decoupling</strong> is the modern payoff. With HDFS, you had to buy compute and storage together (data locality). With S3, you scale them independently. If you accumulate logs but run fewer jobs, pay for storage only. If you need a massive one-time ML training run, spin up 1,000 spot instances for 6 hours and shut them down. 100 Gbps data-center networks have made the latency cost of network-attached storage acceptable.</p> <hr/> <h2 id="5-orchestration-kubernetes-and-yarn">5. Orchestration: Kubernetes and YARN</h2> <p>A cluster of 5,000 servers without coordination is like a construction site with no foremen: machines hoard resources, tasks collide, and nothing ships. <strong>Job orchestrators</strong> (Kubernetes, Hadoop YARN) solve this:</p> <ul> <li>A <strong>resource manager</strong> maintains a real-time ledger of every node’s available CPU, RAM, and storage.</li> <li>A <strong>scheduler</strong> packs incoming jobs into available resources using heuristics (bin-packing variants) — the optimal packing problem is NP-hard, so schedulers find “good enough” in milliseconds.</li> </ul> <h3 id="gang-scheduling-vs-starvation">Gang Scheduling vs. Starvation</h3> <p>If a job needs exactly 100 CPUs simultaneously, two failure modes compete:</p> <ul> <li><strong>Reserve CPUs as they free up</strong>: 80 CPUs sit locked and idle, burning nothing, while smaller jobs queue behind them.</li> <li><strong>Wait for 100 to free simultaneously</strong>: On a busy shared cluster, this might never happen. The big job starves indefinitely.</li> </ul> <p>Schedulers use priority queues, preemptive killing, and fair-share algorithms to navigate this trade-off continuously.</p> <h3 id="spot-instances">Spot Instances</h3> <p>Cloud providers rent idle capacity at 70–80% discounts as <strong>spot instances</strong> (AWS) or <strong>preemptible VMs</strong> (GCP). The catch: termination on 30 seconds’ notice with no warning.</p> <p>Batch processing is the ideal workload for spot instances precisely because of human fault tolerance. The orchestrator divides work into small, independent tasks. If a spot instance is terminated mid-task, the orchestrator restarts that specific piece on a healthy instance. Batch jobs routinely run on spot fleets at a fraction of on-demand pricing.</p> <hr/> <h2 id="6-mapreduce-shuffle-sort-and-reduce">6. MapReduce: Shuffle, Sort, and Reduce</h2> <p>MapReduce is the batch processing paradigm that made Google able to index the entire internet.</p> <pre><code class="language-mermaid">graph LR
    A[HDFS Input] --&gt; M1[Mapper 1]
    A --&gt; M2[Mapper 2]
    A --&gt; M3[Mapper 3]
    M1 --&gt;|shuffle| R1[Reducer A]
    M1 --&gt;|shuffle| R2[Reducer B]
    M2 --&gt;|shuffle| R1
    M2 --&gt;|shuffle| R2
    M3 --&gt;|shuffle| R1
    M3 --&gt;|shuffle| R2
    R1 --&gt; O[Output]
    R2 --&gt; O
</code></pre> <h3 id="the-shuffle-deterministic-routing-not-randomness">The Shuffle: Deterministic Routing, Not Randomness</h3> <p>The name “shuffle” implies randomness. It doesn’t. It’s <strong>deterministic hash-based routing</strong>:</p> <ol> <li>Each mapper reads a portion of the input, extracts a key-value pair (e.g., <code class="language-plaintext highlighter-rouge">candidate_A → 1</code>), and applies a hash function to the key.</li> <li>The hash function deterministically maps the key to a reducer number (e.g., <code class="language-plaintext highlighter-rouge">hash("candidate_A") = 4</code>).</li> <li>Every mapper independently arrives at the same destination for the same key — all <code class="language-plaintext highlighter-rouge">candidate_A</code> tallies converge on reducer 4.</li> </ol> <p>The guarantee: <strong>identical keys always arrive at the same reducer</strong>, enabling the reducer to produce a complete, correct aggregate without any cross-reducer communication.</p> <h3 id="combiners-pre-aggregation-before-the-network">Combiners: Pre-Aggregation Before the Network</h3> <p>Network bandwidth is expensive. If a mapper sees 100,000 instances of <code class="language-plaintext highlighter-rouge">candidate_A</code>, the naive implementation sends 100,000 individual messages over the network. A <strong>combiner</strong> runs locally on the mapper, aggregating those into a single <code class="language-plaintext highlighter-rouge">(candidate_A, 100000)</code> message before the shuffle. This requires the operation to be <strong>associative and commutative</strong> (addition works; computing an average does not — the average of averages is not the true average).</p> <hr/> <h2 id="7-distributed-joins-secondary-sort">7. Distributed Joins: Secondary Sort</h2> <p>A common data engineering task: join click logs (billions of rows) with a user profile table (millions of rows) to analyze whether certain demographics click different ads.</p> <p>A <strong>sort-merge join</strong> in MapReduce:</p> <ol> <li>Run mappers on both datasets, using <code class="language-plaintext highlighter-rouge">user_id</code> as the routing key.</li> <li>The shuffle guarantees all records for <code class="language-plaintext highlighter-rouge">user_id=104</code> — from both the click log and the profile table — land on the same reducer.</li> <li>The reducer receives a stream of mixed records for that user.</li> </ol> <p><strong>The memory problem</strong>: if the reducer receives the 500 clicks first and the profile record last, it must buffer all 500 clicks in RAM. A bot with 50 million clicks triggers an OOM error.</p> <p><strong>Secondary sort</strong> solves this: the framework sorts incoming records by a composite key <code class="language-plaintext highlighter-rouge">(user_id, source_dataset)</code>. Profile records are guaranteed to arrive before click records. The reducer reads the profile record, saves the birthdate in one variable, then streams through millions of clicks one at a time — attaching the stored variable without ever buffering an array.</p> <hr/> <h2 id="8-dataflow-engines-spark-and-flink">8. Dataflow Engines: Spark and Flink</h2> <p>MapReduce has a fatal structural flaw: after every map and reduce step, it writes intermediate data to disk. For a 5-step job, the data makes 10 trips through the hard drive. Even with SSDs, this is orders of magnitude slower than in-memory operations.</p> <p><strong>Dataflow engines</strong> (Apache Spark, Apache Flink) eliminate the parking-garage problem:</p> <ul> <li>They analyze the entire job upfront and build a <strong>Directed Acyclic Graph (DAG)</strong> of operations.</li> <li>Data flows from operation to operation in memory — bypassing the disk entirely except for the final output (or forced spills under memory pressure).</li> <li>Adjacent operations (e.g., filter then map) are <strong>fused</strong> into a single execution loop on the same machine, eliminating even the in-memory copy between steps.</li> </ul> <p><strong>Cost-based query optimizers</strong> take this further: if you write a terrible join order in your Spark SQL, the engine analyzes table sizes, detects the suboptimal plan, and rewrites the execution strategy. A tiny 100-row lookup table might be <strong>broadcast</strong> to every executor, eliminating the shuffle for the billion-row fact table entirely.</p> <hr/> <h2 id="9-real-world-applications">9. Real-World Applications</h2> <table> <thead> <tr> <th>Use Case</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td><strong>ETL</strong></td> <td>Extract raw production data, transform (clean, normalize, join), load into a data warehouse</td> </tr> <tr> <td><strong>Machine Learning</strong></td> <td>Pre-process training data: tokenization, embedding, deduplication — all batch jobs that run before model training</td> </tr> <tr> <td><strong>Graph algorithms</strong></td> <td>PageRank, social network analysis — iterative graph traversal via the <strong>Bulk Synchronous Parallel (BSP/Pregel)</strong> model</td> </tr> <tr> <td><strong>Data contracts</strong></td> <td>In decentralized data mesh architectures, the team owning a service is responsible for producing clean analytical outputs — batch jobs fulfill those contracts</td> </tr> </tbody> </table> <hr/> <h2 id="10-the-critical-rule-never-write-batch-output-directly-to-production">10. The Critical Rule: Never Write Batch Output Directly to Production</h2> <p>A Spark job with 1,000 parallel executors, each connecting directly to a live PostgreSQL database, will instantly exhaust the database’s connection pool and write throughput. Real users’ queries time out. Production goes down.</p> <p>The correct architecture inserts a <strong>buffer layer</strong>:</p> <pre><code class="language-mermaid">graph LR
    S[Spark Job] --&gt;|high-throughput writes| K[Kafka]
    K --&gt;|rate-controlled reads| C[Consumer Service]
    C --&gt;|gentle inserts| P[PostgreSQL]
</code></pre> <p>Or alternatively: the batch job writes perfectly formatted files to object storage, and the live database uses a bulk-load mechanism (e.g., PostgreSQL’s <code class="language-plaintext highlighter-rouge">COPY</code> command) to swap in the new data atomically — completely separating the batch cluster’s write throughput from the production serving layer.</p> <hr/> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th>Component</th> <th>Problem Solved</th> <th>Key Mechanism</th> </tr> </thead> <tbody> <tr> <td>Unix pipes</td> <td>Sequential in-memory processing</td> <td>Streaming I/O between single-purpose tools</td> </tr> <tr> <td>Merge sort on disk</td> <td>Datasets larger than RAM</td> <td>Sequential I/O; spill-and-merge chunks</td> </tr> <tr> <td>HDFS / Object stores</td> <td>Datasets larger than one disk</td> <td>Block replication; flat key-value namespace</td> </tr> <tr> <td>Orchestrators</td> <td>Cluster coordination</td> <td>Resource ledger; bin-packing scheduler</td> </tr> <tr> <td>MapReduce shuffle</td> <td>Distributed aggregation and joins</td> <td>Deterministic hash routing; secondary sort</td> </tr> <tr> <td>Spark / Flink DAG</td> <td>MapReduce’s disk I/O overhead</td> <td>In-memory dataflow; cost-based optimization</td> </tr> <tr> <td>Buffer layer</td> <td>Batch-to-production impedance mismatch</td> <td>Kafka queue; bulk-load swap</td> </tr> </tbody> </table> <p>The fundamental lesson: batch processing is <strong>high-throughput, asynchronous, and immutable</strong>. Its safety comes from never touching the input and always writing to a new location — making every failure recoverable by simply deleting the bad output and re-running.</p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="batch-processing"/><category term="mapreduce"/><category term="hadoop"/><category term="spark"/><category term="distributed-systems"/><summary type="html"><![CDATA[A junior engineer’s beautifully optimized script finishes crunching 10 million recommendations and writes them straight to the live production database — 10 million inserts per second. Two seconds later, the pager goes off. They’ve launched a self-inflicted distributed denial-of-service attack on their own company. Chapter 11 of DDIA is about why that happens, and how the tech giants process petabytes without destroying their systems.]]></summary></entry><entry><title type="html">Part 12: Stream Processing</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-12-Stream-Processing/" rel="alternate" type="text/html" title="Part 12: Stream Processing"/><published>2026-06-26T00:00:00+00:00</published><updated>2026-06-26T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%2012%20-%20Stream%20Processing</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-12-Stream-Processing/"><![CDATA[<p>Right now, as you read this, your phone is generating a continuous, unending stream of data — GPS pings, battery sensor updates, app telemetry. For decades, software engineers pretended that data stopped, artificially slicing it into overnight batches. Chapter 12 of DDIA steps out of that comfortable fiction and into the unbounded, never-ending reality of stream processing.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 12 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-limits-of-batch-stale-data">1. The Limits of Batch: Stale Data</h2> <p>Batch processing is safe and predictable. You have a bounded CSV, you run a script at midnight, you get a report. The problem: by 4 PM, that report is 16 hours stale. For fraud detection, stale is catastrophic — you can’t wait an hour to block a stolen credit card.</p> <p>The core issue is <strong>latency introduced by artificial batch boundaries</strong>. If your ETL runs at midnight and your users generate data continuously between midnight and 11:59 PM, the maximum possible latency of your analytics is 24 hours.</p> <p>Stream processing abandons those fixed slices entirely.</p> <hr/> <h2 id="2-unbounded-data-events-and-immutability">2. Unbounded Data: Events and Immutability</h2> <p>In a streaming context:</p> <ul> <li>A single record is called an <strong>event</strong>: a small, self-contained, immutable object (usually JSON or Protobuf) recording something that happened at a specific instant.</li> <li>Events are <strong>immutable</strong> — you cannot change history. A user clicked “add to cart” at 14:32:07.003. That fact is permanent.</li> <li>A dataset in the real world is <strong>never complete</strong> — unless your company shuts down, your users will produce data indefinitely.</li> </ul> <p>A data stream is therefore <strong>unbounded data</strong>: a sequence of events that has a beginning but no end.</p> <hr/> <h2 id="3-message-transport-from-polling-to-brokers">3. Message Transport: From Polling to Brokers</h2> <h3 id="why-databases-fail-as-queues">Why Databases Fail as Queues</h3> <p>The naive approach: producer writes to a Postgres table; consumer polls with <code class="language-plaintext highlighter-rouge">SELECT * FROM events WHERE processed = false</code>. At real-time throughput, the consumer runs 1,000 queries per second. 999 of them return empty. You burn CPU, exhaust connection pools, and waste bandwidth just to be told “nothing new.”</p> <h3 id="direct-messaging">Direct Messaging</h3> <p>Higher-level direct messaging (HTTP webhooks, UDP multicast) avoids polling. But if the consumer is offline for 30 seconds during a deploy, the producer gets a 500 error. Does it retry? Drop the event? Either choice is wrong.</p> <h3 id="message-brokers">Message Brokers</h3> <p>A <strong>message broker</strong> is specialized infrastructure sitting between producers and consumers:</p> <pre><code class="language-mermaid">graph LR
    P1[Producer A] --&gt; B[Message Broker]
    P2[Producer B] --&gt; B
    B --&gt; C1[Consumer 1]
    B --&gt; C2[Consumer 2]
    B --&gt; C3[Consumer 3]
</code></pre> <p>Producers write to the broker; the broker acknowledges immediately and takes responsibility for delivery. Consumers read at their own pace. Two distribution patterns:</p> <table> <thead> <tr> <th>Pattern</th> <th>Behavior</th> <th>Use Case</th> </tr> </thead> <tbody> <tr> <td><strong>Load balancing</strong></td> <td>Each message goes to exactly one consumer</td> <td>Parallel processing of a high-volume queue</td> </tr> <tr> <td><strong>Fan-out</strong></td> <td>Each message goes to every consumer</td> <td>Multiple independent services react to the same event</td> </tr> </tbody> </table> <h3 id="acknowledgments-and-the-poison-pill-problem">Acknowledgments and the Poison Pill Problem</h3> <p>Consumers send an <strong>ACK</strong> after successfully processing a message. If the consumer crashes before ACKing, the broker re-delivers the message to another consumer — preventing data loss.</p> <p>But what if the message itself is broken? A malformed JSON payload with a missing <code class="language-plaintext highlighter-rouge">user_id</code> field throws a null pointer exception. The consumer crashes. The broker re-delivers the same bad message. The consumer crashes again. In a loop. The queue backs up. The entire stream is paralyzed by one <strong>poison pill</strong>.</p> <p>The fix: <strong>Dead Letter Queue (DLQ)</strong>. After N consecutive failed attempts (e.g., 5), the broker automatically routes the offending message to a separate quarantine queue. Healthy messages keep flowing. A human engineer inspects the DLQ, fixes the root cause, and replays the message later.</p> <hr/> <h2 id="4-log-based-brokers-kafka">4. Log-Based Brokers: Kafka</h2> <p>Traditional message brokers delete messages after a consumer ACKs them. If a new consumer wants to replay last month’s events, those events are gone.</p> <p>Apache Kafka, originally built at LinkedIn, solved this by fusing the durability of a database with the pub-sub model of a broker.</p> <h3 id="architecture">Architecture</h3> <p>A Kafka <strong>topic</strong> is partitioned into multiple <strong>partitions</strong>, each an append-only log written directly to disk.</p> <pre><code class="language-mermaid">graph LR
    P[Producer] --&gt;|append| L[Partition Log]
    C1[Consumer A] --&gt;|read from offset 100| L
    C2[Consumer B] --&gt;|read from offset 47| L
</code></pre> <ul> <li>Producers <strong>append</strong> to the end of the log — no random writes.</li> <li>Consumers <strong>read sequentially</strong> and maintain an <strong>offset</strong> (an integer bookmark) to record their position.</li> <li><strong>Reading is non-destructive</strong> — the consumer never deletes anything.</li> </ul> <h3 id="why-sequential-disk-writes-are-fast">Why Sequential Disk Writes Are Fast</h3> <p>Appending to a file is purely sequential I/O. Spinning disks stream sequential data at 150 MB/s+; SSDs are even faster due to prefetching. The OS buffers sequential writes in the <strong>page cache</strong> in RAM. Consumers reading recent data serve it directly from page cache — <strong>zero disk reads</strong>. This “zero-copy optimization” is why Kafka sustains millions of messages per second on commodity hardware.</p> <h3 id="retention-at-scale">Retention at Scale</h3> <p>A 20 TB disk writing at 250 MB/s takes ~22 hours to fill. At realistic production loads (bursts, not sustained maximum), a Kafka cluster easily retains weeks or months of raw events. This gives consumers a superpower: a new experimental service can rewind to an offset from a week ago and replay actual production data with zero risk to the source.</p> <hr/> <h2 id="5-change-data-capture-taming-the-dual-write-problem">5. Change Data Capture: Taming the Dual-Write Problem</h2> <p>A typical architecture has Postgres as the transactional source of truth, plus a Redis cache, an Elasticsearch index, and a Snowflake data warehouse. Keeping them in sync via application-level dual writes is a silent data corruption waiting to happen:</p> <ul> <li>Client A sets a field to <code class="language-plaintext highlighter-rouge">A</code>.</li> <li>Client B sets the same field to <code class="language-plaintext highlighter-rouge">B</code>.</li> <li>Postgres records <code class="language-plaintext highlighter-rouge">B</code> as the final value (processes A first, then B).</li> <li>Elasticsearch receives the packets in reverse order (network jitter) and records <code class="language-plaintext highlighter-rouge">A</code> as the final value.</li> <li>Both systems returned HTTP 200. No exception was thrown.</li> </ul> <p><strong>Change Data Capture (CDC)</strong> fixes this by making the primary database the single source of ordered truth. Tools like <strong>Debezium</strong> disguise themselves as a Postgres read replica, consuming the internal <strong>Write-Ahead Log (WAL)</strong>. The WAL is the database’s own crash recovery mechanism — an append-only log of every committed change. Debezium reads it and publishes each change as a JSON event to Kafka, including the <code class="language-plaintext highlighter-rouge">before</code> and <code class="language-plaintext highlighter-rouge">after</code> state of every row.</p> <p>All downstream systems — search index, cache, warehouse — become passive consumers of that ordered stream. They cannot receive updates out of order, because the database’s WAL defines the canonical sequence.</p> <hr/> <h2 id="6-log-compaction-bounded-infinity">6. Log Compaction: Bounded Infinity</h2> <p>If you store every insert/update/delete event forever, the log grows without bound. But a new consumer starting from scratch doesn’t care about John’s 50 historical username typos — it only needs his current username.</p> <p><strong>Log compaction</strong> runs a background process that scans the append-only log, groups events by primary key, and discards all but the most recent event for each key. The log transitions from a full history to a <strong>compact snapshot of current state</strong> — still ordered, still immutable, but garbage-collected of superseded records.</p> <hr/> <h2 id="7-state-as-the-integral-of-a-stream">7. State as the Integral of a Stream</h2> <p>A profound mathematical identity from the chapter:</p> <blockquote> <p><strong>State = ∫ stream dt</strong></p> <p><strong>Stream = d(state)/dt</strong></p> </blockquote> <p>Your bank account balance (state) is the sum of every deposit and withdrawal (stream) over time. The ledger of transactions is the stream; the current balance is its integral.</p> <p>This reframes data modeling. Instead of asking “what is the current state?” you ask “how did we arrive at the current state?” This shift has a practical implication: if your database state is corrupted by a bug, you don’t write recovery scripts. You drop the corrupted tables, fix the bug, and replay the immutable event log from offset zero. The correct state rebuilds itself deterministically.</p> <hr/> <h2 id="8-time-event-time-vs-processing-time">8. Time: Event Time vs. Processing Time</h2> <p>When a product manager asks for a “5-minute rolling average of checkout revenue,” you must decide what “5 minutes” means:</p> <table> <thead> <tr> <th>Time concept</th> <th>Definition</th> <th>Risk</th> </tr> </thead> <tbody> <tr> <td><strong>Processing time</strong></td> <td>The server’s clock when it receives the event</td> <td>A network outage causes 1 hour of events to arrive simultaneously — instant spike in metrics that never happened in reality</td> </tr> <tr> <td><strong>Event time</strong></td> <td>The timestamp embedded in the event payload by the originating device</td> <td>Late arrivals (subway tunnels, poor signal) complicate windowing</td> </tr> </tbody> </table> <p><strong>Always window by event time.</strong> Processing time produces metrics that are detached from reality whenever the network is unreliable.</p> <p>The cost: you must decide how long to wait for <strong>straggler events</strong> — data that physically occurred inside a window but arrived late. Stream engines like Apache Flink use <strong>watermarks</strong>: heuristic signals declaring “I am reasonably confident I’ve received all events older than T.” Stragglers arriving after the watermark require a policy choice:</p> <ul> <li><strong>Drop</strong>: acceptable for rough trend analysis, catastrophic for billing.</li> <li><strong>Publish corrections</strong>: output a preliminary result when the watermark passes, then emit an updated result (a retraction) when the straggler arrives. Downstream systems must handle retractions.</li> </ul> <hr/> <h2 id="9-window-types">9. Window Types</h2> <table> <thead> <tr> <th>Window</th> <th>Description</th> <th>Use Case</th> </tr> </thead> <tbody> <tr> <td><strong>Tumbling</strong></td> <td>Fixed length, no overlap. Event belongs to exactly one window.</td> <td>Hourly aggregation buckets</td> </tr> <tr> <td><strong>Hopping</strong></td> <td>Fixed length, overlapping. A 5-min window hopping every 1 min produces a rolling average.</td> <td>Smoothed dashboards</td> </tr> <tr> <td><strong>Sliding</strong></td> <td>Groups events within a proximity of each other, regardless of absolute time.</td> <td>“3 login failures within 5 minutes” fraud detection</td> </tr> <tr> <td><strong>Session</strong></td> <td>Groups a single user’s activity bursts; window closes after a period of inactivity.</td> <td>User engagement and session length</td> </tr> </tbody> </table> <hr/> <h2 id="10-stream-joins-three-types">10. Stream Joins: Three Types</h2> <p>Joining streams is vastly more complex than joining database tables because the data is infinite and arrives at uncontrolled rates.</p> <h3 id="stream-stream-join">Stream-Stream Join</h3> <p>Join a “search query” stream with a “click” stream to calculate click-through rates. The stream processor must hold the search event in RAM for a configurable window (e.g., 10 minutes), waiting for a matching click. High query volumes mean significant memory pressure.</p> <h3 id="stream-table-join-enrichment">Stream-Table Join (Enrichment)</h3> <p>Enrich a high-volume event stream with a static reference table (e.g., join <code class="language-plaintext highlighter-rouge">user_id</code> events with a user profile database to add demographic data). The naive approach — querying Postgres on every event — destroys the database.</p> <p>The solution: <strong>local hash join</strong>. Using CDC, the stream processor maintains a continuously updated local copy of the reference table inside its own embedded store (e.g., RocksDB). Enrichment lookups become in-process memory accesses — no network round-trips, no database load.</p> <h3 id="table-table-join-materialized-view">Table-Table Join (Materialized View)</h3> <p>Maintain a real-time pre-computed join. Example: Twitter’s timeline cache — the join of “posts” and “follower graph” — is continuously updated as new posts arrive or follows change. The stream processor continuously recalculates and caches the result.</p> <hr/> <h2 id="11-fault-tolerance-checkpointing-and-idempotence">11. Fault Tolerance: Checkpointing and Idempotence</h2> <p>If a stream processor crashes mid-window, all its buffered state is lost. Two approaches:</p> <h3 id="micro-batching-spark-streaming">Micro-batching (Spark Streaming)</h3> <p>Slice the infinite stream into 1-second mini-batches and treat each as a tiny batch job. If a mini-batch fails, discard it and retry from the source log. Simple, but introduces 1-second minimum latency.</p> <h3 id="distributed-snapshots-flink">Distributed Snapshots (Flink)</h3> <p>Periodically inject <strong>barrier markers</strong> into the stream. When the processor sees a barrier, it snapshots all in-memory state (windows, local join tables, Kafka offsets) to durable storage (e.g., S3). On crash, reload the last snapshot and replay from the saved offset. No mini-batch latency penalty.</p> <h3 id="idempotence-the-universal-safety-net">Idempotence: The Universal Safety Net</h3> <p>Design every output operation to be <strong>idempotent</strong>: applying it N times produces the same result as applying it once.</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- Not idempotent: each retry increments the counter</span>
<span class="k">UPDATE</span> <span class="k">table</span> <span class="k">SET</span> <span class="n">counter</span> <span class="o">=</span> <span class="n">counter</span> <span class="o">+</span> <span class="mi">1</span> <span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>

<span class="c1">-- Idempotent: duplicate writes are harmless</span>
<span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">processed</span> <span class="p">(</span><span class="n">kafka_offset</span><span class="p">,</span> <span class="k">result</span><span class="p">)</span> <span class="k">VALUES</span> <span class="p">(</span><span class="mi">42</span><span class="p">,</span> <span class="s1">'done'</span><span class="p">)</span>
  <span class="k">ON</span> <span class="n">CONFLICT</span> <span class="p">(</span><span class="n">kafka_offset</span><span class="p">)</span> <span class="k">DO</span> <span class="k">NOTHING</span><span class="p">;</span>
</code></pre></div></div> <p>Using the Kafka offset as a unique key in the output database turns every insert into an idempotent upsert. A crash-and-replay produces a duplicate write attempt; the primary key constraint silently rejects it. Exactly-once semantics achieved without distributed locking.</p> <hr/> <h2 id="12-gdpr-and-crypto-shredding">12. GDPR and Crypto-Shredding</h2> <p>Immutable append-only logs and GDPR’s “right to be forgotten” seem fundamentally contradictory — you cannot delete from an immutable log without destroying consumer offsets.</p> <p>The industry’s solution: <strong>crypto-shredding</strong>. Instead of storing plaintext sensitive fields in the event, encrypt them with a user-specific key before appending. Store the keys in a separate, mutable key store. When a user exercises the right to erasure, delete their encryption key. The encrypted data remains in the log permanently — but it is now mathematically unreadable gibberish, satisfying the compliance requirement without breaking the log’s append-only guarantee.</p> <hr/> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th>Topic</th> <th>Key Insight</th> </tr> </thead> <tbody> <tr> <td>Unbounded data</td> <td>Real-world data streams never end; batch boundaries introduce artificial latency</td> </tr> <tr> <td>Message brokers</td> <td>Decouple producers from consumers; ACKs + DLQ prevent poison pills</td> </tr> <tr> <td>Log-based brokers</td> <td>Non-destructive reads; offset bookmarks; consumers can replay history</td> </tr> <tr> <td>CDC</td> <td>Database WAL as the canonical ordered event stream; fixes dual-write corruption</td> </tr> <tr> <td>Log compaction</td> <td>Compact current state from full history; bounding log size without losing truth</td> </tr> <tr> <td>Event time</td> <td>Always window by event time; watermarks manage straggler events</td> </tr> <tr> <td>Windows</td> <td>Tumbling, hopping, sliding, session — each matches a different analytical pattern</td> </tr> <tr> <td>Stream joins</td> <td>Stream-stream (RAM-intensive); stream-table (local hash join); table-table (materialized view)</td> </tr> <tr> <td>Fault tolerance</td> <td>Micro-batching or Flink snapshots; idempotent output operations</td> </tr> <tr> <td>Crypto-shredding</td> <td>GDPR compliance on immutable logs via key deletion</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="stream-processing"/><category term="kafka"/><category term="messaging"/><category term="event-sourcing"/><category term="cdc"/><summary type="html"><![CDATA[Right now, as you read this, your phone is generating a continuous, unending stream of data — GPS pings, battery sensor updates, app telemetry. For decades, software engineers pretended that data stopped, artificially slicing it into overnight batches. Chapter 12 of DDIA steps out of that comfortable fiction and into the unbounded, never-ending reality of stream processing.]]></summary></entry><entry><title type="html">Part 13: A Philosophy of Streaming Systems</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-13-A-Philosophy-of-Streaming-Systems/" rel="alternate" type="text/html" title="Part 13: A Philosophy of Streaming Systems"/><published>2026-06-26T00:00:00+00:00</published><updated>2026-06-26T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%2013%20-%20A%20Philosophy%20of%20Streaming%20Systems</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-13-A-Philosophy-of-Streaming-Systems/"><![CDATA[<p>A global bank handles billions of dollars flawlessly, even though it is mathematically impossible to know anyone’s true account balance at any given millisecond — because the underlying databases are scattered across continents and thousands of transactions fly through the system every second. Chapter 13 of DDIA takes all the isolated concepts of the previous chapters and weaves them into a unified theory of how to actually build modern, reliable software at scale.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 13 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-integration-dilemma">1. The Integration Dilemma</h2> <p>Every application that grows beyond a hobby project encounters the same crisis: no single database technology can serve all its access patterns.</p> <ul> <li><strong>OLTP database</strong> (Postgres): brilliant for concurrent row-level updates — user signups, shopping cart changes. Terrible for fuzzy full-text search (B-trees are the wrong index structure).</li> <li><strong>Search cluster</strong> (Elasticsearch): optimized for inverted-index lookups. Useless for transactional integrity.</li> <li><strong>Analytics warehouse</strong> (Snowflake): columnar storage, fast for aggregating billions of rows. Catastrophically slow for individual row updates.</li> </ul> <p>So teams cobble together these specialized tools. The next problem: keeping them in sync. Application-level dual writes are a silent data corruption factory. Two clients write the same field concurrently — Postgres applies them in order A → B, Elasticsearch receives them as B → A (network jitter). Both systems return HTTP 200. The inconsistency is invisible until a user complains.</p> <p><strong>Change Data Capture</strong> solves this by making the primary database the single canonical timeline. The database’s WAL defines the definitive order; all other systems become passive followers of that ordered stream.</p> <hr/> <h2 id="2-the-limits-of-total-ordering">2. The Limits of Total Ordering</h2> <p>CDC enforces a single ordered log — but only within one database instance. Once a system must scale globally, total ordering breaks down:</p> <ul> <li><strong>Sharding</strong> splits data across multiple machines, each with its own write log.</li> <li><strong>Multiple data centers</strong> make synchronous cross-site coordination impossible at the speed of light.</li> <li><strong>Microservices</strong> give each service its own database — the payments service and the user service have no shared log.</li> </ul> <p>When events originate from separate sources, there is no universally defined order. Deciding a global order requires distributed consensus protocols — multiple network round-trips — which destroys the performance gains of sharding.</p> <h3 id="the-causality-problem">The Causality Problem</h3> <p>Kleppmann’s illustrative example: User A unfriends User B, then immediately sends a rude message to friends. User A’s causal intent is clear: the unfriend <em>precedes</em> the rude message. But if friendship status and messages live in separate microservices with separate logs, a notification service might consume the message event before the unfriend event due to network lag — and push the rude message directly to User B.</p> <p>The causal dependency was never captured or enforced. Solving this without a single bottleneck log requires vector clocks or causal-ordering protocols — notoriously difficult to implement correctly.</p> <hr/> <h2 id="3-bounded-vs-unbounded-the-processor-spectrum">3. Bounded vs. Unbounded: The Processor Spectrum</h2> <p>At their core, batch processors and stream processors share the same algebra: filter, map, group, join, aggregate. The architectural difference is the nature of the input:</p> <table> <thead> <tr> <th> </th> <th>Batch Processor</th> <th>Stream Processor</th> </tr> </thead> <tbody> <tr> <td>Dataset</td> <td>Bounded (static file with a known end)</td> <td>Unbounded (infinite sequence of events)</td> </tr> <tr> <td>Execution</td> <td>Job finishes and stops</td> <td>Runs continuously, indefinitely</td> </tr> <tr> <td>Historical replay</td> <td>Trivial (re-read the file)</td> <td>Requires log-based broker with durable retention</td> </tr> </tbody> </table> <p>The boundary between these paradigms has been dissolving for years. Reprocessing historical data is not just for analytics — it is a core tool for <strong>application evolution</strong>.</p> <hr/> <h2 id="4-schema-migrations-the-railway-gauge-analogy">4. Schema Migrations: The Railway Gauge Analogy</h2> <p>In 1840s England, rival railway companies built tracks with incompatible gauges. A train built for the Great Western Railway could not run on tracks built by the London and North Western Railway. The government mandated a standard gauge in 1846, but converting thousands of kilometers of track without shutting down the national economy required an ingenious solution: <strong>a third rail added alongside the existing two</strong> (dual gauge), allowing both types of trains to operate simultaneously during the transition. Once all rolling stock was converted, the obsolete rail was removed.</p> <p>This is exactly how stream-based architectures handle schema migrations. If your application must restructure a critical database table:</p> <ol> <li>Write a new stream processing job that replays the full historical event log from offset zero.</li> <li>The job transforms old-schema events into the new schema and writes them to a new, separate database.</li> <li>The old and new databases run simultaneously — powered by the same immutable event log.</li> <li>Gradually shift 1% → 10% → 100% of traffic to the new system.</li> <li>Once the new system is stable, delete the old database.</li> </ol> <p>If anything breaks, the immutable log guarantees you can rebuild the new database from scratch again with no risk to the source. The old system runs unaffected throughout.</p> <hr/> <h2 id="5-lambda-vs-kappa-architecture">5. Lambda vs. Kappa Architecture</h2> <h3 id="lambda-architecture">Lambda Architecture</h3> <p>Historically, teams maintained <strong>two separate codebases</strong>:</p> <ul> <li>A <strong>batch layer</strong> (Hadoop) crunching complete historical data for accuracy.</li> <li>A <strong>speed layer</strong> (Storm) processing live events for recency.</li> <li>A <strong>serving layer</strong> merging the two outputs at query time.</li> </ul> <p>The problem: two codebases computing the same business logic. Bugs crept in when they diverged. Maintaining both was a significant operational burden.</p> <h3 id="kappa-architecture">Kappa Architecture</h3> <p>The modern response: <strong>throw away the batch processor entirely</strong>. Use the same stream processing code for everything:</p> <ul> <li>To process historical data: rewind the Kafka offset to zero and replay.</li> <li>When the stream processor catches up to the present: it seamlessly transitions to processing live events, never stopping.</li> </ul> <p>One codebase. One execution model. The stream processor connects to the storage layer, reads historical data as fast as the disks can spin, and when it reaches “now,” simply keeps running.</p> <hr/> <h2 id="6-exactly-once-semantics">6. Exactly-Once Semantics</h2> <p>The Kappa architecture requires a difficult guarantee: if the stream processor crashes halfway through three years of historical data and reboots, it cannot produce duplicate outputs or lose records.</p> <p>The mechanism:</p> <ol> <li>The processor tracks position via a Kafka <strong>offset</strong> (an integer).</li> <li>When ready to commit a batch of output, it <strong>wraps the output and the new offset in a single atomic transaction</strong>: either both succeed or neither does.</li> <li>On crash, the processor restarts from the last successfully committed offset.</li> <li>Any partial writes from the failed attempt are invisible to downstream consumers (they check committed state only).</li> </ol> <p>The developer sees exactly-once semantics. Physically, the machine may retry computations many times. The transactional commit marker hides that complexity.</p> <hr/> <h2 id="7-unbundling-the-database-the-unix-philosophy-at-scale">7. Unbundling the Database: The Unix Philosophy at Scale</h2> <p>Unix builds complex pipelines from small, single-purpose tools connected by pipes. Relational databases do the opposite: a monolithic system that hides all complexity behind SQL.</p> <p>What does it mean to apply the Unix philosophy at the data center level?</p> <p>A traditional database’s <code class="language-plaintext highlighter-rouge">CREATE INDEX</code> command scans the table, builds a B-tree structure, and continuously watches the WAL to keep the index updated. <strong>Unbundling</strong> extracts this mechanism into a system-wide architecture:</p> <ul> <li>The primary database appends writes to a distributed event log (the Unix pipe).</li> <li>A separate stream processing job acts as the index builder: it consumes the log and writes the structured index into a specialized Elasticsearch cluster.</li> <li>The search cluster is not a standalone product — it is a <strong>continuously updated secondary index</strong> of the primary log, updated asynchronously.</li> </ul> <p>Extend this to every derived system:</p> <pre><code class="language-mermaid">graph LR
    DB[Primary Database] --&gt;|WAL / CDC| K[Event Log]
    K --&gt; ES[Elasticsearch Index]
    K --&gt; R[Redis Cache]
    K --&gt; DW[Data Warehouse]
    K --&gt; ML[ML Feature Store]
</code></pre> <p>The entire organization’s data flow becomes one giant distributed database. The log is the central commit log. Indexes, caches, and analytics stores are specialized secondary views of that log.</p> <h3 id="loose-coupling-as-a-fault-isolation-tool">Loose Coupling as a Fault Isolation Tool</h3> <p>In a tightly coupled architecture, if the search cluster goes offline for maintenance, every user profile update fails (the synchronous write to Elasticsearch times out). In the unbundled, event-driven architecture:</p> <ul> <li>The application writes to the primary database only.</li> <li>The log buffers events on disk during the outage.</li> <li>When the search cluster recovers, it reads the backlog and catches up at high speed.</li> </ul> <p>The application was never disrupted. Components fail independently without cascading.</p> <hr/> <h2 id="8-federated-vs-unbundled-databases">8. Federated vs. Unbundled Databases</h2> <table> <thead> <tr> <th>Approach</th> <th>What it solves</th> <th>What it doesn’t</th> </tr> </thead> <tbody> <tr> <td><strong>Federated</strong> (Presto/Trino)</td> <td>Unifies the <strong>read path</strong> — one SQL interface over Postgres, S3, Neo4j</td> <td>Doesn’t synchronize writes; downstream systems still diverge</td> </tr> <tr> <td><strong>Unbundled</strong> (Kafka + CDC)</td> <td>Unifies the <strong>write path</strong> — single ordered log drives all derived systems</td> <td>Reads still hit specialized engines directly</td> </tr> </tbody> </table> <p>Modern architectures typically use both: CDC keeps all systems in sync (unbundled write path), and a federated query engine joins across those systems for analytics (unified read path).</p> <hr/> <h2 id="9-dataflow-programming-from-visicalc-to-the-data-center">9. Dataflow Programming: From VisiCalc to the Data Center</h2> <p><strong>VisiCalc (1979)</strong> was the first electronic spreadsheet — and a pure dataflow programming environment. When you change cell A1, the spreadsheet detects B1’s dependency on A1 and automatically recalculates B1. No polling. No scripts. The system understands the dependency graph and propagates state changes through it reactively.</p> <p>Modern application architectures are trying to achieve exactly this, at the scale of a global data center: when a user updates their profile, that state change should automatically flow through the dependency graph — recalculating the search index, invalidating the cache, updating the recommendation model — just as a spreadsheet cell updates.</p> <p>Executing that dependency graph across thousands of servers requires stream processing engines. The underlying philosophy is identical to VisiCalc.</p> <hr/> <h2 id="10-the-write-path-vs-the-read-path">10. The Write Path vs. The Read Path</h2> <p>Every data system has a boundary between two types of computation:</p> <pre><code class="language-mermaid">graph LR
    U[User Action] --&gt; WP[Write Path: Eager Evaluation]
    WP --&gt; I[Derived Index / Materialized View]
    I --&gt; RP[Read Path: Lazy Evaluation]
    RP --&gt; Q[User Query]
</code></pre> <ul> <li><strong>Write path (eager)</strong>: computation happens when data arrives, regardless of whether anyone has asked for it yet. Cost: CPU spent even on data nobody ever queries.</li> <li><strong>Read path (lazy)</strong>: computation happens when a user makes a request. Cost: latency on every query; may require scanning millions of rows.</li> </ul> <p>The <strong>materialized view</strong> is the architectural tool for shifting work from the read path to the write path. Instead of scanning a billion rows to count available software engineering books on every query, maintain a counter that increments on checkout and decrements on return. The read becomes a constant-time lookup.</p> <p>The art of systems architecture is deciding where to draw the boundary. Shift too much to the write path: expensive pre-computation for queries that rarely happen. Shift too little: read queries become unbearably slow.</p> <hr/> <h2 id="11-extending-dataflow-to-clients">11. Extending Dataflow to Clients</h2> <p>If the write path already pushes state changes through the entire data center — from primary database to Kafka to search index — why stop at the data center’s edge router?</p> <p>Modern protocols (WebSockets, Server-Sent Events) maintain persistent connections between server and client. A user’s smartphone is not a static HTML snapshot — it is a <strong>local cache of the remote data center’s state</strong>.</p> <p>When a teammate updates a collaborative document on their phone in Tokyo:</p> <ol> <li>The event flows to the server and into the distributed log.</li> <li>The stream engine processes it.</li> <li>The result is pushed down the open WebSocket to your browser in London.</li> <li>The browser’s reactive UI framework (React’s virtual DOM, Elm’s architecture) recalculates and re-renders — automatically, because it was already built on reactive dataflow principles.</li> </ol> <p>This architecture also enables <strong>offline-first applications</strong>: when the Tokyo user loses signal in a subway tunnel, the app continues appending local edits to an on-device log (IndexedDB). On reconnection, the local log synchronizes with the server log, resolving conflicts via the stream processor.</p> <hr/> <h2 id="12-timeliness-vs-integrity">12. Timeliness vs. Integrity</h2> <p>Software engineers arguing about “consistency” often talk past each other because they conflate two distinct properties:</p> <table> <thead> <tr> <th>Property</th> <th>Definition</th> <th>Failure mode</th> </tr> </thead> <tbody> <tr> <td><strong>Timeliness</strong></td> <td>Seeing up-to-date data promptly</td> <td>Stale reads (temporary, self-healing)</td> </tr> <tr> <td><strong>Integrity</strong></td> <td>Absence of corruption; no data silently dropped or calculated incorrectly</td> <td>Permanent data corruption</td> </tr> </tbody> </table> <p>The key rule of scalable systems:</p> <blockquote> <p><strong>Violations of timeliness are acceptable. Violations of integrity are not.</strong></p> </blockquote> <p>A bank transfer that takes 60 seconds to appear in your balance is a timeliness violation — annoying, but it resolves itself. A bank transfer that charges you twice is an integrity violation — permanent, requiring manual intervention.</p> <p>Eventual consistency sacrifices timeliness in exchange for availability and scalability. It is not the same as sacrificing integrity.</p> <hr/> <h2 id="13-the-art-of-the-apology-compensating-transactions">13. The Art of the Apology: Compensating Transactions</h2> <p>Enforcing hard constraints synchronously in a distributed system requires distributed locking (2PC), which has brutal throughput implications. The alternative: <strong>accept eventual inconsistency, detect conflicts asynchronously, and compensate</strong>.</p> <p><strong>Airlines deliberately overbook flights.</strong> Enforcing a strict “max 150 seats” constraint with a distributed lock would prevent selling ticket 151 until 150 other transactions complete — unnecessary overhead on every ticket purchase. Instead, airlines allow overselling, detect it at gate time, and compensate with vouchers and rebooking.</p> <p>The <strong>compensating transaction</strong> is not a failure mode — it is a deliberate architectural choice. The system maintains high write throughput (no locks), accepts that inconsistencies will occasionally materialize in the physical world, and handles them with automated business processes.</p> <blockquote> <p>If the cost of the apology (refund, discount code, email) is low relative to the cost of enforcing the constraint (distributed locks, latency, complexity), accept eventual consistency and build the apology process.</p> </blockquote> <hr/> <h2 id="14-trust-but-verify-checksums-and-merkle-trees">14. Trust but Verify: Checksums and Merkle Trees</h2> <p>Even with immutable event logs and deterministic stream processors, physical hardware is imperfect:</p> <ul> <li>A high-energy cosmic ray flips a RAM bit from 0 to 1.</li> <li>A degraded disk controller silently writes garbage bytes.</li> <li>A logic bug corrupts thousands of rows before anyone notices.</li> </ul> <p>Mature distributed storage systems (HDFS, S3) don’t blindly trust hardware. When writing a file, they compute a <strong>cryptographic checksum</strong> and store it alongside the data. Background daemons continuously crawl the disks, recompute checksums, and compare them against originals and replicas. Detected corruption triggers automatic healing from a healthy replica.</p> <p>The event sourcing architecture amplifies this. If you suspect your production database state has been corrupted by yesterday’s bug:</p> <ol> <li>Take the original immutable event log.</li> <li>Replay it through a fixed, bug-free version of the stream processor in a staging environment.</li> <li>Mathematically compare the freshly generated output to the corrupted production database.</li> </ol> <p>The immutable log is a time machine for auditing.</p> <p><strong>Merkle trees</strong> take this further for adversarial environments (public blockchains). Every transaction is hashed; pairs of hashes are concatenated and hashed again; the process repeats up a tree until a single <strong>root hash</strong> represents the entire dataset. Altering any single transaction cascades through the tree, changing the root hash — making tampering mathematically detectable even when half the nodes are actively lying. Each block embeds the previous block’s root hash, creating an unforgeable chain of custody.</p> <hr/> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th>Concept</th> <th>Core Insight</th> </tr> </thead> <tbody> <tr> <td>Integration dilemma</td> <td>No single DB fits all access patterns; CDC + event log keeps derived systems in sync</td> </tr> <tr> <td>Causality</td> <td>Losing total order at scale creates causal violations that require vector clocks</td> </tr> <tr> <td>Kappa architecture</td> <td>One stream processor for both historical replay and live processing</td> </tr> <tr> <td>Exactly-once</td> <td>Atomic commit of output + offset; physical retries hidden from downstream</td> </tr> <tr> <td>Unbundled database</td> <td>Log as Unix pipe; search indexes and caches as specialized secondary indexes</td> </tr> <tr> <td>Write path vs. read path</td> <td>Materialized views shift computation from queries to ingestion</td> </tr> <tr> <td>Timeliness vs. integrity</td> <td>Eventual consistency sacrifices timeliness, never integrity</td> </tr> <tr> <td>Compensating transactions</td> <td>Accept inconsistency; automate the apology</td> </tr> <tr> <td>Merkle trees</td> <td>Cryptographic tamper-evidence for adversarial distributed environments</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="stream-processing"/><category term="database-architecture"/><category term="event-driven"/><category term="cdc"/><category term="lambda-kappa"/><summary type="html"><![CDATA[A global bank handles billions of dollars flawlessly, even though it is mathematically impossible to know anyone’s true account balance at any given millisecond — because the underlying databases are scattered across continents and thousands of transactions fly through the system every second. Chapter 13 of DDIA takes all the isolated concepts of the previous chapters and weaves them into a unified theory of how to actually build modern, reliable software at scale.]]></summary></entry><entry><title type="html">Part 14: Doing the Right Thing</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-14-Doing-the-Right-Thing/" rel="alternate" type="text/html" title="Part 14: Doing the Right Thing"/><published>2026-06-26T00:00:00+00:00</published><updated>2026-06-26T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%2014%20-%20Doing%20the%20Right%20Thing</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-14-Doing-the-Right-Thing/"><![CDATA[<p>Imagine a physically perfect bridge — flawless steel, mathematically correct load calculations. But the automated toll booth at the entrance systematically refuses entry to certain people, not because they can’t pay, but because an opaque software system associated them with a demographic that caused a traffic jam five years ago. The code compiles. The servers run at optimal capacity. On paper, the system is a triumph of engineering. The societal outcome is a disaster. Chapter 14 of DDIA forces a transition from the <em>how</em> of building systems to the <em>why</em>.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 14 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-illusion-of-neutral-technology">1. The Illusion of Neutral Technology</h2> <p>Technology is often framed as neutral — a hammer can build a house or break a window; the hammer itself has no moral stance. The chapter challenges this framing for data systems.</p> <p>When an engineer stares at a database schema, they see strings, integers, and booleans. But those abstractions represent human behaviors, human interests, and human identities. A <code class="language-plaintext highlighter-rouge">Boolean isEligible = false</code> flag is not neutral:</p> <blockquote> <p>That flag might mean a single mother can’t get a loan to repair her car, which means she can’t get to work, which means she loses her job.</p> </blockquote> <p>The ACM Code of Ethics has existed for decades and is almost never discussed in engineering practice. Teams optimize for uptime and feature velocity, ignoring the social fallout downstream.</p> <hr/> <h2 id="2-the-engineers-responsibility">2. The Engineer’s Responsibility</h2> <p>A common defense: “Ethics is the product manager’s job. I’m just executing sprint tickets.”</p> <p>The chapter refutes this directly. The software engineer is the one writing the logic that executes business decisions. Every day, they make micro-architectural choices that dictate how a system behaves at scale:</p> <ul> <li>How to handle edge cases</li> <li>What default values to assign</li> <li>How to structure the data model</li> <li>What data to log and retain</li> </ul> <blockquote> <p>You cannot evade responsibility by hiding behind “the algorithm made the decision.” The algorithm only does what the code — and the data it was trained on — tell it to do.</p> </blockquote> <p>Unlike a compiler, which throws a deterministic syntax error, ethics require continuous interpretation and dialogue. What one demographic considers a helpful personalized feature, another may consider a privacy violation. This ambiguity makes engineering ethics an ongoing practice, not a one-time compliance checkbox.</p> <hr/> <h2 id="3-predictive-analytics-and-the-algorithmic-prison">3. Predictive Analytics and the Algorithmic Prison</h2> <p>Predictive analytics in beneficial contexts — weather forecasting, epidemic modeling, traffic routing — are genuinely valuable. They become dangerous when applied to human gatekeeping: predicting criminal recidivism, mortgage default risk, or job performance.</p> <p>Businesses that deploy these systems are motivated by risk minimization. From their perspective, the cost of a false negative (denying a good loan) is a missed profit opportunity. The cost of a false positive (approving a bad loan) can be severe. So the algorithms are tuned to be conservative — when statistical confidence drops, the system defaults to <strong>No</strong>.</p> <p>The result is what the text calls the <strong>algorithmic prison</strong>: an individual labeled “high-risk” by an automated system is systematically excluded from employment, housing, and financial services. Unlike the criminal justice system:</p> <ul> <li>There is no presumption of innocence.</li> <li>There is no transparent court hearing.</li> <li>There is often no mechanism for appeal.</li> <li>The decision arrives as a generic error message.</li> </ul> <p>The exclusion operates in the dark.</p> <hr/> <h2 id="4-machine-learning-as-money-laundering-for-bias">4. Machine Learning as “Money Laundering for Bias”</h2> <p>Machine learning models do exactly one thing: extrapolate patterns from historical training data. If that data contains human biases — and it almost certainly does, because human history is deeply biased — the model doesn’t sanitize the bias. It encodes it mathematically into feature weights and amplifies it at unprecedented scale.</p> <blockquote> <p>“Machine learning is like money laundering for bias.”</p> </blockquote> <p>When a human makes a biased decision, you can challenge them. You can demand reasoning. You can hold them accountable. When that same human prejudice is passed through the hidden layers of a neural network, it emerges on the other side disguised as <strong>clean, objective, mathematical truth</strong>. People look at the output and say: “The math indicates you are a high-risk candidate, so it must be true.” The prejudice has been laundered through an algorithm to appear as empirical science.</p> <hr/> <h2 id="5-feedback-loops-and-systemic-failures">5. Feedback Loops and Systemic Failures</h2> <p>The situation becomes worse when the algorithm’s decisions actively alter reality to generate the data that justifies its next decision.</p> <h3 id="the-joblessness-spiral">The Joblessness Spiral</h3> <ol> <li>An employee contracts a severe illness — a random, uncontrollable event.</li> <li>They miss a month of work, burn through savings, and miss a utility bill. Their credit score drops.</li> <li>On recovery, they apply for a new job. The employer’s applicant tracking system uses credit scores as a feature weight. Low score → automatic rejection before a human sees the resume.</li> <li>Unemployed and in financial distress, they miss more bills. Credit score drops further.</li> <li>The algorithm didn’t predict failure. It <strong>engineered</strong> it. Its output became its own future input.</li> </ol> <h3 id="algorithmic-collusion">Algorithmic Collusion</h3> <p>In Germany, several gas station networks introduced algorithmic pricing models to stay competitive. Economists later found that the algorithms had independently learned to collude — simultaneously raising prices together to maximize profit, neutralizing market competition with no human instruction. The feedback loop optimized for revenue extraction while ignoring the intended goal of competitive pricing.</p> <hr/> <h2 id="6-systems-thinking-as-the-fix">6. Systems Thinking as the Fix</h2> <p>The solution is not a better unit test. A unit test verifies that a function returns the correct integer. It says nothing about whether the system, interacting with real humans in the real world, produces a just outcome.</p> <p><strong>Systems thinking</strong> is a holistic mindset: map the entire environment. Evaluate the physical placement of sensors, the human behaviors of people interacting with the system, and how the system’s output physically alters the inputs that feed back into it.</p> <p>An engineer writing predictive logic must ask:</p> <ul> <li>How does the output of my code affect the human on the other end?</li> <li>How does that person’s subsequent behavior feed back into my training data?</li> <li>Where are the self-reinforcing feedback loops?</li> <li>What are the second-order effects I’m not seeing in a sterile staging environment?</li> </ul> <hr/> <h2 id="7-surveillance-capitalism">7. Surveillance Capitalism</h2> <p>Training predictive algorithms requires data. Vast amounts of data. The chapter examines how tech platforms acquire it.</p> <p>A fundamental shift occurred over the past two decades:</p> <table> <thead> <tr> <th>Model</th> <th>Who is the customer?</th> </tr> </thead> <tbody> <tr> <td>Traditional software (spreadsheet, word processor)</td> <td><strong>The user</strong> — you exchange data for utility</td> </tr> <tr> <td>Surveillance advertising (social media, free apps)</td> <td><strong>The advertiser</strong> — your behavioral profile is the product being sold</td> </tr> </tbody> </table> <p>When your behavior is tracked silently as a side effect of using a service — where your mouse hovers, how long you linger on a photo, your scroll velocity — that telemetry is aggregated to sell targeted advertising. You are not the customer. You are the product.</p> <h3 id="daniel-j-bernsteins-substitution-test">Daniel J. Bernstein’s Substitution Test</h3> <p>Replace the word “data” with “surveillance” in standard corporate language:</p> <blockquote> <p><em>“In our surveillance-driven organization, we collect real-time surveillance streams and store them in our surveillance warehouse. Our surveillance scientists derive insights to manipulate the user experience.”</em></p> </blockquote> <p>The substitution is jarring — but the authors argue it is the most accurate description of the business model.</p> <hr/> <h2 id="8-privacy-as-a-decision-right">8. Privacy as a Decision Right</h2> <p>The common defense — “I have nothing to hide, let them track me” — misunderstands what privacy is.</p> <blockquote> <p><strong>Privacy is not about secrecy. It is the fundamental human autonomy to choose what information you reveal, to whom, and in what context.</strong></p> </blockquote> <p>Mass surveillance strips away that decision right. The corporation determines what is extracted and who has access.</p> <p>GDPR mandates “freely given” consent. But if every employer requires a LinkedIn profile and every peer group organizes exclusively on a specific social platform, clicking “I agree to be tracked” is not a free choice. The social and economic friction of opting out is engineered to be prohibitively high. That is <strong>coercion by monopoly</strong>.</p> <h3 id="the-secondary-data-market-problem">The Secondary Data Market Problem</h3> <p>The chapter provides a sobering example: car insurance companies now purchase telemetry data from vehicle manufacturers — acceleration patterns, braking habits, GPS location — to set premiums, often without the driver’s explicit knowledge or consent.</p> <p>Sensor accuracy has advanced to the point where security researchers demonstrated that the accelerometer in a standard smartwatch (designed to count steps) can deduce passwords being typed on a physical keyboard with alarming accuracy. The seemingly innocuous data from one sensor can be repurposed for total behavioral capture.</p> <hr/> <h2 id="9-data-as-uranium-not-gold">9. Data as Uranium, Not Gold</h2> <p>The tech industry’s default frame: <strong>data is the new oil</strong> (or gold) — a resource to be mined and refined for value.</p> <p>The chapter proposes a corrective: <strong>data is the new uranium</strong>. Uranium is highly valuable in the right context but is inherently hazardous to store. Hoarding gold is inert. Hoarding a mountain of sensitive human surveillance data is dangerous:</p> <ul> <li>It can be breached by hackers.</li> <li>It can be compromised by foreign intelligence services.</li> <li>If the company goes bankrupt, the database is a <strong>legally transferable corporate asset</strong> sold in bankruptcy court — violating the original consent of every user whose data is in it.</li> </ul> <blockquote> <p><strong>Geopolitical implication</strong>: today’s friendly democratic government may not be in charge of a jurisdiction in 10, 20, or 50 years. Building a turnkey mass surveillance infrastructure to optimize ad click-through rates means you have zero control over who eventually holds the cryptographic keys.</p> </blockquote> <hr/> <h2 id="10-the-industrial-revolution-analogy">10. The Industrial Revolution Analogy</h2> <p>The Industrial Revolution brought extraordinary economic growth and raised living standards — but also unchecked horrors: toxic smog, chemical waste in drinking water, child labor in dangerous conditions. Decades of severe human suffering passed before society instituted environmental protection laws, workplace safety regulations, and child labor prohibitions. Factory owners complained bitterly that these regulations would ruin profit margins and stifle innovation. Society as a whole benefited enormously.</p> <p>Bruce Schneier’s formulation:</p> <blockquote> <p><strong>“Data is the pollution problem of the information age.”</strong></p> </blockquote> <p>Future generations will look back at the unregulated early decades of the information age the way we look back at Victorian factory owners dumping chemical waste into drinking water.</p> <hr/> <h2 id="11-data-minimization-vs-data-maximization">11. Data Minimization vs. Data Maximization</h2> <p>The chapter identifies a fundamental clash between two philosophies:</p> <table> <thead> <tr> <th>Philosophy</th> <th>Principle</th> <th>Driven by</th> </tr> </thead> <tbody> <tr> <td><strong>GDPR / Data minimization</strong></td> <td>Collect only the minimum data strictly necessary for the stated service. Delete it immediately when the transaction is complete.</td> <td>Privacy regulation, human rights</td> </tr> <tr> <td><strong>Big data / Data maximization</strong></td> <td>Collect everything you can. Never delete anything. Future ML models might extract value from it.</td> <td>Revenue optimization, analytical potential</td> </tr> </tbody> </table> <p>These are not minor policy differences. They are contradictory architectural philosophies. An engineer building a new feature is forced to choose, often implicitly, which philosophy their code embodies.</p> <p>The chapter’s argument: <strong>data you choose not to collect cannot be stolen, leaked, or weaponized against your users</strong>. Data minimization is not just a regulatory compliance exercise. It is the most effective security control available.</p> <hr/> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th>Topic</th> <th>Core Insight</th> </tr> </thead> <tbody> <tr> <td>Neutral technology</td> <td>Schemas contain human dignity; ignoring downstream harm is not neutrality</td> </tr> <tr> <td>Engineer’s responsibility</td> <td>You write the logic that executes the decision; “the algorithm decided” is not an absolution</td> </tr> <tr> <td>Predictive analytics</td> <td>Systems tuned to minimize false positives systematically exclude at scale</td> </tr> <tr> <td>Bias laundering</td> <td>ML encodes historical prejudice as mathematical truth, removing accountability</td> </tr> <tr> <td>Feedback loops</td> <td>An algorithm’s decisions can alter reality in ways that feed back to justify the next decision</td> </tr> <tr> <td>Systems thinking</td> <td>Map the entire human-system interaction, not just the function’s return value</td> </tr> <tr> <td>Surveillance capitalism</td> <td>When the product is “free,” the user is the product; behavioral tracking inverts the customer relationship</td> </tr> <tr> <td>Privacy as autonomy</td> <td>Privacy is the right to choose what you reveal; coercion by monopoly negates free consent</td> </tr> <tr> <td>Data as uranium</td> <td>Sensitive data is hazardous to store; breach, government seizure, and bankruptcy all threaten it</td> </tr> <tr> <td>Data minimization</td> <td>Not collecting data is the strongest possible protection against its misuse</td> </tr> </tbody> </table> <p>The mathematical logic of your code may be perfectly clean. If the automated tollbooth on your beautifully engineered bridge systematically locks people out of society, the bridge is broken. The engineer has both the agency and the responsibility to fix it.</p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="ethics"/><category term="privacy"/><category term="data-systems"/><category term="surveillance"/><category term="ml-bias"/><summary type="html"><![CDATA[Imagine a physically perfect bridge — flawless steel, mathematically correct load calculations. But the automated toll booth at the entrance systematically refuses entry to certain people, not because they can’t pay, but because an opaque software system associated them with a demographic that caused a traffic jam five years ago. The code compiles. The servers run at optimal capacity. On paper, the system is a triumph of engineering. The societal outcome is a disaster. Chapter 14 of DDIA forces a transition from the how of building systems to the why.]]></summary></entry><entry><title type="html">Part 10: Consistency and Consensus</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-10-Consistency-and-Consensus/" rel="alternate" type="text/html" title="Part 10: Consistency and Consensus"/><published>2026-06-16T00:00:00+00:00</published><updated>2026-06-16T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%2010%20-%20Consistency%20and%20Consensus</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-10-Consistency-and-Consensus/"><![CDATA[<p>On a single machine, the illusion is perfect: you write a value, you read it back, it’s there. But the moment you replicate that value across servers in different cities, you have to answer a deceptively hard question — which copy is true? Chapter 10 of DDIA is about the mechanisms that restore that illusion, the trade-offs they force, and the mathematical bedrock underneath it all.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 10 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-two-philosophies-eventual-vs-strong-consistency">1. Two Philosophies: Eventual vs. Strong Consistency</h2> <p>Whenever you replicate data across multiple nodes for safety or speed, the copies can get out of sync. How you handle that divergence defines the character of your system.</p> <h3 id="eventual-consistency">Eventual Consistency</h3> <p>Copies may briefly disagree, but will converge — <em>eventually</em>. The upside is speed and availability: writes succeed immediately on the local node without waiting for remote confirmation. The downside is that conflict resolution lives in your application code. Double-bookings, stale reads, and race conditions become your problem to handle with read repairs, anti-entropy processes, and apology emails.</p> <p>This is the default philosophy of multi-leader and leaderless databases like Cassandra and DynamoDB — optimized for throughput, not strict correctness.</p> <h3 id="strong-consistency">Strong Consistency</h3> <p>Before confirming a write to the user, the database silently coordinates with remote nodes to lock the record. Only once every relevant node agrees does the system return “success.” From the application developer’s perspective, the multi-node cluster behaves exactly like a single machine — no conflicts, no stale reads, no double-bookings.</p> <p>The cost: every write is limited by network latency, and a severed network link can take the system offline entirely even when the servers themselves are healthy.</p> <p><strong>Which should you choose?</strong></p> <table> <thead> <tr> <th>Domain</th> <th>Recommendation</th> </tr> </thead> <tbody> <tr> <td>Social media likes, non-critical feeds</td> <td>Eventual — delayed visibility hurts no one</td> </tr> <tr> <td>Financial ledgers, medical records, unique usernames</td> <td>Strong — silent data corruption is unacceptable</td> </tr> </tbody> </table> <hr/> <h2 id="2-linearizability-the-rule-book-behind-the-illusion">2. Linearizability: The Rule Book Behind the Illusion</h2> <p>Linearizability is the strict mathematical definition of strong consistency. It provides a single guarantee:</p> <blockquote> <p><strong>Once a write is observed by any client, no subsequent read by any client may return an older value.</strong></p> </blockquote> <p>It enforces the arrow of time. Once the system reveals a new truth, it can never backslide — regardless of which server you talk to.</p> <h3 id="when-the-illusion-breaks">When the Illusion Breaks</h3> <p>Imagine two friends, Aaliyah and Bryce, in the same room watching a football final. The game ends. Aaliyah refreshes her phone and reads the final score from the primary database. She shouts the result. Two seconds later, Bryce refreshes the same app. But Bryce’s request routes to a read replica that is lagging by a few seconds — it still shows the game in progress.</p> <p>Bryce’s later read returned older data than Aaliyah’s earlier read. That is a linearizability violation. The system allowed the clock to run backwards.</p> <h3 id="the-gray-area-window">The Gray-Area Window</h3> <p>A write takes time to process — say 50 ms. Reads that arrive during that window are in an ambiguous state: they might hit the database before the disk write, returning the old value, or after, returning the new one. Linearizability tolerates this gray area during the write. But the moment any client successfully reads the new value, the write has been <em>observed</em>. From that point on, every subsequent read everywhere must also return the new value. The line has been crossed; there is no going back.</p> <hr/> <h2 id="3-linearizability-vs-serializability">3. Linearizability vs. Serializability</h2> <p>These terms are constantly confused. They are entirely different properties.</p> <table> <thead> <tr> <th>Property</th> <th>Scope</th> <th>What it guarantees</th> </tr> </thead> <tbody> <tr> <td><strong>Serializability</strong></td> <td>Transactions (multiple reads/writes grouped)</td> <td>Running N concurrent transactions produces the same result as some serial order of those transactions</td> </tr> <tr> <td><strong>Linearizability</strong></td> <td>Single objects (one row, one register)</td> <td>Reads always return the most recently written value</td> </tr> </tbody> </table> <p>A database can be serializable but not linearizable — transactions are safely isolated from each other, but they might be operating on data that is several minutes out of date.</p> <p><strong>Right skew</strong> is the classic serializability bug: a hospital system requires at least one doctor on call at all times. Both doctors check simultaneously, see two doctors on call, and both click “go off call.” Without serializability, both transactions pass the check and commit. Zero doctors remain. Serializability prevents this by isolating the transactions. But it says nothing about whether the roster data is fresh.</p> <hr/> <h2 id="4-where-linearizability-is-non-negotiable">4. Where Linearizability Is Non-Negotiable</h2> <h3 id="leader-election-and-distributed-locks">Leader Election and Distributed Locks</h3> <p>If five database nodes all crash and reboot simultaneously, they all try to become the leader. If the locking mechanism is not linearizable, two nodes may both believe they successfully acquired the lock at the same moment — a split-brain. You now have two primaries accepting conflicting writes from users, two divergent datasets, and no safe way to merge them.</p> <p>A linearizable lock acts like a ticket dispenser: even if two nodes press the button at the exact same microsecond, exactly one receives ticket 1 (the lock) and the other immediately receives “out of stock.”</p> <h3 id="cross-channel-race-conditions">Cross-Channel Race Conditions</h3> <p>A user uploads a photo via a web server. The server saves the file to object storage (slow) and simultaneously sends a message through a queue (fast) telling a background worker to resize it. If the storage system isn’t linearizable, the worker may arrive at storage before the file has finished replicating internally, receiving a “file not found” error for a file that was just written.</p> <p>The storage system must guarantee: once the web server is told the file is written, any subsequent read is 100% guaranteed to see it.</p> <h3 id="why-quorums-dont-save-you">Why Quorums Don’t Save You</h3> <p>A common misconception: “if I require a majority quorum for both reads and writes, I get linearizability.” You do not.</p> <p>Imagine a write propagating to five nodes. It reaches nodes A and B but is delayed on C, D, and E due to network congestion. Client 1 does a quorum read, hits A, B, C — sees the new value on A and B, returns it. Linearizability now demands all subsequent reads return the new value. But Client 2 immediately does a quorum read, hits C, D, E — none of which have the new value yet. Client 2 travels back in time.</p> <p>Quorum read/write sets overlap, but they don’t resolve synchronously across all nodes. Quorums do not guarantee linearizability without additional consensus protocols layered on top.</p> <hr/> <h2 id="5-the-cap-theorem--and-its-limits">5. The CAP Theorem — and Its Limits</h2> <p>The CAP theorem forces a choice during a network partition:</p> <ul> <li><strong>CP (Consistency + Partition Tolerance)</strong>: Halt all operations until the partition heals. You stay correct but go offline. Users get an error; no data is corrupted.</li> <li><strong>AP (Availability + Partition Tolerance)</strong>: Continue serving requests on both sides of the partition. You stay online but sacrifice linearizability. Risk of double-bookings, stale reads.</li> </ul> <p>The theorem is a useful mental model for beginners but is considered too simplistic for real engineering:</p> <ol> <li><strong>Partition tolerance isn’t a choice.</strong> You cannot decide that cables will not be cut by falling trees. Partitions happen. So you only ever choose between C and A <em>when a partition occurs</em>.</li> <li><strong>It ignores degraded performance.</strong> Real outages rarely look like a clean binary partition. Usually you get a network that is simply slow, dropping 10% of packets, or a node frozen by GC. Is that a partition or high latency? The system can’t tell.</li> <li><strong>We abandon linearizability for performance, not just during failures.</strong> Even a multi-core CPU drops linearizability: one core writes to its L1 cache and only asynchronously flushes to RAM. Another core reading the same memory address may see the old value. We sacrifice strict recency inside a silicon chip to keep CPU execution fast. Across a 500 km fiber link, the trade-off is even more unavoidable.</li> </ol> <hr/> <h2 id="6-ordering-without-clocks-lamport-timestamps">6. Ordering Without Clocks: Lamport Timestamps</h2> <p>We need to order events across nodes without relying on physical clocks (which drift, lie, and jump backwards on leap seconds). The solution is a <strong>logical clock</strong>.</p> <h3 id="the-algorithm">The Algorithm</h3> <p>Every node keeps a simple integer counter. Two rules:</p> <ol> <li>Before sending any message, increment your counter.</li> <li>On receiving a message, if the message’s counter is higher than yours, fast-forward your counter to match it, then add 1.</li> </ol> <p>This creates a mathematical chain of causality. If node A reads a message from node B and then sends a new message, A’s new counter is guaranteed to be strictly higher than B’s. The Lamport timestamp proves that A <em>knew about</em> B’s event.</p> <h3 id="total-ordering-with-tiebreakers">Total Ordering with Tiebreakers</h3> <p>What if two nodes — having never communicated — both generate counter value 3 at the same microsecond? Lamport timestamps break ties by appending the node ID. Counter 3 from node A becomes <code class="language-plaintext highlighter-rouge">3-A</code>; counter 3 from node B becomes <code class="language-plaintext highlighter-rouge">3-B</code>. The rule is deterministic and consistent across the whole cluster. It doesn’t matter who physically wrote first — only that every node applies the same tiebreaker.</p> <h3 id="the-critical-limitation-out-of-band-causality">The Critical Limitation: Out-of-Band Causality</h3> <p>Lamport clocks only know about messages they have explicitly seen. They are blind to causality that bypasses the message chain.</p> <p><strong>The privacy breach scenario:</strong></p> <ol> <li>A user opens their laptop and sets their account to <strong>private</strong>. The accounts database tags this write with logical timestamp <strong>15</strong>.</li> <li>Immediately after, the user picks up their phone and uploads a personal photo. This request routes to the photos database, which has a slightly lagging counter. It assigns the photo upload logical timestamp <strong>12</strong>.</li> <li>A viewer loads the page. The system reasons: photo was uploaded at time 12; account went private at time 15; therefore at time 12 the account was still public — show the photo.</li> <li>A private photo is leaked to the public internet because of a lagging integer.</li> </ol> <p>The user communicated causality out-of-band (laptop then phone). The logical clocks had no way to track it.</p> <p><strong>Mitigations:</strong></p> <ul> <li><strong>Hybrid Logical Clocks (HLC)</strong>: Combine physical time (NTP-synced) with Lamport counters. Keeps nodes in roughly the same physical time zone while preserving causality for events in the same millisecond.</li> <li><strong>Vector Clocks</strong>: Each node keeps an array of counters, one per node. Expensive — a 100-node cluster carries 100 integers on every message — but allows the system to definitively identify <em>concurrent</em> events (where neither node knew about the other), rather than just arbitrarily ordering them.</li> </ul> <hr/> <h2 id="7-consensus-agreement-under-chaos">7. Consensus: Agreement Under Chaos</h2> <p>Logical clocks improve ordering but cannot prevent race conditions that require the whole system to stop and agree on a single truth. For that, we need <strong>consensus</strong>: getting a group of independent nodes to agree on one value and never change their minds about it.</p> <h3 id="the-flp-impossibility-result">The FLP Impossibility Result</h3> <p>In 1985, Fischer, Lynch, and Paterson proved mathematically that in a completely asynchronous system — no clocks, no timeouts, network delays potentially infinite — it is impossible to guarantee that a consensus algorithm will always terminate if even one node might crash.</p> <p>Why? Without a timeout, you can never distinguish a crashed node from a very slow one. You wait forever. You can never safely declare it dead and move on without risking a split-brain.</p> <p><strong>How Raft and Paxos get around it</strong>: They cheat. In the real world, we use physical clocks and timeouts aggressively. If a node doesn’t respond in 150 ms, we declare it dead, remove it from the quorum, and proceed. The FLP result relies on the strict academic assumption of no timeouts — an assumption that never holds in practice.</p> <h3 id="epochs-and-leader-election-in-raft">Epochs and Leader Election in Raft</h3> <p>Consensus algorithms don’t elect a leader forever — they elect temporary leaders for a numbered <strong>epoch</strong> (Raft calls it a <em>term</em>). This prevents split-brain when an old leader disconnects and reconnects:</p> <ol> <li>Node 1 is the leader for epoch 4, sending heartbeat pings every 50 ms.</li> <li>Node 1’s network cable is unplugged. Heartbeats stop.</li> <li>Node 2’s election timeout fires (say 150 ms). It increments the epoch to <strong>5</strong>, declares itself a candidate, and requests votes.</li> <li>A node grants its vote to the first candidate it hears from in epoch 5, provided that candidate’s data is at least as up-to-date as its own.</li> <li>If Node 2 collects a majority (3 of 5), it becomes leader for epoch 5 and starts sending heartbeats.</li> <li>Node 1’s cable is plugged back in. It tries to issue a command tagged with epoch 4. Every other node replies: “We are on epoch 5. Your authority is rejected.” Node 1 steps down and becomes a follower.</li> </ol> <p><strong>Split election prevention</strong>: If two nodes timeout simultaneously and both run for leader, they split the votes. Raft solves this with randomized election timeouts — Node 2 might wait 150 ms while Node 3 waits 165 ms. The small random offset almost always ensures one node starts its campaign before the other.</p> <hr/> <h2 id="8-the-unified-field-theory-of-distributed-systems">8. The Unified Field Theory of Distributed Systems</h2> <p>One of the most profound insights in this chapter: <strong>single-value consensus, distributed locks, shared logs, and atomic commit are all mathematically equivalent</strong>.</p> <table> <thead> <tr> <th>Operation</th> <th>What it actually requires</th> </tr> </thead> <tbody> <tr> <td><strong>Compare-and-set lock</strong></td> <td>All nodes agree on who successfully changed a variable first — single-value consensus</td> </tr> <tr> <td><strong>Append to a shared log</strong></td> <td>All nodes agree on which entry occupies position N — single-value consensus on the next slot</td> </tr> <tr> <td><strong>Atomic commit (2PC)</strong></td> <td>All nodes agree on a single binary value: commit or abort — single-value consensus</td> </tr> </tbody> </table> <p>If you solve single-value consensus, you can build a distributed lock. With a reliable lock, you can build a distributed log by locking the next empty slot. With a reliable log, you can implement atomic commit by writing “COMMIT” to the log. Solve one, solve them all.</p> <hr/> <h2 id="9-zookeeper-and-etcd-outsourcing-the-hard-problem">9. ZooKeeper and etcd: Outsourcing the Hard Problem</h2> <p>Implementing Raft or Paxos correctly is notoriously difficult. A microscopic bug in timeout handling will silently corrupt your entire database. In practice, developers outsource consensus to specialized coordination services.</p> <h3 id="why-not-run-consensus-across-all-nodes">Why Not Run Consensus Across All Nodes?</h3> <p>In a 10,000-node database cluster, running a consensus vote for every write would be catastrophic. Consensus requires a majority of nodes to exchange packets before every decision. Network traffic scales quadratically. Latency climbs from milliseconds to minutes. The system chokes on its own voting overhead.</p> <h3 id="the-pattern-a-tiny-consensus-cluster">The Pattern: A Tiny Consensus Cluster</h3> <p>Instead:</p> <ol> <li>Deploy a <strong>dedicated cluster of 3–5 nodes</strong> running ZooKeeper or etcd. These nodes do nothing but run the consensus algorithm.</li> <li>All 10,000 database nodes act as <strong>clients</strong> of that small cluster.</li> <li>When the database cluster needs a leader election, a distributed lock, or a configuration value, it asks the 3–5 node ZooKeeper cluster to decide. ZooKeeper runs fast consensus on 5 nodes, hands back the result.</li> </ol> <p>Think of it as a tiny Supreme Court that the rest of the system consults for undeniable, final decisions.</p> <h3 id="what-coordination-services-provide">What Coordination Services Provide</h3> <ul> <li><strong>Distributed locks</strong>: Only one worker node processes a critical job at a time.</li> <li><strong>Leases</strong>: Locks with automatic expiry — if the holder dies, the lock releases without any cleanup code.</li> <li><strong>Fencing tokens</strong>: Sequentially increasing integers handed out with each lease, used to reject zombie writes from nodes with expired leases (see Part 9).</li> <li><strong>Configuration management</strong>: Update a config value once in ZooKeeper; it safely propagates to all 10,000 nodes. No SSH loops.</li> <li><strong>Failure detection via ephemeral nodes</strong>: Every active node maintains a live heartbeat session with ZooKeeper and creates an <em>ephemeral</em> record saying “I am alive.” When the heartbeats stop, ZooKeeper’s timeout fires, automatically deletes the ephemeral record, and pushes an instant notification to every watcher: “Node 7 is dead.” The cluster triggers failover without any polling.</li> </ul> <p>This exact pattern is the backbone of Apache Kafka, Kubernetes, and Apache Spark.</p> <hr/> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th>Concept</th> <th>The problem</th> <th>The solution</th> </tr> </thead> <tbody> <tr> <td><strong>Eventual consistency</strong></td> <td>Copies diverge; conflicts are application’s problem</td> <td>High availability and throughput; accept occasional stale reads</td> </tr> <tr> <td><strong>Strong consistency / Linearizability</strong></td> <td>Strict recency guarantee across all nodes</td> <td>Coordinate before confirming writes; slower, lower availability</td> </tr> <tr> <td><strong>CAP theorem</strong></td> <td>Network partitions force a choice</td> <td>Pick C (halt) or A (serve stale data); CAP is a useful but oversimplified model</td> </tr> <tr> <td><strong>Lamport timestamps</strong></td> <td>Physical clocks drift and lie</td> <td>Logical counters track causality without wall clocks</td> </tr> <tr> <td><strong>Vector clocks</strong></td> <td>Lamport can’t detect concurrent events</td> <td>Per-node counter arrays; expensive but expressive</td> </tr> <tr> <td><strong>FLP impossibility</strong></td> <td>Consensus cannot terminate in a pure async system</td> <td>Use timeouts to declare nodes dead and move on</td> </tr> <tr> <td><strong>Raft / Paxos</strong></td> <td>Fault-tolerant leader election and agreement</td> <td>Epochs, quorums, randomized timeouts</td> </tr> <tr> <td><strong>ZooKeeper / etcd</strong></td> <td>Implementing consensus correctly is too hard</td> <td>Outsource to a dedicated 3–5 node coordination cluster</td> </tr> </tbody> </table> <p>The modern cloud is a stack of imperfect hardware and unreliable networks held together by linearizability guarantees, logical clocks, consensus algorithms, and the principle that a majority agreement — even among confused, slow, and occasionally dead nodes — is enough to construct a reliable truth.</p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="distributed-systems"/><category term="consistency"/><category term="consensus"/><category term="linearizability"/><category term="raft"/><category term="zookeeper"/><summary type="html"><![CDATA[On a single machine, the illusion is perfect: you write a value, you read it back, it’s there. But the moment you replicate that value across servers in different cities, you have to answer a deceptively hard question — which copy is true? Chapter 10 of DDIA is about the mechanisms that restore that illusion, the trade-offs they force, and the mathematical bedrock underneath it all.]]></summary></entry><entry><title type="html">Part 9: The Trouble with Distributed Systems</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-9-The-Trouble-with-Distributed-Systems/" rel="alternate" type="text/html" title="Part 9: The Trouble with Distributed Systems"/><published>2026-06-15T00:00:00+00:00</published><updated>2026-06-15T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%209%20-%20The%20Trouble%20with%20Distributed%20Systems</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-9-The-Trouble-with-Distributed-Systems/"><![CDATA[<p>On a single machine, a crash is clean. The OS falls on its sword, points at the memory address, and stops. We prefer a dead computer to one that silently calculates the wrong payroll. But the moment you spread your system across 50 machines talking over a network, that comforting determinism evaporates. Chapter 9 of DDIA is about why — and how to build reliable systems out of fundamentally unreliable parts.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 9 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-partial-failure-the-defining-property">1. Partial Failure: The Defining Property</h2> <p>On a single computer, failure is binary: working or crashed. In a distributed cluster of 100 nodes, <strong>partial failure</strong> is the norm. Parts of the system break in unpredictable, non-deterministic ways while other parts keep running:</p> <ul> <li>A network link between two racks degrades and starts dropping 10% of packets.</li> <li>Node 42’s hard drive begins failing, making reads 5,000× slower — but the node hasn’t crashed.</li> <li>A write command sometimes succeeds, sometimes fails, and sometimes disappears with no response at all.</li> </ul> <p>The binary working/crashed model is gone. Dealing with that uncertainty requires a completely different architectural approach.</p> <h3 id="why-we-embrace-failure-instead-of-preventing-it">Why We Embrace Failure Instead of Preventing It</h3> <p>The naive response is to build an infallible system. The industry learned to do the opposite: <strong>assume that components will fail</strong> and design the system to handle partial failures gracefully.</p> <p>The payoff is a massive operational superpower: if the system can survive individual nodes going offline unexpectedly, you can take them offline <strong>intentionally</strong>. Rolling upgrades become trivial — take down node 1, patch it, bring it back. The load balancer shifts traffic to nodes 2 and 3. Users see nothing. A huge infrastructure upgrade happens invisibly beneath their feet.</p> <hr/> <h2 id="2-the-network-lies">2. The Network Lies</h2> <p>In a shared-nothing architecture, machines communicate exclusively by passing messages over the network. And the network is worse than unreliable — it <strong>actively masks truth</strong>.</p> <h3 id="three-failure-scenarios-you-cannot-distinguish">Three Failure Scenarios You Cannot Distinguish</h3> <p>When a client sends a request and gets no response, there are (at least) three possible explanations:</p> <ol> <li><strong>Request lost in transit</strong> — a fiber cable was cut by a backhoe.</li> <li><strong>Server crashed or unresponsive</strong> — the node never saw the message.</li> <li><strong>Server processed it successfully, but the response was lost</strong> — the database wrote your credit card charge and dropped the receipt.</li> </ol> <p>From the client’s perspective, all three look identical: silence. The client has no way to know which scenario happened. Did the charge go through or not?</p> <h3 id="timeouts-the-only-defense">Timeouts: The Only Defense</h3> <p>The only mechanism we have to detect faults in an asynchronous network is the <strong>timeout</strong>: after waiting N seconds with no acknowledgment, declare failure and move on.</p> <p>But choosing the right timeout value is not science — it’s a statistical guess based on your network’s historical latency percentiles:</p> <ul> <li><strong>Too low</strong>: false positives. You declare a node dead when it’s just slow. Retries add traffic to an already-congested network.</li> <li><strong>Too high</strong>: agonizingly slow recovery. Users stare at a spinner for a full minute before seeing an error.</li> </ul> <hr/> <h2 id="3-tcp-udp-and-the-cost-of-reliability">3. TCP, UDP, and the Cost of Reliability</h2> <h3 id="tcp-and-backpressure">TCP and Backpressure</h3> <p>If a powerful sender transmits data faster than the receiver can process it, the receiver’s memory buffers overflow and it silently drops packets. TCP solves this with a <strong>sliding window</strong> mechanism:</p> <ul> <li>The receiver advertises how much buffer space it has available.</li> <li>As the sender transmits, that window shrinks.</li> <li>When the window hits zero, the sender is forbidden from sending more until the receiver acknowledges and reopens the window.</li> </ul> <p>This prevents receiver crashes from overwhelming traffic — but it comes at a cost. If a packet is lost, TCP withholds all subsequent packets in a buffer, silently retransmits the missing one, reassembles everything in order, and then hands the complete sequence to the application. To your software, this looks like the network froze for half a second.</p> <h3 id="udp-fire-and-forget">UDP: Fire and Forget</h3> <p>UDP throws reliability out entirely. No connections, no backpressure, no retransmission. It fires datagrams into the void as fast as the network interface allows.</p> <p>For latency-sensitive applications — VoIP, video conferencing — this is the right call. If a packet carrying a syllable of your voice gets lost, you don’t want TCP to pause the entire audio stream for 300ms to fetch the missing syllable. By the time it arrives, the conversation has moved on. <strong>Delayed audio data is worthless data.</strong> Drop the packet, glitch for a fraction of a second, and keep the real-time stream moving.</p> <h3 id="queuing-delay-and-the-funnel">Queuing Delay and the Funnel</h3> <p>Inside a data center switch, multiple input ports try to route packets to the same output port simultaneously. Since two packets cannot occupy the same wire at the same time, the switch queues them in a memory buffer. If input traffic floods in at 50 Gbps but the output port drains at 10 Gbps, that buffer fills fast. When it’s full, the switch <strong>drops packets</strong>.</p> <p>This queuing delay is the primary source of unpredictable latency in modern networks — not broken cables.</p> <h3 id="why-we-chose-this-architecture">Why We Chose This Architecture</h3> <p>The alternative — dedicated private circuits between every pair of nodes — eliminates queuing but wastes 99% of capacity. The old analog telephone network worked this way (circuit switching): a physical copper path was clamped together for the duration of every call. Unused bandwidth during silences was wasted.</p> <p>Modern data centers use <strong>packet switching</strong>: shared wires, chopped-up data, best-effort delivery. Variable delays are not a law of physics. They are the deliberate economic choice to maximize utilization over predictability. We traded latency guarantees for cheap, high-throughput bandwidth.</p> <hr/> <h2 id="4-clocks-are-lying-too">4. Clocks Are Lying Too</h2> <p>Timeouts require knowing how much time has elapsed. But a computer’s measurement of time is deeply flawed.</p> <h3 id="quartz-drift">Quartz Drift</h3> <p>Inside every server is a quartz crystal oscillator. When current passes through it, it vibrates at a specific frequency and a counter translates those vibrations into seconds. But quartz drifts — and the rate of drift changes with <strong>temperature</strong>. If a server rack goes from 20°C to 26°C because the air conditioning fails, the quartz crystal physically expands, its vibration frequency changes, and the clock runs faster or slower.</p> <h3 id="ntps-fundamental-flaw">NTP’s Fundamental Flaw</h3> <p>NTP daemons compensate by querying atomic-clock-backed time servers over the network. But NTP assumes the network journey to the server takes exactly half the time of the round trip. If routing is asymmetric — which it almost always is — NTP calculates the correction incorrectly. Even with NTP running perfectly, a server’s clock can be off by tens or hundreds of milliseconds.</p> <h3 id="the-leap-second-nightmare">The Leap Second Nightmare</h3> <p>Earth’s rotation is gradually slowing. Occasionally the international body that governs time inserts a <strong>leap second</strong>: a specific minute contains 61 seconds. Most software was written with the hard assumption that a minute always has exactly 60 seconds. When an NTP server tells your Linux machine “it is still 23:59:59” for two consecutive seconds, the kernel’s high-resolution timers can enter an infinite loop. In 2012, a leap second caused a Linux kernel bug that spiked CPU to 100% and took Reddit, Mozilla, and several airlines completely offline.</p> <h3 id="monotonic-clocks-stopwatches-not-calendars">Monotonic Clocks: Stopwatches, Not Calendars</h3> <p>Operating systems provide two types of clocks:</p> <table> <thead> <tr> <th>Clock type</th> <th>Jumps backward?</th> <th>NTP-adjusted?</th> <th>Use for</th> </tr> </thead> <tbody> <tr> <td><strong>Time-of-day</strong></td> <td>Yes (NTP, leap seconds, DST)</td> <td>Yes</td> <td>Calendar timestamps</td> </tr> <tr> <td><strong>Monotonic</strong></td> <td>Never</td> <td>No</td> <td>Measuring elapsed durations</td> </tr> </tbody> </table> <p>For timeouts, always use the <strong>monotonic clock</strong>. Record the counter before sending a request, then compare when checking for expiry. The absolute value is meaningless (it might be milliseconds since last reboot), but the <em>difference</em> is reliable.</p> <p>The fatal limitation: you cannot compare monotonic clocks <strong>across machines</strong>. Node A’s counter of 500 and Node B’s counter of 5,000 tell you nothing about which event happened first. For ordering distributed events, you must use time-of-day clocks — and accept their unreliability.</p> <h3 id="last-write-wins-and-silent-data-loss">Last Write Wins and Silent Data Loss</h3> <p>Many databases use <strong>Last Write Wins (LWW)</strong>: when two nodes have conflicting data, keep the write with the newest timestamp. The problem:</p> <ol> <li>Client A writes <code class="language-plaintext highlighter-rouge">x = 1</code> to node 1. Node 1 tags it with timestamp <code class="language-plaintext highlighter-rouge">100</code>.</li> <li>Client A’s write replicates to node 2.</li> <li>Client B reads <code class="language-plaintext highlighter-rouge">x = 1</code> from node 2, then writes <code class="language-plaintext highlighter-rouge">x = 2</code>. Node 2’s clock is drifting slightly behind — it tags the write with timestamp <code class="language-plaintext highlighter-rouge">99</code>.</li> <li>A third node receives both writes, runs LWW, and keeps <code class="language-plaintext highlighter-rouge">x = 1</code> (timestamp 100). Client B’s increment is silently discarded.</li> </ol> <p>Client B read the state, made a logically valid update, and the database threw it away because of a few milliseconds of hardware clock drift. In a financial system, money just vanished.</p> <h3 id="google-spanners-truetime">Google Spanner’s TrueTime</h3> <p>Google’s solution: stop pretending clocks are synchronized. Install GPS receivers and atomic clocks directly in data center racks to minimize error — then <strong>quantify the remaining uncertainty</strong>.</p> <p>Instead of returning a timestamp, the <strong>TrueTime API</strong> returns a confidence interval: “I cannot tell you exactly what time it is, but the true time is somewhere between 2:14:59 and 2:15:01.” When a transaction wants to commit, Spanner reads the interval’s upper bound and <strong>deliberately waits</strong> for that entire window to pass before committing. This guarantees that any subsequent transaction will have a strictly later interval with no overlap — causal ordering without relying on quartz crystals.</p> <hr/> <h2 id="5-process-pauses-and-the-zombie-problem">5. Process Pauses and the Zombie Problem</h2> <p>Even CPU execution is not the continuous flow it appears to be. An entire application — every thread, every calculation — can be <strong>frozen for seconds or minutes</strong> without knowing it happened.</p> <h3 id="sources-of-process-pauses">Sources of Process Pauses</h3> <ul> <li><strong>Stop-the-world garbage collection</strong>: JVM, CLR, and Go runtimes periodically halt every application thread to reclaim memory. Large Java heaps can pause for tens of seconds.</li> <li><strong>Synchronous disk I/O</strong>: If a file isn’t in the RAM cache, the calling thread blocks until the disk seeks.</li> <li><strong>Memory paging</strong>: When the OS swaps memory to disk, a thread accessing a paged-out variable is frozen until the OS reads it back.</li> <li><strong>VM live migration</strong>: Cloud providers can pause your entire virtual machine, copy its RAM over the data center network to a different physical rack, then resume execution. Multi-second blackout; application never knew.</li> </ul> <h3 id="the-zombie-scenario">The Zombie Scenario</h3> <p>Here’s why pauses are dangerous — they don’t just slow the system down, they create <strong>zombie leaders</strong>:</p> <ol> <li>Node A requests a lease (a distributed lock with a timeout): “I want exclusive write rights for 10 seconds.”</li> <li>Node A gets the lease. One second in, a massive GC pause freezes it.</li> <li>Real wall-clock time ticks on. The 10 seconds elapse. The cluster notices Node A’s heartbeats have stopped and elects Node B as the new leader.</li> <li>Node B begins writing to the database.</li> <li>GC finishes. Node A wakes up <strong>mid-execution</strong>, doesn’t bother to check the clock, and confidently sends a write — because its internal state still says “I hold the lease.”</li> </ol> <p>Node A is a zombie: walking around with authority that mathematically expired, overwriting Node B’s valid data.</p> <hr/> <h2 id="6-fencing-tokens-killing-zombies">6. Fencing Tokens: Killing Zombies</h2> <p>The fix is to make the <strong>storage layer</strong> actively reject stale authority — not rely on the zombie to check its own lease.</p> <p>Every time a lease is granted, the lock service hands out a <strong>sequentially increasing fencing token</strong>:</p> <ul> <li>Client A gets lease + token <strong>33</strong>.</li> <li>Client A pauses. Lease expires. Client B gets lease + token <strong>34</strong>.</li> <li>Client B writes to storage with token 34. Storage records: “highest token seen = 34.”</li> <li>Zombie Client A wakes up and tries to write with token 33.</li> <li>Storage compares: 33 &lt; 34. <strong>Write rejected.</strong> Zombie fenced off.</li> </ul> <p>No clock comparisons. No timeouts. Just a monotonically increasing integer that the storage layer checks on every write.</p> <h3 id="fencing-tokens-in-leaderless-databases">Fencing Tokens in Leaderless Databases</h3> <p>When there’s no single central storage gatekeeper, fencing tokens can be embedded directly into write timestamps. Token 34 generates timestamps starting with <code class="language-plaintext highlighter-rouge">34...</code>; token 33 generates timestamps starting with <code class="language-plaintext highlighter-rouge">33...</code>. Any string starting with <code class="language-plaintext highlighter-rouge">34</code> is lexicographically larger than one starting with <code class="language-plaintext highlighter-rouge">33</code>, so LWW always picks the legitimate leader — regardless of quartz drift.</p> <hr/> <h2 id="7-byzantine-faults">7. Byzantine Faults</h2> <p>Everything above assumes nodes are <strong>honest</strong>: slow, crashed, or confused — but not malicious. A zombie node that overwrites data does so because it doesn’t know its lease expired, not because it’s trying to subvert the system.</p> <p>A <strong>Byzantine fault</strong> is when a node actively lies: a compromised node crafts a fake fencing token of <code class="language-plaintext highlighter-rouge">999,999</code> to bypass the storage guard, or sends contradictory messages to different peers to prevent consensus.</p> <h3 id="when-byzantine-tolerance-matters">When Byzantine Tolerance Matters</h3> <table> <thead> <tr> <th>Environment</th> <th>Assume Byzantine?</th> </tr> </thead> <tbody> <tr> <td>Private enterprise data center</td> <td>No — you own the hardware; garbage is almost always a bug, not an attack</td> </tr> <tr> <td>Public blockchain (Bitcoin, Ethereum)</td> <td>Yes — anonymous, mutually untrusting participants; assume active fraud</td> </tr> </tbody> </table> <p>Building Byzantine fault tolerance into standard databases would mean cryptographic verification of every internal packet — prohibitive overhead for no practical benefit in a controlled data center. For blockchains, you have no choice: proof-of-work and PBFT exist precisely because you cannot trust the nodes.</p> <hr/> <h2 id="8-system-models">8. System Models</h2> <p>To reason mathematically about distributed algorithms, we need to formalize what kinds of failures we expect.</p> <h3 id="timing-models">Timing Models</h3> <table> <thead> <tr> <th>Model</th> <th>Assumption</th> <th>Reality check</th> </tr> </thead> <tbody> <tr> <td><strong>Synchronous</strong></td> <td>Network delays, process pauses, and clock drift are all bounded by a known maximum</td> <td>Fairy tale for cloud computing; only realistic in hard real-time avionics</td> </tr> <tr> <td><strong>Asynchronous</strong></td> <td>No timing assumptions at all; no clocks, no timeouts</td> <td>Mathematically impossible to distinguish a crashed node from a slow one; almost no useful algorithms survive</td> </tr> <tr> <td><strong>Partially synchronous</strong></td> <td>System behaves synchronously <em>most</em> of the time, but occasionally violates all bounds</td> <td>This is reality: millisecond-level delays normally, multi-second GC pauses and network spikes occasionally</td> </tr> </tbody> </table> <p>Practitioners design for <strong>partial synchrony</strong>.</p> <h3 id="safety-vs-liveness">Safety vs. Liveness</h3> <p>Every distributed algorithm must satisfy two kinds of properties:</p> <ul> <li> <p><strong>Safety</strong> — nothing bad ever happens. Example: two nodes never hold the same lease simultaneously. If violated, the damage is permanent — you can’t un-corrupt a database write. Safety must hold in all possible situations, even during network partitions. If the algorithm cannot guarantee a safe result, it must <strong>halt and refuse</strong> rather than produce a wrong answer.</p> </li> <li> <p><strong>Liveness</strong> — something good eventually happens. Example: if a client sends a read request, the system eventually responds. Liveness is allowed to be conditional: “the system will respond <em>provided</em> a majority of nodes are alive and the network eventually recovers.” During a partition, liveness is temporarily suspended; safety is not.</p> </li> </ul> <p>The framework gives you a clean question for every feature: is this a safety requirement or a liveness requirement? The answer determines how strictly you must enforce it.</p> <hr/> <h2 id="9-deterministic-simulation-testing">9. Deterministic Simulation Testing</h2> <p>You can prove your algorithm correct on a whiteboard, but eventually you write real code. And the bugs live in the implementation — in the exact microsecond timing of GC pauses, network drops, and thread scheduling across 10 machines.</p> <p>Reproducing a concurrency bug in a live distributed environment is virtually impossible. <strong>Deterministic Simulation Testing (DST)</strong>, pioneered by FoundationDB, solves this by bringing the comforting single-node illusion back — for the purpose of testing.</p> <h3 id="how-dst-works">How DST Works</h3> <p>Replace every component that touches the real physical world:</p> <ul> <li>Real TCP stack → <strong>mock simulated network</strong></li> <li>Real hardware clock → <strong>simulated software clock</strong></li> <li>Thread schedulers, disk I/O → <strong>simulated counterparts</strong></li> <li>Random number generators → <strong>single seeded PRNG</strong></li> </ul> <p>Run all 10 database instances inside a single-threaded process loop on a laptop. The simulator has absolute control: it dictates exactly when mock packets arrive, exactly when simulated processes pause, exactly what the clock reads. Because everything runs in software with no real I/O, you can fast-forward through millions of randomized failure scenarios — dropping every fifth packet, injecting a 10-second GC pause on node 3, jumping node 2’s clock forward by an hour.</p> <h3 id="the-magic-of-the-seed">The Magic of the Seed</h3> <p>If the simulator finds a bug at simulated hour 45 — say, a bizarre sequence of 100 packet drops triggers a split-brain data corruption — it stops and records the <strong>random seed</strong> that produced that exact sequence. You hand the developer the seed. They run the simulation on their laptop with that seed, watch the bug happen deterministically, attach a debugger, step through the code, and fix it.</p> <p>DST turns the unpredictable, chaotic nightmare of distributed failures into a repeatable, debuggable, single-node puzzle.</p> <hr/> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th>Failure domain</th> <th>The problem</th> <th>The defense</th> </tr> </thead> <tbody> <tr> <td>Network</td> <td>Packets drop silently; client can’t distinguish lost request from lost response</td> <td>Timeouts (statistical guess); retry with idempotency</td> </tr> <tr> <td>Flow control</td> <td>Fast sender overwhelms slow receiver</td> <td>TCP sliding window (backpressure); drop strategy for UDP</td> </tr> <tr> <td>Clock drift</td> <td>Quartz drifts with temperature; NTP assumes symmetric routing; leap seconds</td> <td>Monotonic clocks for durations; TrueTime intervals for global ordering</td> </tr> <tr> <td>Process pauses</td> <td>GC, disk I/O, VM migration freeze application without its knowledge</td> <td>Fencing tokens; make the storage layer reject stale writes</td> </tr> <tr> <td>Byzantine nodes</td> <td>Actively malicious nodes fabricate authority</td> <td>PBFT / proof-of-work (only for untrusted environments)</td> </tr> <tr> <td>Algorithm correctness</td> <td>Partial synchrony makes proofs hard</td> <td>Safety + liveness decomposition; DST for implementation testing</td> </tr> </tbody> </table> <p>The next time you see a spinning wheel in an app, ask yourself: is the network congested, did a database node pause for GC, or is the backend fending off a zombie holding an expired fencing token? The reality behind that wheel is a ballet of distributed chaos — hopefully protected by a solid quorum and rigorous simulation testing.</p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="distributed-systems"/><category term="networks"/><category term="clocks"/><category term="fault-tolerance"/><category term="consensus"/><summary type="html"><![CDATA[On a single machine, a crash is clean. The OS falls on its sword, points at the memory address, and stops. We prefer a dead computer to one that silently calculates the wrong payroll. But the moment you spread your system across 50 machines talking over a network, that comforting determinism evaporates. Chapter 9 of DDIA is about why — and how to build reliable systems out of fundamentally unreliable parts.]]></summary></entry><entry><title type="html">Part 6: Replication</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-6-Replication/" rel="alternate" type="text/html" title="Part 6: Replication"/><published>2026-06-06T00:00:00+00:00</published><updated>2026-06-06T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%206%20-%20Replication</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-6-Replication/"><![CDATA[<p>Saving a copy of data to a single machine means one hard drive failure can wipe everything out. <em>Replication</em> — keeping the same data on multiple machines connected over a network — is the foundational technique that gives modern databases fault tolerance, geographic reach, and read scalability. Chapter 6 of DDIA reveals just how hard that simple-sounding idea actually is.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 6 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-why-replicate">1. Why Replicate?</h2> <p>There are three distinct reasons engineering teams invest in replication:</p> <table> <thead> <tr> <th>Goal</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td><strong>High availability</strong></td> <td>If one node dies, the others keep serving traffic</td> </tr> <tr> <td><strong>Reduced latency</strong></td> <td>Place data physically close to users in different regions</td> </tr> <tr> <td><strong>Read scalability</strong></td> <td>Spread millions of concurrent reads across many replicas</td> </tr> </tbody> </table> <p>The chapter organises the strategies for achieving these goals into three architectures: <strong>single-leader</strong>, <strong>multi-leader</strong>, and <strong>leaderless</strong> replication.</p> <hr/> <h2 id="2-single-leader-replication">2. Single-Leader Replication</h2> <h3 id="the-hierarchy">The Hierarchy</h3> <p>One node is elected as the <strong>leader</strong> (also called master or primary). Every write request must be routed to it. The leader writes the change to its local storage, then streams a log of that change to <strong>follower</strong> nodes (replicas). Followers are read-only — they copy the leader’s log and serve read traffic.</p> <p>This architecture is the default for PostgreSQL, MySQL, and Kafka.</p> <h3 id="zero-disk-architectures">Zero-Disk Architectures</h3> <p>The industry is shifting toward removing local hard drives entirely from database nodes. In a <strong>zero-disk architecture (ZDA)</strong>, all permanent data lives in object storage like Amazon S3. Nodes use RAM and fast local NVMe drives purely as ephemeral write buffers, asynchronously flushing batches to S3 in the background. This decouples compute from storage: a crashed node can be replaced by spinning up a fresh one and pointing it at the same S3 bucket.</p> <h3 id="synchronous-vs-asynchronous-replication">Synchronous vs Asynchronous Replication</h3> <p>This is one of the most consequential choices in the entire architecture.</p> <p><strong>Synchronous:</strong> The leader waits for the follower to confirm every write before acknowledging success to the client. If the follower is slow (garbage collection pause, dropped packet), the entire system halts. In practice, no production system runs fully synchronous because a single slow replica can bring writes to a standstill.</p> <p><strong>Asynchronous:</strong> The leader writes to its local write-ahead log (WAL), immediately acknowledges the client, and streams updates to followers in the background. If the leader dies before the background process syncs, any acknowledged-but-unsynced writes are permanently lost.</p> <p><strong>Semi-synchronous (the real-world compromise):</strong> Exactly one follower confirms synchronously, guaranteeing at least two durable copies. The rest catch up asynchronously.</p> <hr/> <h2 id="3-failover-and-its-nightmares">3. Failover and Its Nightmares</h2> <p>When the leader fails, the system must:</p> <ol> <li><strong>Detect the failure</strong> — nodes exchange heartbeat messages; if no response arrives within a timeout (typically 30 s), the node is declared dead.</li> <li><strong>Elect a new leader</strong> — remaining nodes run a consensus algorithm (Raft, Paxos) and promote the follower with the most up-to-date log.</li> <li><strong>Reconfigure routing</strong> — all clients must be redirected to the new leader.</li> </ol> <h3 id="split-brain">Split-Brain</h3> <p>The most dangerous failure mode: the old leader was not dead, just temporarily disconnected. When it reconnects, it still believes it is the legitimate leader. Two nodes both accepting writes produces <strong>split-brain</strong> — two divergent truths that cannot be automatically merged in a single-leader system.</p> <p>The industry’s blunt solution is <strong>STONITH</strong> (<em>Shoot The Other Node In The Head</em>): a node detecting split-brain signals a power distribution unit to cut electricity to the old leader, physically preventing it from accepting further writes.</p> <hr/> <h2 id="4-eventual-consistency-and-its-anomalies">4. Eventual Consistency and Its Anomalies</h2> <p>Because followers replicate asynchronously, they may lag behind the leader by milliseconds or even minutes. During this window the system is <strong>eventually consistent</strong> — a weak guarantee that all replicas will converge <em>if</em> writes stop, but offering no promise about intermediate states.</p> <p>This lag creates three classical user-visible anomalies:</p> <h3 id="41-read-after-write-read-your-writes">4.1 Read-After-Write (Read-Your-Writes)</h3> <p>A user writes data to the leader, then immediately reads it from a lagging follower. Their own update appears to have vanished.</p> <p><strong>Fix:</strong> Route a user’s own reads back to the leader for a short window after a write. Other users’ reads can safely go to followers because the reader has no expectation about the exact freshness of data they didn’t write.</p> <h3 id="42-monotonic-reads">4.2 Monotonic Reads</h3> <p>A load balancer routes successive reads from the same user to different replicas. If the second replica is lagging more than the first, the user observes a comment appear, then disappear on the next page refresh — time appears to move backwards.</p> <p><strong>Fix:</strong> Hash the user ID to permanently pin each user to a specific replica for reads.</p> <h3 id="43-consistent-prefix-reads">4.3 Consistent Prefix Reads</h3> <p>In a partitioned database, if a question and its answer are stored on different shards with uneven replication lag, an observer can see the answer before the question — causality is shattered.</p> <p><strong>Fix:</strong> Ensure causally related writes go to the same partition so the partition’s internal log preserves their order.</p> <hr/> <h2 id="5-multi-leader-replication">5. Multi-Leader Replication</h2> <p>When users are distributed globally, forcing every write across an ocean to a single leader in one data center adds hundreds of milliseconds of latency. The solution: place a <strong>leader in each major region</strong>. A Tokyo write hits the Tokyo leader instantly; that leader then asynchronously forwards the change to the London leader in the background.</p> <p>Multi-leader is also the model used by <strong>collaborative offline apps</strong> (notes apps, document editors): your phone’s local SQLite database acts as a leader, syncing changes to the cloud when connectivity is restored.</p> <h3 id="leader-topologies">Leader Topologies</h3> <table> <thead> <tr> <th>Topology</th> <th>Structure</th> <th>Risk</th> </tr> </thead> <tbody> <tr> <td><strong>Circular</strong></td> <td>A → B → C → A</td> <td>One failed node breaks the chain</td> </tr> <tr> <td><strong>Star</strong></td> <td>One hub broadcasts to leaf nodes</td> <td>Hub is a bottleneck</td> </tr> <tr> <td><strong>All-to-all</strong></td> <td>Every leader broadcasts to every other</td> <td>Most resilient; ordering problems</td> </tr> </tbody> </table> <h3 id="write-conflicts">Write Conflicts</h3> <p>Single-leader systems avoid conflicts entirely because writes are serialised through one node. Multi-leader systems cannot — two users can concurrently modify the same record on two different continents and both leaders accept the write.</p> <p><strong>Conflict resolution strategies:</strong></p> <table> <thead> <tr> <th>Strategy</th> <th>Mechanism</th> <th>Downside</th> </tr> </thead> <tbody> <tr> <td><strong>Operational Transformation (OT)</strong></td> <td>Tracks the intent of every keystroke and transforms concurrent operations to preserve intent</td> <td>Notoriously hard to implement correctly</td> </tr> <tr> <td><strong>CRDTs</strong></td> <td>Assigns a unique permanent ID to every element; operations commute in any order</td> <td>Deleted items leave invisible “tombstone” ghosts in memory, causing storage bloat</td> </tr> <tr> <td><strong>Last Write Wins (LWW)</strong></td> <td>The write with the highest timestamp survives</td> <td>Clock skew between servers means the “winner” may actually be the older write; silently discards data</td> </tr> </tbody> </table> <hr/> <h2 id="6-leaderless-replication">6. Leaderless Replication</h2> <p>Leaderless systems (pioneered by Amazon Dynamo, used by Cassandra and Riak) eliminate the hierarchy entirely. Every replica can accept any write.</p> <h3 id="quorum-reads-and-writes">Quorum Reads and Writes</h3> <p>Consistency is maintained through the <strong>quorum condition</strong>: with <em>N</em> total replicas, if you write to <em>W</em> nodes and read from <em>R</em> nodes, and <em>W + R &gt; N</em>, at least one node in every read set must overlap with every write set.</p> <p>A typical configuration for <em>N</em> = 5: <em>W</em> = 3, <em>R</em> = 3.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>W + R = 3 + 3 = 6 &gt; 5 = N  ✓
</code></pre></div></div> <p>Even if two nodes are offline, any read of three nodes is mathematically guaranteed to include at least one node that witnessed the write.</p> <h3 id="read-repair">Read Repair</h3> <p>When a client reads from multiple replicas and sees different versions (identified by monotonically increasing version numbers), it writes the newest version back to the stale nodes. The act of reading heals the database.</p> <h3 id="version-vectors">Version Vectors</h3> <p>A single version number only tracks absolute ordering from one node’s perspective. A <strong>version vector</strong> is an array tracking each replica’s version number independently — e.g. <code class="language-plaintext highlighter-rouge">{nodeA: 2, nodeB: 1, nodeC: 0}</code>. This creates a mathematical causal family tree of the data, allowing the system to distinguish:</p> <ul> <li><strong>Causal writes</strong> (flour was added after milk → flour is a descendant)</li> <li><strong>Concurrent writes</strong> (eggs and bacon added simultaneously → neither is an ancestor of the other)</li> </ul> <h3 id="concurrency-without-clocks">Concurrency Without Clocks</h3> <p>The most mind-bending insight in this section: two operations are <strong>concurrent</strong> if neither knows about the other — regardless of when they happened on a wall clock. If a network cable under the ocean is severed, a write made in London on Tuesday and a write made in Tokyo on Thursday are concurrent to the database, because neither server knew about the other’s action when it was created. When the cable is repaired, those writes meet as peers with no causal relationship.</p> <blockquote> <p>Concurrency is formally defined by the <em>absence of a happens-before relationship</em>, not by physical time.</p> </blockquote> <hr/> <h2 id="7-architecture-comparison">7. Architecture Comparison</h2> <table> <thead> <tr> <th>Approach</th> <th>Strengths</th> <th>Weaknesses</th> </tr> </thead> <tbody> <tr> <td><strong>Single-leader</strong></td> <td>Simple ordering, no write conflicts</td> <td>Single point of failure, replication lag anomalies, complex failover</td> </tr> <tr> <td><strong>Multi-leader</strong></td> <td>Low geographic latency, offline support</td> <td>Mandatory conflict resolution, OT/CRDT complexity</td> </tr> <tr> <td><strong>Leaderless</strong></td> <td>Extreme resilience, no failover needed</td> <td>No linear ordering, quorum math required, version vectors complex</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="distributed-systems"/><category term="replication"/><category term="consistency"/><category term="databases"/><summary type="html"><![CDATA[Saving a copy of data to a single machine means one hard drive failure can wipe everything out. Replication — keeping the same data on multiple machines connected over a network — is the foundational technique that gives modern databases fault tolerance, geographic reach, and read scalability. Chapter 6 of DDIA reveals just how hard that simple-sounding idea actually is.]]></summary></entry><entry><title type="html">Part 7: Sharding</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-7-Sharding/" rel="alternate" type="text/html" title="Part 7: Sharding"/><published>2026-06-06T00:00:00+00:00</published><updated>2026-06-06T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%207%20-%20Sharding</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-7-Sharding/"><![CDATA[<p>Chapter 6 showed how to keep <em>copies</em> of data on multiple machines. Chapter 7 asks a different question: what do you do when your dataset is so large that no single machine can hold all of it? The answer is <strong>sharding</strong> — splitting the data into independent chunks and distributing those chunks across a fleet of nodes.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 7 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-vertical-scaling-and-its-hard-limits">1. Vertical Scaling and Its Hard Limits</h2> <p>The instinctive first response to a slow database is to buy a bigger server — more RAM, more CPU cores, faster SSDs. This is <strong>vertical scaling</strong>, and it works well up to a point.</p> <p>The wall it hits is the <strong>NUMA (Non-Uniform Memory Access)</strong> architecture of modern multi-socket servers. When a CPU needs data stored in the RAM bank of a different CPU socket, it must cross the motherboard’s interconnect bus. As you add more sockets, cross-socket communication becomes a bottleneck. The machine spends increasing fractions of its time shuffling data internally rather than executing your queries.</p> <p>On top of that, hardware pricing is super-linear: a server with 128 cores can cost five times more than a 64-core machine, not twice as much.</p> <p><strong>Vertical scaling has a hard physical and economic ceiling.</strong></p> <hr/> <h2 id="2-horizontal-scaling-the-shared-nothing-architecture">2. Horizontal Scaling: The Shared-Nothing Architecture</h2> <p>Instead of one enormous server, use a fleet of commodity machines. Each node handles a roughly equal fraction of the total data and query load. This is <strong>horizontal scaling</strong> (also called a <em>shared-nothing</em> architecture).</p> <p>A critical point: <strong>sharding and replication are not mutually exclusive.</strong> In production, shards are always replicated. A node might be the primary leader for Shard A while simultaneously acting as a follower replica for Shards B and C. If the primary for Shard A dies, the cluster promotes its replica with no data loss.</p> <hr/> <h2 id="3-multi-tenancy-and-isolation">3. Multi-Tenancy and Isolation</h2> <p>Beyond raw throughput, sharding solves a second problem: <strong>resource isolation between tenants</strong> in SaaS products.</p> <p>In a centralised database serving thousands of corporate clients, one client running a heavy analytical query can consume all available CPU and RAM — starving every other client. Assigning each tenant to their own shard creates hard resource boundaries.</p> <p>Isolation benefits extend further:</p> <ul> <li><strong>Permission isolation:</strong> A bug that drops a <code class="language-plaintext highlighter-rouge">WHERE</code> clause in application code cannot leak Tenant A’s data to Tenant B when they are on physically separate machines.</li> <li><strong>Restore isolation:</strong> Rolling back a specific tenant’s shard to last hour’s backup does not affect any other tenant on the platform.</li> <li><strong>Data privacy compliance (GDPR):</strong> Deleting a European user’s data is a localised, verifiable operation on their shard rather than a complex archaeological dig through shared B-trees.</li> </ul> <hr/> <h2 id="4-the-enemy-skew">4. The Enemy: Skew</h2> <p>The entire goal of a sharding strategy is an even distribution of data and query load. When the distribution is uneven, some nodes become <strong>hotspots</strong> while others sit idle. This is called <strong>skew</strong> — and it defeats the entire purpose of building a cluster.</p> <hr/> <h2 id="5-key-range-sharding">5. Key-Range Sharding</h2> <p>Divide the total spectrum of primary keys into contiguous sorted ranges and assign each range to a node. Think of a printed encyclopedia: Volume 1 holds A–B, Volume 2 holds C–D, and so on.</p> <p><strong>Advantages:</strong></p> <ul> <li>Data is stored in sorted order on disk (typically in SSTables or B-trees).</li> <li><strong>Range scans</strong> are extremely fast: a query for “all temperature readings in July” locates the first entry for July 1 and streams sequentially to August 1 — no random seeks.</li> </ul> <p><strong>The fatal flaw — write hotspots:</strong></p> <p>If your partition key is a monotonically increasing timestamp and sensors write thousands of records per second, 100% of incoming writes land on the shard covering <em>right now</em>. Every other shard sits idle.</p> <p><strong>Fix: compound keys.</strong> Prepend the sensor ID to the timestamp — <code class="language-plaintext highlighter-rouge">sensor_73:2026-07-01</code>. Now writes for the current moment are spread across as many shards as there are sensors. The cost is that a query for “all sensors in July” must scatter across all shards and gather the results, increasing read complexity.</p> <hr/> <h2 id="6-hash-sharding">6. Hash Sharding</h2> <p>Pass the primary key through a hash function (e.g., Murmur3 or MD5) before routing. A good hash function has the <strong>avalanche effect</strong>: even slightly different input keys produce wildly different output values, uniformly scattered across the output space.</p> <p>This <strong>mathematically eliminates skew</strong> from predictable input distributions.</p> <p><strong>The cost:</strong> all notion of ordering is destroyed. User <code class="language-plaintext highlighter-rouge">0001</code> and User <code class="language-plaintext highlighter-rouge">0002</code> will land on entirely different shards. Range queries become impossible.</p> <hr/> <h2 id="7-the-modulo-trap">7. The Modulo Trap</h2> <p>A natural implementation of hash sharding is to compute <code class="language-plaintext highlighter-rouge">hash(key) mod N</code>, where <em>N</em> is the number of nodes. This routes each key to a node in range <code class="language-plaintext highlighter-rouge">[0, N-1]</code>.</p> <p><strong>The problem:</strong> when <em>N</em> changes (a node is added or removed), almost every key maps to a different node. The cluster must physically migrate terabytes of data across the network — a migration storm that saturates bandwidth and can cause a full production outage.</p> <hr/> <h2 id="8-consistent-hashing-with-fixed-logical-shards">8. Consistent Hashing with Fixed Logical Shards</h2> <p>The solution is to <strong>decouple logical shards from physical nodes</strong>.</p> <p>Create a large fixed number of logical shards (e.g., 1,000 virtual buckets). The hash is always computed modulo 1,000 — a denominator that never changes. These 1,000 buckets are then assigned to physical nodes like dealing cards:</p> <ul> <li>10 nodes → ~100 buckets per node</li> <li>Node fails → only its ~100 buckets are redistributed to surviving nodes; the other 900 are untouched.</li> <li>Node added → steal ~100 buckets from existing nodes; no wholesale rehashing.</li> </ul> <p>This limits data movement to the absolute minimum required to maintain balance.</p> <p>A variant used by Apache Cassandra is the <strong>hash ring</strong>: the hash output space is visualised as a circle, and each node “owns” a contiguous arc of that circle. Overloaded nodes split their arc and hand half to a newly spawned node.</p> <hr/> <h2 id="9-celebrity-hotspots-when-math-isnt-enough">9. Celebrity Hotspots: When Math Isn’t Enough</h2> <p>Hash functions neutralise skew from <em>normally distributed</em> key access. They are powerless against extreme concentration at a single key — the <strong>celebrity problem</strong>.</p> <p>When a celebrity with half a billion followers posts a controversial photo, millions of concurrent requests target a single primary key (the celebrity’s user ID). The hash function dutifully routes all of them to the same physical node, which melts under the load.</p> <p><strong>Application-layer fix:</strong> when the application detects a viral key, it appends a random 2-digit suffix (00–99) before writing. The comment for that post is now stored under 100 distinct keys, which the hash function scatters across 100 different nodes.</p> <p><strong>The read trade-off:</strong> to reassemble all comments, the application must issue a <strong>scatter-gather</strong> query to all 100 shards, wait for every response, merge the results in memory, sort them, and return them to the client. Write pressure is relieved at the cost of dramatically higher read complexity and latency.</p> <hr/> <h2 id="10-automated-vs-manual-rebalancing">10. Automated vs. Manual Rebalancing</h2> <p>Fully automated, aggressive rebalancing is dangerous. A temporary garbage collection pause can cause a node to miss a health-check heartbeat, triggering a false positive. The automated system declares the node dead and launches an emergency data migration. The migration itself saturates CPU and disk on the stressed node — causing it to actually crash. The traffic from the crashed node dumps onto healthy nodes, which now miss their heartbeats. The panic cascades. Within minutes the entire cluster is offline, brought down entirely by the algorithm that was trying to help.</p> <p><strong>The safer approach:</strong> use humans in the loop. An on-call engineer reviews a dashboard, recognises a temporary spike, and declines the rebalancing operation. Slower, but dramatically less likely to trigger a cascade failure.</p> <hr/> <h2 id="11-service-discovery-and-zookeeper">11. Service Discovery and ZooKeeper</h2> <p>With data scattered across hundreds of dynamically changing nodes, how does a client application find the correct shard?</p> <p>A dedicated <strong>routing tier</strong> (fleet of proxy servers) stands between clients and database nodes. Clients never talk to database nodes directly — they talk to the routing tier, which consults a <strong>coordination service</strong>.</p> <p><strong>Apache ZooKeeper</strong> is the classic example. It runs as a small cluster (3–5 nodes) using consensus algorithms to maintain a fault-tolerant, real-time map of which shard lives on which IP address.</p> <p>Every database node holds an open TCP connection (heartbeat) to ZooKeeper and registers an <strong>ephemeral node</strong> in ZooKeeper’s memory. If a database node dies, its TCP connection drops, ZooKeeper deletes its ephemeral node, orchestrates a replica promotion, and immediately pushes the updated map to the routing tier — all within milliseconds. Clients are transparently rerouted with no manual intervention.</p> <hr/> <h2 id="12-secondary-indexes-in-a-sharded-database">12. Secondary Indexes in a Sharded Database</h2> <p>All of the above assumes you know the primary key. Real users search by secondary attributes (“red running shoes, size 10”). Secondary indexes on a sharded database force a hard architectural choice:</p> <h3 id="local-secondary-index-document-partitioning">Local Secondary Index (Document Partitioning)</h3> <p>Each shard maintains its own index covering only the data it holds.</p> <table> <thead> <tr> <th>Operation</th> <th>Performance</th> </tr> </thead> <tbody> <tr> <td><strong>Write</strong></td> <td>Blazing fast — update the local index with no network coordination</td> </tr> <tr> <td><strong>Read</strong></td> <td>Slow — must scatter-gather across every shard, suffering <strong>tail latency amplification</strong></td> </tr> </tbody> </table> <p><strong>Tail latency amplification math:</strong> if each node has a 1% chance of a slow response, querying 100 nodes gives a &gt;60% chance of hitting at least one slow node. Your query is as fast as its slowest component.</p> <h3 id="global-secondary-index-term-partitioning">Global Secondary Index (Term Partitioning)</h3> <p>The index itself is sharded by the indexed term. The global index for “red” lives on a single dedicated node, which holds pointers to every red item across the entire cluster.</p> <table> <thead> <tr> <th>Operation</th> <th>Performance</th> </tr> </thead> <tbody> <tr> <td><strong>Read</strong></td> <td>Fast — one targeted query to the index node returns all matching IDs</td> </tr> <tr> <td><strong>Write</strong></td> <td>Slow — updating one record may require modifying index entries on several shards, requiring a <strong>distributed transaction</strong> (2PC) for consistency</td> </tr> </tbody> </table> <p><strong>The fundamental trade-off:</strong></p> <ul> <li><strong>Local index:</strong> fast writes, slow scatter-gather reads</li> <li><strong>Global index:</strong> fast targeted reads, slow distributed-transaction writes</li> </ul> <p>The right choice depends on your application’s read/write ratio.</p> <hr/> <h2 id="13-summary">13. Summary</h2> <table> <thead> <tr> <th>Concept</th> <th>Core Idea</th> </tr> </thead> <tbody> <tr> <td><strong>Vertical scaling</strong></td> <td>Hits NUMA and cost limits; eventually fails</td> </tr> <tr> <td><strong>Key-range sharding</strong></td> <td>Sorted for range scans; beware write hotspots from monotonic keys</td> </tr> <tr> <td><strong>Hash sharding</strong></td> <td>Even distribution; destroys ordering</td> </tr> <tr> <td><strong>Consistent hashing</strong></td> <td>Fixed logical shards avoid migration storms on topology changes</td> </tr> <tr> <td><strong>Celebrity hotspot fix</strong></td> <td>Append random suffix to viral key; pay with scatter-gather reads</td> </tr> <tr> <td><strong>ZooKeeper</strong></td> <td>Fault-tolerant real-time directory for shard-to-node mapping</td> </tr> <tr> <td><strong>Local index</strong></td> <td>Fast writes, slow scatter-gather reads</td> </tr> <tr> <td><strong>Global index</strong></td> <td>Fast reads, slow distributed-transaction writes</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="distributed-systems"/><category term="sharding"/><category term="partitioning"/><category term="scalability"/><category term="databases"/><summary type="html"><![CDATA[Chapter 6 showed how to keep copies of data on multiple machines. Chapter 7 asks a different question: what do you do when your dataset is so large that no single machine can hold all of it? The answer is sharding — splitting the data into independent chunks and distributing those chunks across a fleet of nodes.]]></summary></entry><entry><title type="html">Part 8: Transactions</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-8-Transactions/" rel="alternate" type="text/html" title="Part 8: Transactions"/><published>2026-06-06T00:00:00+00:00</published><updated>2026-06-06T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%208%20-%20Transactions</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-8-Transactions/"><![CDATA[<p>At 2:00 a.m., a hospital system shows zero doctors on call. Each physician individually checked the rules and got approval to take sick leave. Nothing was coded incorrectly. The bug lived not in any single operation, but in the invisible gap <em>between</em> operations. Chapter 8 of DDIA is about the mechanisms databases use to close that gap.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 8 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-problem-production-is-chaos">1. The Problem: Production Is Chaos</h2> <p>A database in a data center faces a relentlessly hostile environment:</p> <ul> <li>Hard drives develop bad sectors; SSDs wear out.</li> <li>RAM experiences bit flips from cosmic rays.</li> <li>Network cables get cut.</li> <li>Top-of-rack switches reboot mid-query due to firmware bugs.</li> <li>The OS out-of-memory manager kills your process mid-transaction.</li> <li>10,000 concurrent clients race to write the same row simultaneously.</li> </ul> <p>Without help from the database, an application developer must write thousands of lines of error-handling code to safely update a single record. <strong>Transactions</strong> exist to hide this chaos behind a clean abstraction.</p> <hr/> <h2 id="2-the-transaction-abstraction">2. The Transaction Abstraction</h2> <p>A <strong>transaction</strong> groups a sequence of reads and writes into a single logical unit with two possible outcomes:</p> <ul> <li><strong>Commit</strong> — all operations succeed; all changes become visible and permanent.</li> <li><strong>Abort / Rollback</strong> — something went wrong; the database undoes everything as if the transaction never happened.</li> </ul> <p>The application developer doesn’t need to handle partial failures manually. They simply open a transaction, do their work, and call commit. The database handles the rest.</p> <h3 id="the-nosql-detour">The NoSQL Detour</h3> <p>During the late-2000s NoSQL boom, transaction support was deliberately dropped to achieve web-scale throughput. The assumption was that strong guarantees and global scale were mutually exclusive. Developers were left to implement their own consistency checks in application code — a fragile, error-prone approach.</p> <p>This assumption was disproven by <strong>NewSQL</strong> databases like Google Spanner, CockroachDB, and TiDB, which combine global sharding with consensus protocols to deliver strong transactional guarantees at massive scale.</p> <hr/> <h2 id="3-acid">3. ACID</h2> <p>The properties of transactions are captured by the acronym <strong>ACID</strong>, coined in 1983 by Haerder and Reuter. Despite its apparent precision, ACID is today partly a marketing term — different databases implement each letter differently.</p> <h3 id="a--atomicity">A — Atomicity</h3> <p>In ACID, atomicity is about <strong>abortability</strong>, not concurrency (despite the word’s usage in multithreaded programming, where it means something entirely different).</p> <p>If a transaction touches three tables and the server crashes after the second update, the database reverts the first two updates automatically.</p> <p><strong>Mechanism — the undo log (write-ahead log):</strong> Before modifying any real data file on disk, the database appends a note to an append-only log: <em>“I am about to change account A’s balance from $500 to $400.”</em> If the system crashes, the recovery manager reads the log, sees no commit marker, and uses the recorded old values to reverse every change.</p> <h3 id="c--consistency">C — Consistency</h3> <p>Consistency in ACID means the database is in a <em>good state</em> according to your application’s invariants — for example, in double-entry bookkeeping, debits and credits must always sum to zero.</p> <p><strong>Critical point:</strong> the database engine cannot enforce application-level business rules on its own. Foreign key constraints and uniqueness checks help, but defining the invariants and writing code that maintains them is the developer’s responsibility. ACID merely promises that if your transaction preserves consistency, the database will safely store the result.</p> <h3 id="i--isolation">I — Isolation</h3> <p>Multiple concurrent transactions should appear to run as if they are the only transactions on the system. This property is formally called <strong>serializability</strong>: the database guarantees that the final state of the data is identical to some valid serial (one-at-a-time) execution order.</p> <p>Isolation prevents races like two users simultaneously reading a counter as 42 and both writing 43, losing one increment.</p> <h3 id="d--durability">D — Durability</h3> <p>Once the database acknowledges a commit, the data is safe even if the server immediately loses power.</p> <p>In practice, durability is a <strong>spectrum of risk reduction</strong>, not an absolute guarantee:</p> <ul> <li>Writes are persisted to disk using <code class="language-plaintext highlighter-rouge">fsync</code> — but disk controllers lie, reporting success before data has left their own volatile cache.</li> <li>Data is replicated across multiple geographic regions.</li> <li>Regular backups are taken.</li> </ul> <p>Even battle-tested databases have had subtle <code class="language-plaintext highlighter-rouge">fsync</code> bugs. Modern SSDs can silently corrupt data after extended power loss as electrons leak from floating gate transistors. Perfect durability does not exist; the goal is reducing the probability of loss to negligible levels.</p> <hr/> <h2 id="4-single-object-vs-multi-object-operations">4. Single-Object vs Multi-Object Operations</h2> <p>A single-row update within a transaction is simple. The real complexity emerges when a logical operation spans multiple tables or rows — for example, delivering an email requires:</p> <ol> <li>Inserting the email body into the inbox table.</li> <li>Incrementing the unread-message counter.</li> <li>Adding an entry to the search index.</li> </ol> <p>Without multi-object atomicity, a crash between steps 1 and 2 leaves the system permanently inconsistent: the email exists but the counter is wrong. Without isolation, a reader between steps 1 and 2 sees the email but a counter of zero — a coherent-looking but logically wrong state.</p> <p>Pure key-value stores that prohibit multi-object transactions shift all this error handling to application code — a pattern that produces notoriously fragile systems.</p> <hr/> <h2 id="5-isolation-levels">5. Isolation Levels</h2> <p>Full serializability is expensive. Real databases offer a spectrum of <strong>weak isolation levels</strong> that trade safety guarantees for performance. Understanding which anomalies each level prevents is essential.</p> <h3 id="read-committed">Read Committed</h3> <p>The default isolation level in PostgreSQL, SQL Server, and many others. Provides two guarantees:</p> <table> <thead> <tr> <th>Guarantee</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td><strong>No dirty reads</strong></td> <td>You only see data that has been fully committed — never a half-finished transaction’s intermediate state</td> </tr> <tr> <td><strong>No dirty writes</strong></td> <td>If two transactions try to write the same row simultaneously, one waits until the other commits or aborts</td> </tr> </tbody> </table> <p><strong>What it does not prevent — read skew (non-repeatable reads):</strong> Aaliyah has $500 in account A and $500 in account B. A background job moves $100 from A to B. If Aaliyah reads account A ($500) before the transfer and account B ($600) after it, her app shows $1,100 — money appears from nowhere. Both reads returned fully committed data; the world just changed between them.</p> <h3 id="snapshot-isolation-repeatable-read">Snapshot Isolation (Repeatable Read)</h3> <p>Designed specifically to eliminate read skew. The database gives each transaction a <strong>consistent snapshot</strong> of the data at the moment the transaction started. No committed changes made by other transactions after that point are visible.</p> <p><strong>Implementation — MVCC (Multi-Version Concurrency Control):</strong></p> <p>PostgreSQL does not overwrite rows in place. An <code class="language-plaintext highlighter-rouge">UPDATE</code> is implemented as a logical delete of the old row and an insert of a new row. Both rows coexist on disk with hidden metadata:</p> <ul> <li><code class="language-plaintext highlighter-rouge">xmin</code> — the transaction ID that inserted this version of the row.</li> <li><code class="language-plaintext highlighter-rouge">xmax</code> — the transaction ID that deleted it (or null if still current).</li> </ul> <p>If your transaction has ID 12, and a row was modified by transaction 13, the database’s visibility rules hide the modification from you (13 &gt; 12, so it happened “in the future” relative to your snapshot). You see the pre-modification row as if it were still current.</p> <p><strong>The critical property of MVCC: readers never block writers, and writers never block readers.</strong> A long-running analytics query or backup can scan the entire database without holding locks, while concurrent writes proceed unimpeded.</p> <p>A background <strong>vacuum daemon</strong> periodically cleans up old row versions once it is certain no active transaction needs them.</p> <hr/> <h2 id="6-write-skew-and-phantoms">6. Write-Skew and Phantoms</h2> <p>Snapshot isolation eliminates read anomalies but does not protect against all write conflicts.</p> <h3 id="write-skew">Write-Skew</h3> <p>The hospital scenario from the introduction is <strong>write-skew</strong>: two transactions each read a shared condition, each decide it is safe to proceed, and each write to <em>different</em> rows — yet their combined effect violates a business invariant.</p> <p>The hospital invariant: at least one doctor must be on-call.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T1 (Dr. Aaliyah)          T2 (Dr. Bryce)
SELECT COUNT(*)           SELECT COUNT(*)
→ 2                       → 2 (same snapshot)
(2 ≥ 1, safe to leave)   (2 ≥ 1, safe to leave)
UPDATE → off-call         UPDATE → off-call
COMMIT                    COMMIT
→ 0 doctors on-call  ✗
</code></pre></div></div> <p>Neither transaction overwrote the other’s row. A <code class="language-plaintext highlighter-rouge">SELECT FOR UPDATE</code> on Aaliyah’s row would not help Bryce, because he is updating his own row.</p> <h3 id="phantoms">Phantoms</h3> <p>A phantom occurs when a write in one transaction changes the result of a <em>search query</em> in another. If you search for overlapping meeting room bookings before inserting your own, a concurrent insert from another transaction creates a new row that your search did not see — a “phantom” row that should have stopped your booking.</p> <p><strong>Materialising conflicts (last resort):</strong> Pre-populate a table with rows representing every possible time slot for every room, so the application has a concrete row to <code class="language-plaintext highlighter-rouge">SELECT FOR UPDATE</code> before checking availability. This works but pollutes the data model with artificial rows.</p> <hr/> <h2 id="7-serializable-isolation-three-approaches">7. Serializable Isolation: Three Approaches</h2> <p>Full serializability — the guarantee that the outcome is identical to some serial execution — is achievable three ways, each with distinct trade-offs.</p> <h3 id="71-actual-serial-execution">7.1 Actual Serial Execution</h3> <p>Literally run one transaction at a time on a single CPU core, using stored procedures so there is no round-trip latency. Used by Redis and VoltDB.</p> <p><strong>Works when:</strong> all active data fits in RAM and transactions are tiny and fast.</p> <p><strong>Bottleneck:</strong> scaling beyond one core requires partitioning, and cross-partition transactions stall while coordinating over the network.</p> <h3 id="72-two-phase-locking-2pl">7.2 Two-Phase Locking (2PL)</h3> <p>The classical approach, used for decades. Writers block readers; readers block writers.</p> <ul> <li>To <strong>read</strong> a row: acquire a <em>shared lock</em>. Many can hold it simultaneously.</li> <li>To <strong>write</strong> a row: acquire an <em>exclusive lock</em>. No one else can read or write while you hold it.</li> </ul> <p>2PL fully prevents write-skew: each transaction locks the rows it reads, so another transaction cannot modify those rows mid-execution.</p> <p><strong>Cost:</strong> heavy lock contention, reduced throughput, and <strong>deadlocks</strong> (Transaction A holds row 1 and waits for row 2; Transaction B holds row 2 and waits for row 1). The database must run background deadlock detection and forcefully abort one side, requiring a retry.</p> <h3 id="73-serializable-snapshot-isolation-ssi">7.3 Serializable Snapshot Isolation (SSI)</h3> <p>The modern approach, implemented in PostgreSQL and CockroachDB. It combines the MVCC snapshots of snapshot isolation with a monitoring layer that detects conflicts.</p> <p><strong>Key insight:</strong> instead of preventing conflicts via blocking locks, assume conflicts are rare and let transactions run freely. Track what each transaction has read, and abort only when a commit would produce a non-serializable outcome.</p> <p>Applied to the hospital scenario:</p> <ol> <li>Dr. Aaliyah’s transaction commits first. She is now off-call.</li> <li>Dr. Bryce’s transaction attempts to commit.</li> <li>SSI checks: “Did Bryce’s read of the on-call count become stale because another transaction modified it?” Yes — Aaliyah changed it.</li> <li>SSI aborts Bryce’s transaction. He retries, now sees count = 1, and his leave request is correctly rejected.</li> </ol> <p><strong>Result:</strong> readers don’t block writers; writers don’t block readers; full serializability is maintained. The overhead only appears on the rare transaction that actually conflicts.</p> <hr/> <h2 id="8-distributed-transactions">8. Distributed Transactions</h2> <p>All of the above describes single-node databases. When a transaction must span multiple shards or services — for example, deducting $100 from a New York shard and crediting $100 to a Tokyo shard — we need distributed atomicity.</p> <h3 id="two-phase-commit-2pc">Two-Phase Commit (2PC)</h3> <p>A <strong>coordinator</strong> node orchestrates a two-phase protocol:</p> <p><strong>Phase 1 — Prepare:</strong> Coordinator asks all participants: “Can you commit?” Each participant checks constraints, writes its undo log to disk, and replies yes or no. A yes is an irrevocable promise — the participant surrenders its right to abort unilaterally.</p> <p><strong>Phase 2 — Commit:</strong> If all say yes, coordinator broadcasts commit. If anyone said no, coordinator broadcasts abort.</p> <p><strong>The fatal flaw:</strong> if the coordinator crashes between Phase 1 and Phase 2, participants are left <strong>in doubt</strong> — they cannot abort (they promised) and they cannot commit (they haven’t received the order). They hold locks on database rows indefinitely. The entire system stalls until the coordinator recovers.</p> <p><strong>XA transactions</strong> extend 2PC across heterogeneous systems (e.g., PostgreSQL + Apache Kafka). The standardised lowest-common-denominator protocol performs poorly and cannot detect cross-system deadlocks.</p> <h3 id="consensus-backed-distributed-transactions">Consensus-Backed Distributed Transactions</h3> <p>NewSQL databases replace the single coordinator with a <strong>consensus cluster</strong> (e.g., Raft or Paxos). The coordinator’s state — the transaction intent log — is replicated across 3–5 nodes. A quorum must acknowledge each log entry before it is committed.</p> <p>If the leader coordinator dies, the surviving nodes detect the failure via missed heartbeats, elect a new leader in milliseconds, and resume the transaction from where the replicated log left off. There is no stuck <em>in-doubt</em> state because the log is durable across the cluster.</p> <p>This is the architectural foundation of Google Spanner and CockroachDB: strong, distributed ACID transactions that survive coordinator failures without stalling.</p> <hr/> <h2 id="9-summary">9. Summary</h2> <table> <thead> <tr> <th>Concept</th> <th>Core Idea</th> </tr> </thead> <tbody> <tr> <td><strong>Atomicity</strong></td> <td>All-or-nothing via undo log; abortability is the key property</td> </tr> <tr> <td><strong>Consistency</strong></td> <td>Application’s responsibility; database enforces constraints, not business rules</td> </tr> <tr> <td><strong>Durability</strong></td> <td>Spectrum of risk reduction; <code class="language-plaintext highlighter-rouge">fsync</code>, replication, backups</td> </tr> <tr> <td><strong>Isolation</strong></td> <td>Concurrency safety; serializability is the gold standard</td> </tr> <tr> <td><strong>Read Committed</strong></td> <td>Prevents dirty reads and dirty writes; allows read skew</td> </tr> <tr> <td><strong>Snapshot Isolation / MVCC</strong></td> <td>Consistent snapshot via xmin/xmax; readers never block writers</td> </tr> <tr> <td><strong>Write-Skew</strong></td> <td>Concurrent transactions each check a shared condition, then write to different rows, breaking an invariant</td> </tr> <tr> <td><strong>SSI</strong></td> <td>Optimistic serializability; abort only on actual conflict</td> </tr> <tr> <td><strong>2PC</strong></td> <td>Distributed atomicity; coordinator is a single point of failure</td> </tr> <tr> <td><strong>Raft/Paxos in NewSQL</strong></td> <td>Replicated coordinator eliminates the 2PC crash risk</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="distributed-systems"/><category term="transactions"/><category term="acid"/><category term="concurrency"/><category term="databases"/><summary type="html"><![CDATA[At 2:00 a.m., a hospital system shows zero doctors on call. Each physician individually checked the rules and got approval to take sick leave. Nothing was coded incorrectly. The bug lived not in any single operation, but in the invisible gap between operations. Chapter 8 of DDIA is about the mechanisms databases use to close that gap.]]></summary></entry><entry><title type="html">Part 1: Trade-Offs in Data Systems Architecture</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-1-Trade-Offs-in-Data-Systems-Architecture/" rel="alternate" type="text/html" title="Part 1: Trade-Offs in Data Systems Architecture"/><published>2026-06-04T00:00:00+00:00</published><updated>2026-06-04T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%201%20-%20Trade-Offs%20in%20Data%20Systems%20Architecture</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-1-Trade-Offs-in-Data-Systems-Architecture/"><![CDATA[<p>The very first lesson of serious systems engineering is that there is no perfect architecture. As the economist Thomas Sowell put it: <em>“There are no solutions, there are only trade-offs.”</em> Every technology choice — from the database you pick to how you deploy your service — involves accepting costs in exchange for benefits. Chapter 1 of DDIA (2nd ed.) frames the entire book around this principle.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 1 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-two-audiences-for-data">1. The Two Audiences for Data</h2> <p>Modern systems serve two fundamentally different classes of users, each with radically different access patterns:</p> <table> <thead> <tr> <th>Audience</th> <th>Goal</th> <th>Pattern</th> </tr> </thead> <tbody> <tr> <td><strong>End users</strong></td> <td>Interact with the live application</td> <td>High-frequency, low-latency point lookups</td> </tr> <tr> <td><strong>Analysts / scientists</strong></td> <td>Understand trends in the business</td> <td>Rare, expensive scans over large datasets</td> </tr> </tbody> </table> <p>Trying to serve both audiences with the same database is one of the most common early-stage performance mistakes.</p> <hr/> <h2 id="2-oltp-vs-olap">2. OLTP vs OLAP</h2> <h3 id="online-transaction-processing-oltp">Online Transaction Processing (OLTP)</h3> <p>OLTP databases power customer-facing applications. Think of PostgreSQL backing a web shop: every login, cart update, and purchase is a small, targeted operation against a handful of rows.</p> <p><strong>Under the hood:</strong></p> <ul> <li>Data is laid out <strong>row by row</strong> on disk (row-oriented storage).</li> <li>Queries navigate directly to the relevant row via a <strong>B-tree index</strong> — the database never has to scan the whole table.</li> <li>Optimised for <strong>low-latency point reads and writes</strong> (single-digit milliseconds).</li> </ul> <h3 id="online-analytical-processing-olap">Online Analytical Processing (OLAP)</h3> <p>Business analysts and data scientists ask questions like <em>“What was revenue per region in Q4 across all 50 stores?”</em> — queries that scan millions of rows but only read a handful of columns.</p> <p>Running such a query against a live OLTP database would pin the CPU at 100% and freeze the application for every real user. The solution is a <strong>separate, read-only data warehouse</strong>.</p> <p><strong>Under the hood:</strong></p> <ul> <li>Data is loaded via an nightly <strong>ETL</strong> (Extract–Transform–Load) process.</li> <li>The warehouse uses <strong>columnar storage</strong>: all values for one column (e.g., all prices) are stored together on disk.</li> <li>When the analyst asks for the average price, the database reads only the price column into the CPU — never the name, address, or email of each customer.</li> <li>Combined with SIMD vector processing, columnar layout can aggregate billions of rows in seconds.</li> </ul> <pre><code class="language-mermaid">graph LR
    A[Live App] --&gt;|point queries| B[(OLTP DB)]
    B --&gt;|nightly ETL| C[(Data Warehouse)]
    C --&gt;|analytical queries| D[Analysts / BI]

    classDef default fill:#1a1a1a,stroke:#888,stroke-width:2px,color:#fff;
    classDef highlight fill:#2196F3,stroke:#fff,stroke-width:2px,color:#fff;
    class A,D highlight;
</code></pre> <hr/> <h2 id="3-data-lakes-the-schema-on-read-revolution">3. Data Lakes: The Schema-on-Read Revolution</h2> <p>A data warehouse works beautifully for questions you already know how to ask. But machine learning requires raw, messy data — unstructured JSON logs, audio files, IoT sensor readings — that doesn’t fit a rigid SQL schema.</p> <table> <thead> <tr> <th> </th> <th>Data Warehouse</th> <th>Data Lake</th> </tr> </thead> <tbody> <tr> <td><strong>Schema enforcement</strong></td> <td>Schema-on-write (rejected at ingest)</td> <td>Schema-on-read (interpreted at query time)</td> </tr> <tr> <td><strong>Typical storage</strong></td> <td>Columnar files (Parquet, ORC)</td> <td>Raw files on object storage (S3, GCS)</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Known analytical queries</td> <td>Exploratory ML, NLP, feature engineering</td> </tr> </tbody> </table> <p>The practical rule: a data warehouse answers questions you already understand; a data lake holds raw material for questions you haven’t thought of yet. Both often coexist in the same organisation.</p> <hr/> <h2 id="4-systems-of-record-vs-derived-data">4. Systems of Record vs. Derived Data</h2> <p>Every non-trivial system separates these two roles:</p> <ul> <li><strong>System of record</strong> — the authoritative, permanent source of truth (e.g., the primary PostgreSQL database). Optimised for safe, consistent writes.</li> <li><strong>Derived data system</strong> — a redundant copy optimised for a specific access pattern (e.g., a Redis cache, a search index, a materialised view). It can always be rebuilt from the source of record.</li> </ul> <p>The hardest problem this introduces is <strong>cache invalidation</strong>: ensuring derived copies are updated when the source of truth changes. This is a fundamental tension that appears throughout the book.</p> <hr/> <h2 id="5-cloud-vs-self-hosting">5. Cloud vs. Self-Hosting</h2> <h3 id="the-self-hosting-problem-capacity-planning">The Self-Hosting Problem: Capacity Planning</h3> <p>Owning bare-metal servers forces a brutal binary choice:</p> <ul> <li><strong>Under-provision</strong> → your app crashes on a viral traffic spike.</li> <li><strong>Over-provision</strong> → you pay for idle hardware 99% of the time.</li> </ul> <h3 id="cloud-elasticity">Cloud Elasticity</h3> <p>Cloud providers virtualise hardware and let you rent compute by the second. If traffic spikes, you spin up 100 extra VMs in milliseconds (<strong>horizontal scaling</strong>). When traffic drops, you terminate them and stop paying.</p> <p>The modern operations role (SRE / DevOps) shifted from physically racking servers to writing <strong>infrastructure-as-code</strong> (Terraform, Pulumi) and managing cloud spend (<strong>FinOps</strong>).</p> <h3 id="separation-of-storage-and-compute">Separation of Storage and Compute</h3> <p>The cloud’s fast intra-datacenter networks enable a powerful pattern: <strong>disaggregation</strong>. Systems like Snowflake store data cheaply in S3 and spin up separate, ephemeral compute clusters only when a query runs. A 10 TB dataset costs pennies per month to store; you only pay for expensive CPUs the five minutes per month you actually need them.</p> <hr/> <h2 id="6-monoliths-vs-microservices">6. Monoliths vs. Microservices</h2> <h3 id="the-monolith-problem">The Monolith Problem</h3> <p>A monolithic application bundles every feature — auth, payments, inventory, email — into a single codebase and process. One memory leak in the image upload service brings down the checkout and homepage alike. Deploying a CSS fix requires coordinating every team and re-deploying the entire application.</p> <h3 id="microservices">Microservices</h3> <p>Microservices decompose the monolith into small, independently deployable processes. The critical rule: <strong>each service owns its own database</strong>. Services communicate only through well-defined APIs (REST or gRPC). A failure in one service is contained — other services keep running.</p> <p>The trade-off is substantial: you replace in-process function calls (nanoseconds, 100% reliable) with network calls (milliseconds, fail constantly). You also take on enormous operational complexity — distributed tracing, retry logic, service discovery, and network partitions.</p> <p><strong>The rule of thumb:</strong> microservices pay off when your organisation has enough engineers that the human coordination cost of sharing one codebase exceeds the engineering cost of managing the distributed system.</p> <hr/> <h2 id="7-serverless-function-as-a-service">7. Serverless (Function-as-a-Service)</h2> <p>Serverless takes the pay-as-you-go model to its logical extreme. You deploy individual functions (AWS Lambda, Google Cloud Functions); the provider handles the infrastructure entirely. You pay only for the milliseconds your code actually executes.</p> <table> <thead> <tr> <th>Advantage</th> <th>Trade-off</th> </tr> </thead> <tbody> <tr> <td>Zero cost at zero traffic</td> <td><strong>Cold start latency</strong> (~100ms–1s on first invocation)</td> </tr> <tr> <td>Scales to millions of requests automatically</td> <td><strong>Stateless</strong> — no memory between requests</td> </tr> <tr> <td>No OS patching or server management</td> <td>Requires event-driven, asynchronous architecture</td> </tr> </tbody> </table> <hr/> <h2 id="8-data-privacy-as-an-architectural-constraint">8. Data Privacy as an Architectural Constraint</h2> <p>The chapter closes with a dimension that many engineers treat as an afterthought: privacy law. GDPR (Europe), CCPA (California), and PCI-DSS (payment cards) impose hard engineering requirements.</p> <p>The collision point is <strong>append-only immutable logs</strong> — a widely used pattern (Kafka, event sourcing) where deleting past records is either impossible or structurally corrupting.</p> <p>Two approaches have emerged:</p> <ol> <li><strong>Data minimisation</strong> — only collect what is strictly needed. If you don’t have the data, it can’t be breached, leaked, or subpoenaed.</li> <li><strong>Crypto-shredding</strong> — encrypt each user’s personal payload with a unique key. When they invoke the right to be forgotten, destroy the key. The bytes remain in the log but are permanently unreadable.</li> </ol> <p>The deeper lesson: privacy laws are engineering constraints just like network latency or disk IOPS. Treating them as legal afterthoughts leads to architectures that literally cannot comply with the law.</p> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li>There is no universally best architecture — every choice trades one set of costs for another.</li> <li>The OLTP/OLAP split exists because the physical access patterns of transactional and analytical workloads are fundamentally incompatible.</li> <li>Cloud elasticity shifts hardware from a capital cost to an operational cost and enables storage/compute disaggregation.</li> <li>Microservices solve a human coordination problem but create a distributed systems engineering problem.</li> <li>Privacy requirements must be designed in from day one, not retrofitted.</li> </ul> <p><em>Next: Chapter 2 — Defining Non-Functional Requirements (scalability, reliability, maintainability).</em></p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="data-systems"/><category term="oltp"/><category term="olap"/><category term="cloud"/><category term="distributed-systems"/><summary type="html"><![CDATA[The very first lesson of serious systems engineering is that there is no perfect architecture. As the economist Thomas Sowell put it: “There are no solutions, there are only trade-offs.” Every technology choice — from the database you pick to how you deploy your service — involves accepting costs in exchange for benefits. Chapter 1 of DDIA (2nd ed.) frames the entire book around this principle.]]></summary></entry></feed>