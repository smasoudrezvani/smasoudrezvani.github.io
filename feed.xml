<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://smasoudrezvani.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://smasoudrezvani.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-07T13:24:25+00:00</updated><id>https://smasoudrezvani.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal website of Masoud Rezvani (also known as Masoud Rezvaninejad) — Data Scientist, MBA, and Master&apos;s student at UvA. Research interests include AI, Deep Learning, LLMs, NLP, and MLOps. </subtitle><entry><title type="html">Part 6: Replication</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-6-Replication/" rel="alternate" type="text/html" title="Part 6: Replication"/><published>2026-06-06T00:00:00+00:00</published><updated>2026-06-06T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%206%20-%20Replication</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-6-Replication/"><![CDATA[<p>Saving a copy of data to a single machine means one hard drive failure can wipe everything out. <em>Replication</em> — keeping the same data on multiple machines connected over a network — is the foundational technique that gives modern databases fault tolerance, geographic reach, and read scalability. Chapter 6 of DDIA reveals just how hard that simple-sounding idea actually is.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 6 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-why-replicate">1. Why Replicate?</h2> <p>There are three distinct reasons engineering teams invest in replication:</p> <table> <thead> <tr> <th>Goal</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td><strong>High availability</strong></td> <td>If one node dies, the others keep serving traffic</td> </tr> <tr> <td><strong>Reduced latency</strong></td> <td>Place data physically close to users in different regions</td> </tr> <tr> <td><strong>Read scalability</strong></td> <td>Spread millions of concurrent reads across many replicas</td> </tr> </tbody> </table> <p>The chapter organises the strategies for achieving these goals into three architectures: <strong>single-leader</strong>, <strong>multi-leader</strong>, and <strong>leaderless</strong> replication.</p> <hr/> <h2 id="2-single-leader-replication">2. Single-Leader Replication</h2> <h3 id="the-hierarchy">The Hierarchy</h3> <p>One node is elected as the <strong>leader</strong> (also called master or primary). Every write request must be routed to it. The leader writes the change to its local storage, then streams a log of that change to <strong>follower</strong> nodes (replicas). Followers are read-only — they copy the leader’s log and serve read traffic.</p> <p>This architecture is the default for PostgreSQL, MySQL, and Kafka.</p> <h3 id="zero-disk-architectures">Zero-Disk Architectures</h3> <p>The industry is shifting toward removing local hard drives entirely from database nodes. In a <strong>zero-disk architecture (ZDA)</strong>, all permanent data lives in object storage like Amazon S3. Nodes use RAM and fast local NVMe drives purely as ephemeral write buffers, asynchronously flushing batches to S3 in the background. This decouples compute from storage: a crashed node can be replaced by spinning up a fresh one and pointing it at the same S3 bucket.</p> <h3 id="synchronous-vs-asynchronous-replication">Synchronous vs Asynchronous Replication</h3> <p>This is one of the most consequential choices in the entire architecture.</p> <p><strong>Synchronous:</strong> The leader waits for the follower to confirm every write before acknowledging success to the client. If the follower is slow (garbage collection pause, dropped packet), the entire system halts. In practice, no production system runs fully synchronous because a single slow replica can bring writes to a standstill.</p> <p><strong>Asynchronous:</strong> The leader writes to its local write-ahead log (WAL), immediately acknowledges the client, and streams updates to followers in the background. If the leader dies before the background process syncs, any acknowledged-but-unsynced writes are permanently lost.</p> <p><strong>Semi-synchronous (the real-world compromise):</strong> Exactly one follower confirms synchronously, guaranteeing at least two durable copies. The rest catch up asynchronously.</p> <hr/> <h2 id="3-failover-and-its-nightmares">3. Failover and Its Nightmares</h2> <p>When the leader fails, the system must:</p> <ol> <li><strong>Detect the failure</strong> — nodes exchange heartbeat messages; if no response arrives within a timeout (typically 30 s), the node is declared dead.</li> <li><strong>Elect a new leader</strong> — remaining nodes run a consensus algorithm (Raft, Paxos) and promote the follower with the most up-to-date log.</li> <li><strong>Reconfigure routing</strong> — all clients must be redirected to the new leader.</li> </ol> <h3 id="split-brain">Split-Brain</h3> <p>The most dangerous failure mode: the old leader was not dead, just temporarily disconnected. When it reconnects, it still believes it is the legitimate leader. Two nodes both accepting writes produces <strong>split-brain</strong> — two divergent truths that cannot be automatically merged in a single-leader system.</p> <p>The industry’s blunt solution is <strong>STONITH</strong> (<em>Shoot The Other Node In The Head</em>): a node detecting split-brain signals a power distribution unit to cut electricity to the old leader, physically preventing it from accepting further writes.</p> <hr/> <h2 id="4-eventual-consistency-and-its-anomalies">4. Eventual Consistency and Its Anomalies</h2> <p>Because followers replicate asynchronously, they may lag behind the leader by milliseconds or even minutes. During this window the system is <strong>eventually consistent</strong> — a weak guarantee that all replicas will converge <em>if</em> writes stop, but offering no promise about intermediate states.</p> <p>This lag creates three classical user-visible anomalies:</p> <h3 id="41-read-after-write-read-your-writes">4.1 Read-After-Write (Read-Your-Writes)</h3> <p>A user writes data to the leader, then immediately reads it from a lagging follower. Their own update appears to have vanished.</p> <p><strong>Fix:</strong> Route a user’s own reads back to the leader for a short window after a write. Other users’ reads can safely go to followers because the reader has no expectation about the exact freshness of data they didn’t write.</p> <h3 id="42-monotonic-reads">4.2 Monotonic Reads</h3> <p>A load balancer routes successive reads from the same user to different replicas. If the second replica is lagging more than the first, the user observes a comment appear, then disappear on the next page refresh — time appears to move backwards.</p> <p><strong>Fix:</strong> Hash the user ID to permanently pin each user to a specific replica for reads.</p> <h3 id="43-consistent-prefix-reads">4.3 Consistent Prefix Reads</h3> <p>In a partitioned database, if a question and its answer are stored on different shards with uneven replication lag, an observer can see the answer before the question — causality is shattered.</p> <p><strong>Fix:</strong> Ensure causally related writes go to the same partition so the partition’s internal log preserves their order.</p> <hr/> <h2 id="5-multi-leader-replication">5. Multi-Leader Replication</h2> <p>When users are distributed globally, forcing every write across an ocean to a single leader in one data center adds hundreds of milliseconds of latency. The solution: place a <strong>leader in each major region</strong>. A Tokyo write hits the Tokyo leader instantly; that leader then asynchronously forwards the change to the London leader in the background.</p> <p>Multi-leader is also the model used by <strong>collaborative offline apps</strong> (notes apps, document editors): your phone’s local SQLite database acts as a leader, syncing changes to the cloud when connectivity is restored.</p> <h3 id="leader-topologies">Leader Topologies</h3> <table> <thead> <tr> <th>Topology</th> <th>Structure</th> <th>Risk</th> </tr> </thead> <tbody> <tr> <td><strong>Circular</strong></td> <td>A → B → C → A</td> <td>One failed node breaks the chain</td> </tr> <tr> <td><strong>Star</strong></td> <td>One hub broadcasts to leaf nodes</td> <td>Hub is a bottleneck</td> </tr> <tr> <td><strong>All-to-all</strong></td> <td>Every leader broadcasts to every other</td> <td>Most resilient; ordering problems</td> </tr> </tbody> </table> <h3 id="write-conflicts">Write Conflicts</h3> <p>Single-leader systems avoid conflicts entirely because writes are serialised through one node. Multi-leader systems cannot — two users can concurrently modify the same record on two different continents and both leaders accept the write.</p> <p><strong>Conflict resolution strategies:</strong></p> <table> <thead> <tr> <th>Strategy</th> <th>Mechanism</th> <th>Downside</th> </tr> </thead> <tbody> <tr> <td><strong>Operational Transformation (OT)</strong></td> <td>Tracks the intent of every keystroke and transforms concurrent operations to preserve intent</td> <td>Notoriously hard to implement correctly</td> </tr> <tr> <td><strong>CRDTs</strong></td> <td>Assigns a unique permanent ID to every element; operations commute in any order</td> <td>Deleted items leave invisible “tombstone” ghosts in memory, causing storage bloat</td> </tr> <tr> <td><strong>Last Write Wins (LWW)</strong></td> <td>The write with the highest timestamp survives</td> <td>Clock skew between servers means the “winner” may actually be the older write; silently discards data</td> </tr> </tbody> </table> <hr/> <h2 id="6-leaderless-replication">6. Leaderless Replication</h2> <p>Leaderless systems (pioneered by Amazon Dynamo, used by Cassandra and Riak) eliminate the hierarchy entirely. Every replica can accept any write.</p> <h3 id="quorum-reads-and-writes">Quorum Reads and Writes</h3> <p>Consistency is maintained through the <strong>quorum condition</strong>: with <em>N</em> total replicas, if you write to <em>W</em> nodes and read from <em>R</em> nodes, and <em>W + R &gt; N</em>, at least one node in every read set must overlap with every write set.</p> <p>A typical configuration for <em>N</em> = 5: <em>W</em> = 3, <em>R</em> = 3.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>W + R = 3 + 3 = 6 &gt; 5 = N  ✓
</code></pre></div></div> <p>Even if two nodes are offline, any read of three nodes is mathematically guaranteed to include at least one node that witnessed the write.</p> <h3 id="read-repair">Read Repair</h3> <p>When a client reads from multiple replicas and sees different versions (identified by monotonically increasing version numbers), it writes the newest version back to the stale nodes. The act of reading heals the database.</p> <h3 id="version-vectors">Version Vectors</h3> <p>A single version number only tracks absolute ordering from one node’s perspective. A <strong>version vector</strong> is an array tracking each replica’s version number independently — e.g. <code class="language-plaintext highlighter-rouge">{nodeA: 2, nodeB: 1, nodeC: 0}</code>. This creates a mathematical causal family tree of the data, allowing the system to distinguish:</p> <ul> <li><strong>Causal writes</strong> (flour was added after milk → flour is a descendant)</li> <li><strong>Concurrent writes</strong> (eggs and bacon added simultaneously → neither is an ancestor of the other)</li> </ul> <h3 id="concurrency-without-clocks">Concurrency Without Clocks</h3> <p>The most mind-bending insight in this section: two operations are <strong>concurrent</strong> if neither knows about the other — regardless of when they happened on a wall clock. If a network cable under the ocean is severed, a write made in London on Tuesday and a write made in Tokyo on Thursday are concurrent to the database, because neither server knew about the other’s action when it was created. When the cable is repaired, those writes meet as peers with no causal relationship.</p> <blockquote> <p>Concurrency is formally defined by the <em>absence of a happens-before relationship</em>, not by physical time.</p> </blockquote> <hr/> <h2 id="7-architecture-comparison">7. Architecture Comparison</h2> <table> <thead> <tr> <th>Approach</th> <th>Strengths</th> <th>Weaknesses</th> </tr> </thead> <tbody> <tr> <td><strong>Single-leader</strong></td> <td>Simple ordering, no write conflicts</td> <td>Single point of failure, replication lag anomalies, complex failover</td> </tr> <tr> <td><strong>Multi-leader</strong></td> <td>Low geographic latency, offline support</td> <td>Mandatory conflict resolution, OT/CRDT complexity</td> </tr> <tr> <td><strong>Leaderless</strong></td> <td>Extreme resilience, no failover needed</td> <td>No linear ordering, quorum math required, version vectors complex</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="distributed-systems"/><category term="replication"/><category term="consistency"/><category term="databases"/><summary type="html"><![CDATA[Saving a copy of data to a single machine means one hard drive failure can wipe everything out. Replication — keeping the same data on multiple machines connected over a network — is the foundational technique that gives modern databases fault tolerance, geographic reach, and read scalability. Chapter 6 of DDIA reveals just how hard that simple-sounding idea actually is.]]></summary></entry><entry><title type="html">Part 7: Sharding</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-7-Sharding/" rel="alternate" type="text/html" title="Part 7: Sharding"/><published>2026-06-06T00:00:00+00:00</published><updated>2026-06-06T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%207%20-%20Sharding</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-7-Sharding/"><![CDATA[<p>Chapter 6 showed how to keep <em>copies</em> of data on multiple machines. Chapter 7 asks a different question: what do you do when your dataset is so large that no single machine can hold all of it? The answer is <strong>sharding</strong> — splitting the data into independent chunks and distributing those chunks across a fleet of nodes.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 7 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-vertical-scaling-and-its-hard-limits">1. Vertical Scaling and Its Hard Limits</h2> <p>The instinctive first response to a slow database is to buy a bigger server — more RAM, more CPU cores, faster SSDs. This is <strong>vertical scaling</strong>, and it works well up to a point.</p> <p>The wall it hits is the <strong>NUMA (Non-Uniform Memory Access)</strong> architecture of modern multi-socket servers. When a CPU needs data stored in the RAM bank of a different CPU socket, it must cross the motherboard’s interconnect bus. As you add more sockets, cross-socket communication becomes a bottleneck. The machine spends increasing fractions of its time shuffling data internally rather than executing your queries.</p> <p>On top of that, hardware pricing is super-linear: a server with 128 cores can cost five times more than a 64-core machine, not twice as much.</p> <p><strong>Vertical scaling has a hard physical and economic ceiling.</strong></p> <hr/> <h2 id="2-horizontal-scaling-the-shared-nothing-architecture">2. Horizontal Scaling: The Shared-Nothing Architecture</h2> <p>Instead of one enormous server, use a fleet of commodity machines. Each node handles a roughly equal fraction of the total data and query load. This is <strong>horizontal scaling</strong> (also called a <em>shared-nothing</em> architecture).</p> <p>A critical point: <strong>sharding and replication are not mutually exclusive.</strong> In production, shards are always replicated. A node might be the primary leader for Shard A while simultaneously acting as a follower replica for Shards B and C. If the primary for Shard A dies, the cluster promotes its replica with no data loss.</p> <hr/> <h2 id="3-multi-tenancy-and-isolation">3. Multi-Tenancy and Isolation</h2> <p>Beyond raw throughput, sharding solves a second problem: <strong>resource isolation between tenants</strong> in SaaS products.</p> <p>In a centralised database serving thousands of corporate clients, one client running a heavy analytical query can consume all available CPU and RAM — starving every other client. Assigning each tenant to their own shard creates hard resource boundaries.</p> <p>Isolation benefits extend further:</p> <ul> <li><strong>Permission isolation:</strong> A bug that drops a <code class="language-plaintext highlighter-rouge">WHERE</code> clause in application code cannot leak Tenant A’s data to Tenant B when they are on physically separate machines.</li> <li><strong>Restore isolation:</strong> Rolling back a specific tenant’s shard to last hour’s backup does not affect any other tenant on the platform.</li> <li><strong>Data privacy compliance (GDPR):</strong> Deleting a European user’s data is a localised, verifiable operation on their shard rather than a complex archaeological dig through shared B-trees.</li> </ul> <hr/> <h2 id="4-the-enemy-skew">4. The Enemy: Skew</h2> <p>The entire goal of a sharding strategy is an even distribution of data and query load. When the distribution is uneven, some nodes become <strong>hotspots</strong> while others sit idle. This is called <strong>skew</strong> — and it defeats the entire purpose of building a cluster.</p> <hr/> <h2 id="5-key-range-sharding">5. Key-Range Sharding</h2> <p>Divide the total spectrum of primary keys into contiguous sorted ranges and assign each range to a node. Think of a printed encyclopedia: Volume 1 holds A–B, Volume 2 holds C–D, and so on.</p> <p><strong>Advantages:</strong></p> <ul> <li>Data is stored in sorted order on disk (typically in SSTables or B-trees).</li> <li><strong>Range scans</strong> are extremely fast: a query for “all temperature readings in July” locates the first entry for July 1 and streams sequentially to August 1 — no random seeks.</li> </ul> <p><strong>The fatal flaw — write hotspots:</strong></p> <p>If your partition key is a monotonically increasing timestamp and sensors write thousands of records per second, 100% of incoming writes land on the shard covering <em>right now</em>. Every other shard sits idle.</p> <p><strong>Fix: compound keys.</strong> Prepend the sensor ID to the timestamp — <code class="language-plaintext highlighter-rouge">sensor_73:2026-07-01</code>. Now writes for the current moment are spread across as many shards as there are sensors. The cost is that a query for “all sensors in July” must scatter across all shards and gather the results, increasing read complexity.</p> <hr/> <h2 id="6-hash-sharding">6. Hash Sharding</h2> <p>Pass the primary key through a hash function (e.g., Murmur3 or MD5) before routing. A good hash function has the <strong>avalanche effect</strong>: even slightly different input keys produce wildly different output values, uniformly scattered across the output space.</p> <p>This <strong>mathematically eliminates skew</strong> from predictable input distributions.</p> <p><strong>The cost:</strong> all notion of ordering is destroyed. User <code class="language-plaintext highlighter-rouge">0001</code> and User <code class="language-plaintext highlighter-rouge">0002</code> will land on entirely different shards. Range queries become impossible.</p> <hr/> <h2 id="7-the-modulo-trap">7. The Modulo Trap</h2> <p>A natural implementation of hash sharding is to compute <code class="language-plaintext highlighter-rouge">hash(key) mod N</code>, where <em>N</em> is the number of nodes. This routes each key to a node in range <code class="language-plaintext highlighter-rouge">[0, N-1]</code>.</p> <p><strong>The problem:</strong> when <em>N</em> changes (a node is added or removed), almost every key maps to a different node. The cluster must physically migrate terabytes of data across the network — a migration storm that saturates bandwidth and can cause a full production outage.</p> <hr/> <h2 id="8-consistent-hashing-with-fixed-logical-shards">8. Consistent Hashing with Fixed Logical Shards</h2> <p>The solution is to <strong>decouple logical shards from physical nodes</strong>.</p> <p>Create a large fixed number of logical shards (e.g., 1,000 virtual buckets). The hash is always computed modulo 1,000 — a denominator that never changes. These 1,000 buckets are then assigned to physical nodes like dealing cards:</p> <ul> <li>10 nodes → ~100 buckets per node</li> <li>Node fails → only its ~100 buckets are redistributed to surviving nodes; the other 900 are untouched.</li> <li>Node added → steal ~100 buckets from existing nodes; no wholesale rehashing.</li> </ul> <p>This limits data movement to the absolute minimum required to maintain balance.</p> <p>A variant used by Apache Cassandra is the <strong>hash ring</strong>: the hash output space is visualised as a circle, and each node “owns” a contiguous arc of that circle. Overloaded nodes split their arc and hand half to a newly spawned node.</p> <hr/> <h2 id="9-celebrity-hotspots-when-math-isnt-enough">9. Celebrity Hotspots: When Math Isn’t Enough</h2> <p>Hash functions neutralise skew from <em>normally distributed</em> key access. They are powerless against extreme concentration at a single key — the <strong>celebrity problem</strong>.</p> <p>When a celebrity with half a billion followers posts a controversial photo, millions of concurrent requests target a single primary key (the celebrity’s user ID). The hash function dutifully routes all of them to the same physical node, which melts under the load.</p> <p><strong>Application-layer fix:</strong> when the application detects a viral key, it appends a random 2-digit suffix (00–99) before writing. The comment for that post is now stored under 100 distinct keys, which the hash function scatters across 100 different nodes.</p> <p><strong>The read trade-off:</strong> to reassemble all comments, the application must issue a <strong>scatter-gather</strong> query to all 100 shards, wait for every response, merge the results in memory, sort them, and return them to the client. Write pressure is relieved at the cost of dramatically higher read complexity and latency.</p> <hr/> <h2 id="10-automated-vs-manual-rebalancing">10. Automated vs. Manual Rebalancing</h2> <p>Fully automated, aggressive rebalancing is dangerous. A temporary garbage collection pause can cause a node to miss a health-check heartbeat, triggering a false positive. The automated system declares the node dead and launches an emergency data migration. The migration itself saturates CPU and disk on the stressed node — causing it to actually crash. The traffic from the crashed node dumps onto healthy nodes, which now miss their heartbeats. The panic cascades. Within minutes the entire cluster is offline, brought down entirely by the algorithm that was trying to help.</p> <p><strong>The safer approach:</strong> use humans in the loop. An on-call engineer reviews a dashboard, recognises a temporary spike, and declines the rebalancing operation. Slower, but dramatically less likely to trigger a cascade failure.</p> <hr/> <h2 id="11-service-discovery-and-zookeeper">11. Service Discovery and ZooKeeper</h2> <p>With data scattered across hundreds of dynamically changing nodes, how does a client application find the correct shard?</p> <p>A dedicated <strong>routing tier</strong> (fleet of proxy servers) stands between clients and database nodes. Clients never talk to database nodes directly — they talk to the routing tier, which consults a <strong>coordination service</strong>.</p> <p><strong>Apache ZooKeeper</strong> is the classic example. It runs as a small cluster (3–5 nodes) using consensus algorithms to maintain a fault-tolerant, real-time map of which shard lives on which IP address.</p> <p>Every database node holds an open TCP connection (heartbeat) to ZooKeeper and registers an <strong>ephemeral node</strong> in ZooKeeper’s memory. If a database node dies, its TCP connection drops, ZooKeeper deletes its ephemeral node, orchestrates a replica promotion, and immediately pushes the updated map to the routing tier — all within milliseconds. Clients are transparently rerouted with no manual intervention.</p> <hr/> <h2 id="12-secondary-indexes-in-a-sharded-database">12. Secondary Indexes in a Sharded Database</h2> <p>All of the above assumes you know the primary key. Real users search by secondary attributes (“red running shoes, size 10”). Secondary indexes on a sharded database force a hard architectural choice:</p> <h3 id="local-secondary-index-document-partitioning">Local Secondary Index (Document Partitioning)</h3> <p>Each shard maintains its own index covering only the data it holds.</p> <table> <thead> <tr> <th>Operation</th> <th>Performance</th> </tr> </thead> <tbody> <tr> <td><strong>Write</strong></td> <td>Blazing fast — update the local index with no network coordination</td> </tr> <tr> <td><strong>Read</strong></td> <td>Slow — must scatter-gather across every shard, suffering <strong>tail latency amplification</strong></td> </tr> </tbody> </table> <p><strong>Tail latency amplification math:</strong> if each node has a 1% chance of a slow response, querying 100 nodes gives a &gt;60% chance of hitting at least one slow node. Your query is as fast as its slowest component.</p> <h3 id="global-secondary-index-term-partitioning">Global Secondary Index (Term Partitioning)</h3> <p>The index itself is sharded by the indexed term. The global index for “red” lives on a single dedicated node, which holds pointers to every red item across the entire cluster.</p> <table> <thead> <tr> <th>Operation</th> <th>Performance</th> </tr> </thead> <tbody> <tr> <td><strong>Read</strong></td> <td>Fast — one targeted query to the index node returns all matching IDs</td> </tr> <tr> <td><strong>Write</strong></td> <td>Slow — updating one record may require modifying index entries on several shards, requiring a <strong>distributed transaction</strong> (2PC) for consistency</td> </tr> </tbody> </table> <p><strong>The fundamental trade-off:</strong></p> <ul> <li><strong>Local index:</strong> fast writes, slow scatter-gather reads</li> <li><strong>Global index:</strong> fast targeted reads, slow distributed-transaction writes</li> </ul> <p>The right choice depends on your application’s read/write ratio.</p> <hr/> <h2 id="13-summary">13. Summary</h2> <table> <thead> <tr> <th>Concept</th> <th>Core Idea</th> </tr> </thead> <tbody> <tr> <td><strong>Vertical scaling</strong></td> <td>Hits NUMA and cost limits; eventually fails</td> </tr> <tr> <td><strong>Key-range sharding</strong></td> <td>Sorted for range scans; beware write hotspots from monotonic keys</td> </tr> <tr> <td><strong>Hash sharding</strong></td> <td>Even distribution; destroys ordering</td> </tr> <tr> <td><strong>Consistent hashing</strong></td> <td>Fixed logical shards avoid migration storms on topology changes</td> </tr> <tr> <td><strong>Celebrity hotspot fix</strong></td> <td>Append random suffix to viral key; pay with scatter-gather reads</td> </tr> <tr> <td><strong>ZooKeeper</strong></td> <td>Fault-tolerant real-time directory for shard-to-node mapping</td> </tr> <tr> <td><strong>Local index</strong></td> <td>Fast writes, slow scatter-gather reads</td> </tr> <tr> <td><strong>Global index</strong></td> <td>Fast reads, slow distributed-transaction writes</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="distributed-systems"/><category term="sharding"/><category term="partitioning"/><category term="scalability"/><category term="databases"/><summary type="html"><![CDATA[Chapter 6 showed how to keep copies of data on multiple machines. Chapter 7 asks a different question: what do you do when your dataset is so large that no single machine can hold all of it? The answer is sharding — splitting the data into independent chunks and distributing those chunks across a fleet of nodes.]]></summary></entry><entry><title type="html">Part 8: Transactions</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-8-Transactions/" rel="alternate" type="text/html" title="Part 8: Transactions"/><published>2026-06-06T00:00:00+00:00</published><updated>2026-06-06T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%208%20-%20Transactions</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-8-Transactions/"><![CDATA[<p>At 2:00 a.m., a hospital system shows zero doctors on call. Each physician individually checked the rules and got approval to take sick leave. Nothing was coded incorrectly. The bug lived not in any single operation, but in the invisible gap <em>between</em> operations. Chapter 8 of DDIA is about the mechanisms databases use to close that gap.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 8 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-problem-production-is-chaos">1. The Problem: Production Is Chaos</h2> <p>A database in a data center faces a relentlessly hostile environment:</p> <ul> <li>Hard drives develop bad sectors; SSDs wear out.</li> <li>RAM experiences bit flips from cosmic rays.</li> <li>Network cables get cut.</li> <li>Top-of-rack switches reboot mid-query due to firmware bugs.</li> <li>The OS out-of-memory manager kills your process mid-transaction.</li> <li>10,000 concurrent clients race to write the same row simultaneously.</li> </ul> <p>Without help from the database, an application developer must write thousands of lines of error-handling code to safely update a single record. <strong>Transactions</strong> exist to hide this chaos behind a clean abstraction.</p> <hr/> <h2 id="2-the-transaction-abstraction">2. The Transaction Abstraction</h2> <p>A <strong>transaction</strong> groups a sequence of reads and writes into a single logical unit with two possible outcomes:</p> <ul> <li><strong>Commit</strong> — all operations succeed; all changes become visible and permanent.</li> <li><strong>Abort / Rollback</strong> — something went wrong; the database undoes everything as if the transaction never happened.</li> </ul> <p>The application developer doesn’t need to handle partial failures manually. They simply open a transaction, do their work, and call commit. The database handles the rest.</p> <h3 id="the-nosql-detour">The NoSQL Detour</h3> <p>During the late-2000s NoSQL boom, transaction support was deliberately dropped to achieve web-scale throughput. The assumption was that strong guarantees and global scale were mutually exclusive. Developers were left to implement their own consistency checks in application code — a fragile, error-prone approach.</p> <p>This assumption was disproven by <strong>NewSQL</strong> databases like Google Spanner, CockroachDB, and TiDB, which combine global sharding with consensus protocols to deliver strong transactional guarantees at massive scale.</p> <hr/> <h2 id="3-acid">3. ACID</h2> <p>The properties of transactions are captured by the acronym <strong>ACID</strong>, coined in 1983 by Haerder and Reuter. Despite its apparent precision, ACID is today partly a marketing term — different databases implement each letter differently.</p> <h3 id="a--atomicity">A — Atomicity</h3> <p>In ACID, atomicity is about <strong>abortability</strong>, not concurrency (despite the word’s usage in multithreaded programming, where it means something entirely different).</p> <p>If a transaction touches three tables and the server crashes after the second update, the database reverts the first two updates automatically.</p> <p><strong>Mechanism — the undo log (write-ahead log):</strong> Before modifying any real data file on disk, the database appends a note to an append-only log: <em>“I am about to change account A’s balance from $500 to $400.”</em> If the system crashes, the recovery manager reads the log, sees no commit marker, and uses the recorded old values to reverse every change.</p> <h3 id="c--consistency">C — Consistency</h3> <p>Consistency in ACID means the database is in a <em>good state</em> according to your application’s invariants — for example, in double-entry bookkeeping, debits and credits must always sum to zero.</p> <p><strong>Critical point:</strong> the database engine cannot enforce application-level business rules on its own. Foreign key constraints and uniqueness checks help, but defining the invariants and writing code that maintains them is the developer’s responsibility. ACID merely promises that if your transaction preserves consistency, the database will safely store the result.</p> <h3 id="i--isolation">I — Isolation</h3> <p>Multiple concurrent transactions should appear to run as if they are the only transactions on the system. This property is formally called <strong>serializability</strong>: the database guarantees that the final state of the data is identical to some valid serial (one-at-a-time) execution order.</p> <p>Isolation prevents races like two users simultaneously reading a counter as 42 and both writing 43, losing one increment.</p> <h3 id="d--durability">D — Durability</h3> <p>Once the database acknowledges a commit, the data is safe even if the server immediately loses power.</p> <p>In practice, durability is a <strong>spectrum of risk reduction</strong>, not an absolute guarantee:</p> <ul> <li>Writes are persisted to disk using <code class="language-plaintext highlighter-rouge">fsync</code> — but disk controllers lie, reporting success before data has left their own volatile cache.</li> <li>Data is replicated across multiple geographic regions.</li> <li>Regular backups are taken.</li> </ul> <p>Even battle-tested databases have had subtle <code class="language-plaintext highlighter-rouge">fsync</code> bugs. Modern SSDs can silently corrupt data after extended power loss as electrons leak from floating gate transistors. Perfect durability does not exist; the goal is reducing the probability of loss to negligible levels.</p> <hr/> <h2 id="4-single-object-vs-multi-object-operations">4. Single-Object vs Multi-Object Operations</h2> <p>A single-row update within a transaction is simple. The real complexity emerges when a logical operation spans multiple tables or rows — for example, delivering an email requires:</p> <ol> <li>Inserting the email body into the inbox table.</li> <li>Incrementing the unread-message counter.</li> <li>Adding an entry to the search index.</li> </ol> <p>Without multi-object atomicity, a crash between steps 1 and 2 leaves the system permanently inconsistent: the email exists but the counter is wrong. Without isolation, a reader between steps 1 and 2 sees the email but a counter of zero — a coherent-looking but logically wrong state.</p> <p>Pure key-value stores that prohibit multi-object transactions shift all this error handling to application code — a pattern that produces notoriously fragile systems.</p> <hr/> <h2 id="5-isolation-levels">5. Isolation Levels</h2> <p>Full serializability is expensive. Real databases offer a spectrum of <strong>weak isolation levels</strong> that trade safety guarantees for performance. Understanding which anomalies each level prevents is essential.</p> <h3 id="read-committed">Read Committed</h3> <p>The default isolation level in PostgreSQL, SQL Server, and many others. Provides two guarantees:</p> <table> <thead> <tr> <th>Guarantee</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td><strong>No dirty reads</strong></td> <td>You only see data that has been fully committed — never a half-finished transaction’s intermediate state</td> </tr> <tr> <td><strong>No dirty writes</strong></td> <td>If two transactions try to write the same row simultaneously, one waits until the other commits or aborts</td> </tr> </tbody> </table> <p><strong>What it does not prevent — read skew (non-repeatable reads):</strong> Aaliyah has $500 in account A and $500 in account B. A background job moves $100 from A to B. If Aaliyah reads account A ($500) before the transfer and account B ($600) after it, her app shows $1,100 — money appears from nowhere. Both reads returned fully committed data; the world just changed between them.</p> <h3 id="snapshot-isolation-repeatable-read">Snapshot Isolation (Repeatable Read)</h3> <p>Designed specifically to eliminate read skew. The database gives each transaction a <strong>consistent snapshot</strong> of the data at the moment the transaction started. No committed changes made by other transactions after that point are visible.</p> <p><strong>Implementation — MVCC (Multi-Version Concurrency Control):</strong></p> <p>PostgreSQL does not overwrite rows in place. An <code class="language-plaintext highlighter-rouge">UPDATE</code> is implemented as a logical delete of the old row and an insert of a new row. Both rows coexist on disk with hidden metadata:</p> <ul> <li><code class="language-plaintext highlighter-rouge">xmin</code> — the transaction ID that inserted this version of the row.</li> <li><code class="language-plaintext highlighter-rouge">xmax</code> — the transaction ID that deleted it (or null if still current).</li> </ul> <p>If your transaction has ID 12, and a row was modified by transaction 13, the database’s visibility rules hide the modification from you (13 &gt; 12, so it happened “in the future” relative to your snapshot). You see the pre-modification row as if it were still current.</p> <p><strong>The critical property of MVCC: readers never block writers, and writers never block readers.</strong> A long-running analytics query or backup can scan the entire database without holding locks, while concurrent writes proceed unimpeded.</p> <p>A background <strong>vacuum daemon</strong> periodically cleans up old row versions once it is certain no active transaction needs them.</p> <hr/> <h2 id="6-write-skew-and-phantoms">6. Write-Skew and Phantoms</h2> <p>Snapshot isolation eliminates read anomalies but does not protect against all write conflicts.</p> <h3 id="write-skew">Write-Skew</h3> <p>The hospital scenario from the introduction is <strong>write-skew</strong>: two transactions each read a shared condition, each decide it is safe to proceed, and each write to <em>different</em> rows — yet their combined effect violates a business invariant.</p> <p>The hospital invariant: at least one doctor must be on-call.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T1 (Dr. Aaliyah)          T2 (Dr. Bryce)
SELECT COUNT(*)           SELECT COUNT(*)
→ 2                       → 2 (same snapshot)
(2 ≥ 1, safe to leave)   (2 ≥ 1, safe to leave)
UPDATE → off-call         UPDATE → off-call
COMMIT                    COMMIT
→ 0 doctors on-call  ✗
</code></pre></div></div> <p>Neither transaction overwrote the other’s row. A <code class="language-plaintext highlighter-rouge">SELECT FOR UPDATE</code> on Aaliyah’s row would not help Bryce, because he is updating his own row.</p> <h3 id="phantoms">Phantoms</h3> <p>A phantom occurs when a write in one transaction changes the result of a <em>search query</em> in another. If you search for overlapping meeting room bookings before inserting your own, a concurrent insert from another transaction creates a new row that your search did not see — a “phantom” row that should have stopped your booking.</p> <p><strong>Materialising conflicts (last resort):</strong> Pre-populate a table with rows representing every possible time slot for every room, so the application has a concrete row to <code class="language-plaintext highlighter-rouge">SELECT FOR UPDATE</code> before checking availability. This works but pollutes the data model with artificial rows.</p> <hr/> <h2 id="7-serializable-isolation-three-approaches">7. Serializable Isolation: Three Approaches</h2> <p>Full serializability — the guarantee that the outcome is identical to some serial execution — is achievable three ways, each with distinct trade-offs.</p> <h3 id="71-actual-serial-execution">7.1 Actual Serial Execution</h3> <p>Literally run one transaction at a time on a single CPU core, using stored procedures so there is no round-trip latency. Used by Redis and VoltDB.</p> <p><strong>Works when:</strong> all active data fits in RAM and transactions are tiny and fast.</p> <p><strong>Bottleneck:</strong> scaling beyond one core requires partitioning, and cross-partition transactions stall while coordinating over the network.</p> <h3 id="72-two-phase-locking-2pl">7.2 Two-Phase Locking (2PL)</h3> <p>The classical approach, used for decades. Writers block readers; readers block writers.</p> <ul> <li>To <strong>read</strong> a row: acquire a <em>shared lock</em>. Many can hold it simultaneously.</li> <li>To <strong>write</strong> a row: acquire an <em>exclusive lock</em>. No one else can read or write while you hold it.</li> </ul> <p>2PL fully prevents write-skew: each transaction locks the rows it reads, so another transaction cannot modify those rows mid-execution.</p> <p><strong>Cost:</strong> heavy lock contention, reduced throughput, and <strong>deadlocks</strong> (Transaction A holds row 1 and waits for row 2; Transaction B holds row 2 and waits for row 1). The database must run background deadlock detection and forcefully abort one side, requiring a retry.</p> <h3 id="73-serializable-snapshot-isolation-ssi">7.3 Serializable Snapshot Isolation (SSI)</h3> <p>The modern approach, implemented in PostgreSQL and CockroachDB. It combines the MVCC snapshots of snapshot isolation with a monitoring layer that detects conflicts.</p> <p><strong>Key insight:</strong> instead of preventing conflicts via blocking locks, assume conflicts are rare and let transactions run freely. Track what each transaction has read, and abort only when a commit would produce a non-serializable outcome.</p> <p>Applied to the hospital scenario:</p> <ol> <li>Dr. Aaliyah’s transaction commits first. She is now off-call.</li> <li>Dr. Bryce’s transaction attempts to commit.</li> <li>SSI checks: “Did Bryce’s read of the on-call count become stale because another transaction modified it?” Yes — Aaliyah changed it.</li> <li>SSI aborts Bryce’s transaction. He retries, now sees count = 1, and his leave request is correctly rejected.</li> </ol> <p><strong>Result:</strong> readers don’t block writers; writers don’t block readers; full serializability is maintained. The overhead only appears on the rare transaction that actually conflicts.</p> <hr/> <h2 id="8-distributed-transactions">8. Distributed Transactions</h2> <p>All of the above describes single-node databases. When a transaction must span multiple shards or services — for example, deducting $100 from a New York shard and crediting $100 to a Tokyo shard — we need distributed atomicity.</p> <h3 id="two-phase-commit-2pc">Two-Phase Commit (2PC)</h3> <p>A <strong>coordinator</strong> node orchestrates a two-phase protocol:</p> <p><strong>Phase 1 — Prepare:</strong> Coordinator asks all participants: “Can you commit?” Each participant checks constraints, writes its undo log to disk, and replies yes or no. A yes is an irrevocable promise — the participant surrenders its right to abort unilaterally.</p> <p><strong>Phase 2 — Commit:</strong> If all say yes, coordinator broadcasts commit. If anyone said no, coordinator broadcasts abort.</p> <p><strong>The fatal flaw:</strong> if the coordinator crashes between Phase 1 and Phase 2, participants are left <strong>in doubt</strong> — they cannot abort (they promised) and they cannot commit (they haven’t received the order). They hold locks on database rows indefinitely. The entire system stalls until the coordinator recovers.</p> <p><strong>XA transactions</strong> extend 2PC across heterogeneous systems (e.g., PostgreSQL + Apache Kafka). The standardised lowest-common-denominator protocol performs poorly and cannot detect cross-system deadlocks.</p> <h3 id="consensus-backed-distributed-transactions">Consensus-Backed Distributed Transactions</h3> <p>NewSQL databases replace the single coordinator with a <strong>consensus cluster</strong> (e.g., Raft or Paxos). The coordinator’s state — the transaction intent log — is replicated across 3–5 nodes. A quorum must acknowledge each log entry before it is committed.</p> <p>If the leader coordinator dies, the surviving nodes detect the failure via missed heartbeats, elect a new leader in milliseconds, and resume the transaction from where the replicated log left off. There is no stuck <em>in-doubt</em> state because the log is durable across the cluster.</p> <p>This is the architectural foundation of Google Spanner and CockroachDB: strong, distributed ACID transactions that survive coordinator failures without stalling.</p> <hr/> <h2 id="9-summary">9. Summary</h2> <table> <thead> <tr> <th>Concept</th> <th>Core Idea</th> </tr> </thead> <tbody> <tr> <td><strong>Atomicity</strong></td> <td>All-or-nothing via undo log; abortability is the key property</td> </tr> <tr> <td><strong>Consistency</strong></td> <td>Application’s responsibility; database enforces constraints, not business rules</td> </tr> <tr> <td><strong>Durability</strong></td> <td>Spectrum of risk reduction; <code class="language-plaintext highlighter-rouge">fsync</code>, replication, backups</td> </tr> <tr> <td><strong>Isolation</strong></td> <td>Concurrency safety; serializability is the gold standard</td> </tr> <tr> <td><strong>Read Committed</strong></td> <td>Prevents dirty reads and dirty writes; allows read skew</td> </tr> <tr> <td><strong>Snapshot Isolation / MVCC</strong></td> <td>Consistent snapshot via xmin/xmax; readers never block writers</td> </tr> <tr> <td><strong>Write-Skew</strong></td> <td>Concurrent transactions each check a shared condition, then write to different rows, breaking an invariant</td> </tr> <tr> <td><strong>SSI</strong></td> <td>Optimistic serializability; abort only on actual conflict</td> </tr> <tr> <td><strong>2PC</strong></td> <td>Distributed atomicity; coordinator is a single point of failure</td> </tr> <tr> <td><strong>Raft/Paxos in NewSQL</strong></td> <td>Replicated coordinator eliminates the 2PC crash risk</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="distributed-systems"/><category term="transactions"/><category term="acid"/><category term="concurrency"/><category term="databases"/><summary type="html"><![CDATA[At 2:00 a.m., a hospital system shows zero doctors on call. Each physician individually checked the rules and got approval to take sick leave. Nothing was coded incorrectly. The bug lived not in any single operation, but in the invisible gap between operations. Chapter 8 of DDIA is about the mechanisms databases use to close that gap.]]></summary></entry><entry><title type="html">Part 1: Trade-Offs in Data Systems Architecture</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-1-Trade-Offs-in-Data-Systems-Architecture/" rel="alternate" type="text/html" title="Part 1: Trade-Offs in Data Systems Architecture"/><published>2026-06-04T00:00:00+00:00</published><updated>2026-06-04T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%201%20-%20Trade-Offs%20in%20Data%20Systems%20Architecture</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-1-Trade-Offs-in-Data-Systems-Architecture/"><![CDATA[<p>The very first lesson of serious systems engineering is that there is no perfect architecture. As the economist Thomas Sowell put it: <em>“There are no solutions, there are only trade-offs.”</em> Every technology choice — from the database you pick to how you deploy your service — involves accepting costs in exchange for benefits. Chapter 1 of DDIA (2nd ed.) frames the entire book around this principle.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 1 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-two-audiences-for-data">1. The Two Audiences for Data</h2> <p>Modern systems serve two fundamentally different classes of users, each with radically different access patterns:</p> <table> <thead> <tr> <th>Audience</th> <th>Goal</th> <th>Pattern</th> </tr> </thead> <tbody> <tr> <td><strong>End users</strong></td> <td>Interact with the live application</td> <td>High-frequency, low-latency point lookups</td> </tr> <tr> <td><strong>Analysts / scientists</strong></td> <td>Understand trends in the business</td> <td>Rare, expensive scans over large datasets</td> </tr> </tbody> </table> <p>Trying to serve both audiences with the same database is one of the most common early-stage performance mistakes.</p> <hr/> <h2 id="2-oltp-vs-olap">2. OLTP vs OLAP</h2> <h3 id="online-transaction-processing-oltp">Online Transaction Processing (OLTP)</h3> <p>OLTP databases power customer-facing applications. Think of PostgreSQL backing a web shop: every login, cart update, and purchase is a small, targeted operation against a handful of rows.</p> <p><strong>Under the hood:</strong></p> <ul> <li>Data is laid out <strong>row by row</strong> on disk (row-oriented storage).</li> <li>Queries navigate directly to the relevant row via a <strong>B-tree index</strong> — the database never has to scan the whole table.</li> <li>Optimised for <strong>low-latency point reads and writes</strong> (single-digit milliseconds).</li> </ul> <h3 id="online-analytical-processing-olap">Online Analytical Processing (OLAP)</h3> <p>Business analysts and data scientists ask questions like <em>“What was revenue per region in Q4 across all 50 stores?”</em> — queries that scan millions of rows but only read a handful of columns.</p> <p>Running such a query against a live OLTP database would pin the CPU at 100% and freeze the application for every real user. The solution is a <strong>separate, read-only data warehouse</strong>.</p> <p><strong>Under the hood:</strong></p> <ul> <li>Data is loaded via an nightly <strong>ETL</strong> (Extract–Transform–Load) process.</li> <li>The warehouse uses <strong>columnar storage</strong>: all values for one column (e.g., all prices) are stored together on disk.</li> <li>When the analyst asks for the average price, the database reads only the price column into the CPU — never the name, address, or email of each customer.</li> <li>Combined with SIMD vector processing, columnar layout can aggregate billions of rows in seconds.</li> </ul> <pre><code class="language-mermaid">graph LR
    A[Live App] --&gt;|point queries| B[(OLTP DB)]
    B --&gt;|nightly ETL| C[(Data Warehouse)]
    C --&gt;|analytical queries| D[Analysts / BI]

    classDef default fill:#1a1a1a,stroke:#888,stroke-width:2px,color:#fff;
    classDef highlight fill:#2196F3,stroke:#fff,stroke-width:2px,color:#fff;
    class A,D highlight;
</code></pre> <hr/> <h2 id="3-data-lakes-the-schema-on-read-revolution">3. Data Lakes: The Schema-on-Read Revolution</h2> <p>A data warehouse works beautifully for questions you already know how to ask. But machine learning requires raw, messy data — unstructured JSON logs, audio files, IoT sensor readings — that doesn’t fit a rigid SQL schema.</p> <table> <thead> <tr> <th> </th> <th>Data Warehouse</th> <th>Data Lake</th> </tr> </thead> <tbody> <tr> <td><strong>Schema enforcement</strong></td> <td>Schema-on-write (rejected at ingest)</td> <td>Schema-on-read (interpreted at query time)</td> </tr> <tr> <td><strong>Typical storage</strong></td> <td>Columnar files (Parquet, ORC)</td> <td>Raw files on object storage (S3, GCS)</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Known analytical queries</td> <td>Exploratory ML, NLP, feature engineering</td> </tr> </tbody> </table> <p>The practical rule: a data warehouse answers questions you already understand; a data lake holds raw material for questions you haven’t thought of yet. Both often coexist in the same organisation.</p> <hr/> <h2 id="4-systems-of-record-vs-derived-data">4. Systems of Record vs. Derived Data</h2> <p>Every non-trivial system separates these two roles:</p> <ul> <li><strong>System of record</strong> — the authoritative, permanent source of truth (e.g., the primary PostgreSQL database). Optimised for safe, consistent writes.</li> <li><strong>Derived data system</strong> — a redundant copy optimised for a specific access pattern (e.g., a Redis cache, a search index, a materialised view). It can always be rebuilt from the source of record.</li> </ul> <p>The hardest problem this introduces is <strong>cache invalidation</strong>: ensuring derived copies are updated when the source of truth changes. This is a fundamental tension that appears throughout the book.</p> <hr/> <h2 id="5-cloud-vs-self-hosting">5. Cloud vs. Self-Hosting</h2> <h3 id="the-self-hosting-problem-capacity-planning">The Self-Hosting Problem: Capacity Planning</h3> <p>Owning bare-metal servers forces a brutal binary choice:</p> <ul> <li><strong>Under-provision</strong> → your app crashes on a viral traffic spike.</li> <li><strong>Over-provision</strong> → you pay for idle hardware 99% of the time.</li> </ul> <h3 id="cloud-elasticity">Cloud Elasticity</h3> <p>Cloud providers virtualise hardware and let you rent compute by the second. If traffic spikes, you spin up 100 extra VMs in milliseconds (<strong>horizontal scaling</strong>). When traffic drops, you terminate them and stop paying.</p> <p>The modern operations role (SRE / DevOps) shifted from physically racking servers to writing <strong>infrastructure-as-code</strong> (Terraform, Pulumi) and managing cloud spend (<strong>FinOps</strong>).</p> <h3 id="separation-of-storage-and-compute">Separation of Storage and Compute</h3> <p>The cloud’s fast intra-datacenter networks enable a powerful pattern: <strong>disaggregation</strong>. Systems like Snowflake store data cheaply in S3 and spin up separate, ephemeral compute clusters only when a query runs. A 10 TB dataset costs pennies per month to store; you only pay for expensive CPUs the five minutes per month you actually need them.</p> <hr/> <h2 id="6-monoliths-vs-microservices">6. Monoliths vs. Microservices</h2> <h3 id="the-monolith-problem">The Monolith Problem</h3> <p>A monolithic application bundles every feature — auth, payments, inventory, email — into a single codebase and process. One memory leak in the image upload service brings down the checkout and homepage alike. Deploying a CSS fix requires coordinating every team and re-deploying the entire application.</p> <h3 id="microservices">Microservices</h3> <p>Microservices decompose the monolith into small, independently deployable processes. The critical rule: <strong>each service owns its own database</strong>. Services communicate only through well-defined APIs (REST or gRPC). A failure in one service is contained — other services keep running.</p> <p>The trade-off is substantial: you replace in-process function calls (nanoseconds, 100% reliable) with network calls (milliseconds, fail constantly). You also take on enormous operational complexity — distributed tracing, retry logic, service discovery, and network partitions.</p> <p><strong>The rule of thumb:</strong> microservices pay off when your organisation has enough engineers that the human coordination cost of sharing one codebase exceeds the engineering cost of managing the distributed system.</p> <hr/> <h2 id="7-serverless-function-as-a-service">7. Serverless (Function-as-a-Service)</h2> <p>Serverless takes the pay-as-you-go model to its logical extreme. You deploy individual functions (AWS Lambda, Google Cloud Functions); the provider handles the infrastructure entirely. You pay only for the milliseconds your code actually executes.</p> <table> <thead> <tr> <th>Advantage</th> <th>Trade-off</th> </tr> </thead> <tbody> <tr> <td>Zero cost at zero traffic</td> <td><strong>Cold start latency</strong> (~100ms–1s on first invocation)</td> </tr> <tr> <td>Scales to millions of requests automatically</td> <td><strong>Stateless</strong> — no memory between requests</td> </tr> <tr> <td>No OS patching or server management</td> <td>Requires event-driven, asynchronous architecture</td> </tr> </tbody> </table> <hr/> <h2 id="8-data-privacy-as-an-architectural-constraint">8. Data Privacy as an Architectural Constraint</h2> <p>The chapter closes with a dimension that many engineers treat as an afterthought: privacy law. GDPR (Europe), CCPA (California), and PCI-DSS (payment cards) impose hard engineering requirements.</p> <p>The collision point is <strong>append-only immutable logs</strong> — a widely used pattern (Kafka, event sourcing) where deleting past records is either impossible or structurally corrupting.</p> <p>Two approaches have emerged:</p> <ol> <li><strong>Data minimisation</strong> — only collect what is strictly needed. If you don’t have the data, it can’t be breached, leaked, or subpoenaed.</li> <li><strong>Crypto-shredding</strong> — encrypt each user’s personal payload with a unique key. When they invoke the right to be forgotten, destroy the key. The bytes remain in the log but are permanently unreadable.</li> </ol> <p>The deeper lesson: privacy laws are engineering constraints just like network latency or disk IOPS. Treating them as legal afterthoughts leads to architectures that literally cannot comply with the law.</p> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li>There is no universally best architecture — every choice trades one set of costs for another.</li> <li>The OLTP/OLAP split exists because the physical access patterns of transactional and analytical workloads are fundamentally incompatible.</li> <li>Cloud elasticity shifts hardware from a capital cost to an operational cost and enables storage/compute disaggregation.</li> <li>Microservices solve a human coordination problem but create a distributed systems engineering problem.</li> <li>Privacy requirements must be designed in from day one, not retrofitted.</li> </ul> <p><em>Next: Chapter 2 — Defining Non-Functional Requirements (scalability, reliability, maintainability).</em></p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="data-systems"/><category term="oltp"/><category term="olap"/><category term="cloud"/><category term="distributed-systems"/><summary type="html"><![CDATA[The very first lesson of serious systems engineering is that there is no perfect architecture. As the economist Thomas Sowell put it: “There are no solutions, there are only trade-offs.” Every technology choice — from the database you pick to how you deploy your service — involves accepting costs in exchange for benefits. Chapter 1 of DDIA (2nd ed.) frames the entire book around this principle.]]></summary></entry><entry><title type="html">Part 2: Defining Non-Functional Requirements</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-2-Defining-Nonfunctional-Requirements/" rel="alternate" type="text/html" title="Part 2: Defining Non-Functional Requirements"/><published>2026-06-04T00:00:00+00:00</published><updated>2026-06-04T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%202%20-%20Defining%20Nonfunctional%20Requirements</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-2-Defining-Nonfunctional-Requirements/"><![CDATA[<p>Functional requirements describe <em>what</em> a system does. Non-functional requirements describe <em>how well</em> it does it under real-world conditions. Chapter 2 of DDIA makes clear that this “how well” dimension is often the difference between a system that survives production and one that collapses under its own weight.</p> <p>The three pillars are: <strong>scalability</strong>, <strong>reliability</strong>, and <strong>maintainability</strong>.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 2 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-scalability-handling-growth">1. Scalability: Handling Growth</h2> <p>Scalability is not a binary property you can add after the fact. It must be designed in, and it looks different depending on which dimension is growing.</p> <h3 id="the-twitter--x-timeline-case-study">The Twitter / X Timeline Case Study</h3> <p>The home timeline is a deceptively simple requirement: “show me the most recent posts from the 200 people I follow.” Two strategies:</p> <p><strong>Strategy 1 — Pull on read (normalised)</strong></p> <p>When a user requests their timeline, execute a SQL join:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="n">tweets</span><span class="p">.</span><span class="o">*</span> <span class="k">FROM</span> <span class="n">tweets</span>
<span class="k">JOIN</span> <span class="n">follows</span> <span class="k">ON</span> <span class="n">follows</span><span class="p">.</span><span class="n">followee_id</span> <span class="o">=</span> <span class="n">tweets</span><span class="p">.</span><span class="n">user_id</span>
<span class="k">WHERE</span> <span class="n">follows</span><span class="p">.</span><span class="n">follower_id</span> <span class="o">=</span> <span class="p">:</span><span class="n">user_id</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">tweets</span><span class="p">.</span><span class="n">created_at</span> <span class="k">DESC</span> <span class="k">LIMIT</span> <span class="mi">20</span><span class="p">;</span>
</code></pre></div></div> <p>This is elegant but catastrophically slow at scale. With 10 million concurrent users refreshing every 5 seconds, you need to run this expensive multi-table join <strong>~2 million times per second</strong>. The database CPU pins at 100% and the system falls over.</p> <p><strong>Strategy 2 — Fan-out on write (materialised)</strong></p> <p>When a user posts a tweet, immediately write that tweet into a pre-computed <strong>timeline cache</strong> for every follower. Reading the timeline becomes a cheap, sequential key-value lookup.</p> <p><strong>The fan-out math:</strong></p> \[\text{cache writes/sec} = \text{tweets/sec} \times \text{avg\_followers} = 5{,}800 \times 200 = 1{,}160{,}000 \text{ writes/sec}\] <p>That’s tractable with Redis — in-memory writes are cheap.</p> <p><strong>The celebrity problem:</strong> If a user has 100 million followers, a single tweet triggers 100 million cache writes in seconds. The fan-out queue overflows and the system crashes.</p> <p><strong>The hybrid solution:</strong> Most users get fan-out on write. Celebrities are exempt — their tweets are fetched live on read and merged in memory at request time. One system uses two strategies simultaneously, choosing per-user based on follower count.</p> <pre><code class="language-mermaid">graph TD
    Tweet[New Tweet Posted]
    Tweet --&gt; Check{Celebrity?}
    Check --&gt;|No: &lt; 1M followers| FanOut[Fan-out to follower caches]
    Check --&gt;|Yes: &gt;= 1M followers| SpecialDB[Write to celebrity tweet store]
    FanOut --&gt; TimelineCache[(Timeline Cache)]
    SpecialDB --&gt; ReadMerge[Merge at read time]
    TimelineCache --&gt; ReadMerge
    ReadMerge --&gt; UserFeed[User's home feed]

    classDef default fill:#1a1a1a,stroke:#888,stroke-width:2px,color:#fff;
    classDef highlight fill:#2196F3,stroke:#fff,stroke-width:2px,color:#fff;
    class Tweet,UserFeed highlight;
</code></pre> <hr/> <h2 id="2-performance-metrics-why-averages-lie">2. Performance Metrics: Why Averages Lie</h2> <h3 id="throughput-vs-response-time">Throughput vs. Response Time</h3> <ul> <li><strong>Throughput</strong>: volume of work per unit time (requests/sec, MB/sec). Analogous to the diameter of a pipe.</li> <li><strong>Response time</strong>: elapsed time from sending a request to receiving the full response. Includes network latency, queuing delays, and server processing.</li> </ul> <h3 id="the-problem-with-averages">The Problem with Averages</h3> <p>Suppose 99 requests complete in 1 ms and 1 request takes 3 hours. The arithmetic mean is ~1.8 minutes — technically accurate, but it tells you nothing useful about the experience of either group.</p> <p><strong>The fix: percentiles.</strong></p> <p>Line up all response times from fastest to slowest:</p> <table> <thead> <tr> <th>Percentile</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td><strong>P50</strong> (median)</td> <td>Half of requests are faster than this</td> </tr> <tr> <td><strong>P95</strong></td> <td>95% of requests are faster than this</td> </tr> <tr> <td><strong>P99</strong></td> <td>99% of requests are faster than this</td> </tr> <tr> <td><strong>P99.9</strong></td> <td>999 out of 1000 requests are faster than this</td> </tr> </tbody> </table> <p>Amazon optimises for P99.9 because the slowest 0.1% tend to be customers with the largest order histories — their most valuable, most frequent buyers. If you only optimise the median, you systematically penalise your best customers.</p> <blockquote> <p><strong>Rule of thumb:</strong> Set SLOs (Service Level Objectives) in percentiles, never averages. Typical targets: P99 &lt; 200ms, P99.9 &lt; 1s.</p> </blockquote> <h3 id="tail-latency-amplification">Tail Latency Amplification</h3> <p>In a microservices architecture, a single user-facing request fans out to dozens of backend services. The final response cannot be sent until <em>all</em> of them reply — the slowest one dictates the overall latency.</p> <p>If each of 100 backend services has a 1% chance of hitting its slow path:</p> \[P(\text{all fast}) = (1 - 0.01)^{100} \approx 0.366\] <p>So <strong>63.4% of user requests</strong> will be delayed by at least one slow service, even when each individual service is “slow” only 1% of the time. This is tail latency amplification.</p> <hr/> <h2 id="3-reliability">3. Reliability</h2> <h3 id="fault-vs-failure">Fault vs. Failure</h3> <ul> <li>A <strong>fault</strong> is a localised component misbehaving (a disk dying, a node crashing).</li> <li>A <strong>failure</strong> is when the whole system becomes unavailable to users.</li> </ul> <p>Reliable systems are engineered to tolerate faults without cascading into failures.</p> <h3 id="hardware-faults">Hardware Faults</h3> <ul> <li>Magnetic hard drives fail at 2–5% per year. A cluster of 10,000 drives replaces one drive every single day.</li> <li>SSDs have uncorrectable bit errors.</li> <li><strong>Cosmic rays</strong> — high-energy particles from stellar events — can flip a DRAM bit mid-computation (a “single-event upset”). ECC memory catches most of these.</li> </ul> <p>Hardware faults are <strong>random and independent</strong> — they strike different machines at different times. Redundancy (RAID, dual power supplies, geographic replication) works well here.</p> <h3 id="software-faults-are-different">Software Faults Are Different</h3> <p>Software bugs are <strong>correlated</strong>: deploy the same bug to 10,000 servers and you’ve now deployed it 10,000 times simultaneously. A triggering condition wipes out every redundant copy at once.</p> <p>Famous examples:</p> <ul> <li><strong>2012 leap second bug</strong>: a Linux kernel flaw caused a large fraction of the internet to lock up the instant the extra second was added to the global clock.</li> <li><strong>SSD firmware bug</strong>: drives would permanently brick themselves after exactly 32,768 hours of uptime. Data centres that bought drives in batches and powered them on together lost all their “redundant” drives in a single hour, three years later.</li> </ul> <h3 id="chaos-engineering">Chaos Engineering</h3> <p>The practice of intentionally injecting faults in production — killing random servers, severing network links, corrupting data — to verify that redundancy actually works under live conditions. Netflix’s Chaos Monkey is the canonical example. The only way to be confident your spares work is to prove it while the car is moving at 70 mph.</p> <h3 id="human-error-and-blameless-culture">Human Error and Blameless Culture</h3> <p>The majority of production incidents are ultimately caused by human operators — misconfiguration, bad deployment, accidental data deletion. The correct response is never to blame the individual. Instead, conduct <strong>blameless postmortems</strong> that ask: <em>why did the interface make this mistake easy? Why didn’t the tests catch it? How do we change the system so this class of error is harder to make?</em></p> <p>The stakes are real. The UK Post Office Horizon scandal (1999–2019) saw hundreds of innocent branch managers prosecuted for theft and fraud because courts treated buggy accounting software as infallible truth. Software failures are not abstract — they ruin lives.</p> <hr/> <h2 id="4-scalability-strategies">4. Scalability Strategies</h2> <h3 id="vertical-scaling-scale-up">Vertical Scaling (Scale Up)</h3> <p>Buy a bigger machine: more CPUs, more RAM, faster disks. Simple to operate — your existing software runs unchanged. But there are hard physical limits, and a single machine is a single point of failure.</p> <h3 id="horizontal-scaling--shared-nothing-architecture-scale-out">Horizontal Scaling / Shared-Nothing Architecture (Scale Out)</h3> <p>Distribute load across many cheap commodity machines. No machine shares state with another; failures are isolated. If one node dies, the others continue.</p> <p>The engineering complexity is substantial: data must be partitioned (sharded), consistency across nodes requires careful design, and debugging distributed systems is notoriously difficult. But horizontal scaling is the only path to true fault tolerance and theoretically unbounded capacity.</p> <hr/> <h2 id="5-maintainability">5. Maintainability</h2> <p>The majority of a software system’s lifetime cost is not building version 1.0. It’s the years of maintenance that follow: fixing bugs, adapting to new requirements, onboarding new engineers who didn’t write the original code.</p> <p>The key to maintainable systems is <strong>managing complexity</strong>:</p> <ul> <li><strong>Operability</strong>: make the system easy to run, monitor, and debug. Good metrics, clear runbooks, graceful degradation.</li> <li><strong>Simplicity</strong>: ruthlessly eliminate accidental complexity. Abstractions (SQL hiding B-tree internals, Kubernetes hiding VM management) let future engineers work at the right level of detail without drowning in irrelevant mechanics.</li> <li><strong>Evolvability</strong>: design for change. Loose coupling, well-defined interfaces, and comprehensive tests make it possible to modify one part of the system without fear of breaking another.</li> </ul> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li>Average response time is a dangerous metric that hides the worst user experiences. Track percentiles — especially P95 and P99.</li> <li>Tail latency amplification means even low per-service slow-path probabilities produce frequent slow end-to-end requests.</li> <li>Faults are inevitable; design systems to contain them rather than prevent them entirely.</li> <li>Hardware faults are random and independent; software bugs are correlated — redundancy helps with the former but not the latter.</li> <li>Horizontal scaling is the only path to true elastic capacity and fault isolation, at the cost of distributed-systems complexity.</li> </ul> <p><em>Next: Chapter 3 — Data Models and Query Languages.</em></p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="scalability"/><category term="reliability"/><category term="maintainability"/><category term="performance"/><summary type="html"><![CDATA[Functional requirements describe what a system does. Non-functional requirements describe how well it does it under real-world conditions. Chapter 2 of DDIA makes clear that this “how well” dimension is often the difference between a system that survives production and one that collapses under its own weight.]]></summary></entry><entry><title type="html">Part 3: Data Models and Query Languages</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-3-Data-Models-and-Query-Languages/" rel="alternate" type="text/html" title="Part 3: Data Models and Query Languages"/><published>2026-06-04T00:00:00+00:00</published><updated>2026-06-04T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%203%20-%20Data%20Models%20and%20Query%20Languages</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-3-Data-Models-and-Query-Languages/"><![CDATA[<p>Ludwig Wittgenstein wrote: <em>“The limits of my language mean the limits of my world.”</em> In software, the data model you choose to represent your domain defines the questions you can even ask about it — and the queries you can run efficiently. Choose wrong, and performance limits become architectural limits.</p> <p>Chapter 3 of DDIA surveys the major data modelling paradigms, the query languages they inspired, and the trade-offs that determine which fits your problem.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 3 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-layers-of-abstraction">1. Layers of Abstraction</h2> <p>Every system stacks four abstraction layers:</p> <ol> <li><strong>Application code</strong> — real-world concepts modelled as objects, structs, and APIs.</li> <li><strong>General-purpose data model</strong> — tables, JSON documents, graph vertices. This is where the critical architectural decisions live.</li> <li><strong>Storage engine</strong> — how the data model is represented as bytes on disk.</li> <li><strong>Hardware</strong> — how bytes map to electrical signals on silicon.</li> </ol> <p>The lower layers hide their complexity from the upper ones. You don’t need to understand NAND flash physics to write a SQL query — but you absolutely must understand layer 2 to design a system that won’t collapse at scale.</p> <hr/> <h2 id="2-declarative-vs-imperative-query-languages">2. Declarative vs. Imperative Query Languages</h2> <h3 id="imperative-tell-the-machine-how">Imperative: tell the machine <em>how</em></h3> <p>In an imperative approach (C, Python loops, early ORM defaults) you specify exact steps: <em>scan this index, iterate these rows, filter these columns</em>. You are micromanaging the CPU.</p> <h3 id="declarative-tell-the-machine-what">Declarative: tell the machine <em>what</em></h3> <p>SQL is declarative. You specify the goal — the shape of the result you want — and surrender the execution strategy to the <strong>query optimizer</strong>:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="n">user_id</span><span class="p">,</span> <span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">as</span> <span class="n">order_count</span>
<span class="k">FROM</span> <span class="n">orders</span>
<span class="k">WHERE</span> <span class="n">region</span> <span class="o">=</span> <span class="s1">'EU'</span>
<span class="k">GROUP</span> <span class="k">BY</span> <span class="n">user_id</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">order_count</span> <span class="k">DESC</span>
<span class="k">LIMIT</span> <span class="mi">10</span><span class="p">;</span>
</code></pre></div></div> <p>The optimizer decides which indexes to use, whether to do a hash join or a nested-loop join, whether to parallelize across 16 CPU cores. You get that optimization for free, and every future version of the database engine that improves the optimizer makes your query faster without any code change.</p> <hr/> <h2 id="3-the-document-model">3. The Document Model</h2> <p>If your data forms a natural <strong>one-to-many tree</strong> — a user with many past jobs, a blog post with many comments, an order with many line items — the document model is a natural fit.</p> <p>Instead of spreading the data across multiple joined tables, you store everything in a single self-contained JSON/BSON document. This is <strong>data locality</strong>: all the bytes you need live together on disk. One disk read retrieves the entire object.</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"user_id"</span><span class="p">:</span><span class="w"> </span><span class="mi">42</span><span class="p">,</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Alice"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"positions"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Engineer"</span><span class="p">,</span><span class="w"> </span><span class="nl">"company"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Acme"</span><span class="p">,</span><span class="w"> </span><span class="nl">"years"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2019-2022"</span><span class="w"> </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Senior Engineer"</span><span class="p">,</span><span class="w"> </span><span class="nl">"company"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Beta Corp"</span><span class="p">,</span><span class="w"> </span><span class="nl">"years"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2022-"</span><span class="w"> </span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"education"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="nl">"degree"</span><span class="p">:</span><span class="w"> </span><span class="s2">"BSc Computer Science"</span><span class="p">,</span><span class="w"> </span><span class="nl">"school"</span><span class="p">:</span><span class="w"> </span><span class="s2">"MIT"</span><span class="p">,</span><span class="w"> </span><span class="nl">"year"</span><span class="p">:</span><span class="w"> </span><span class="mi">2019</span><span class="w"> </span><span class="p">}]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>Benefits:</strong></p> <ul> <li>No joins required for reading a complete entity.</li> <li>Schema flexibility — different documents can have different fields (schema-on-read).</li> </ul> <p><strong>Limitations:</strong></p> <ul> <li>If you only need <code class="language-plaintext highlighter-rouge">current_city</code> from a 5 MB document, the whole 5 MB must be loaded into memory.</li> <li>Many document stores require rewriting the <strong>entire document</strong> on update, causing write amplification.</li> <li>Poor support for many-to-many relationships.</li> </ul> <h3 id="the-impedance-mismatch">The Impedance Mismatch</h3> <p>Object-oriented application code models data as objects; relational databases model data as flat tables. Bridging the two requires an <strong>ORM</strong> (Active Record, Hibernate, SQLAlchemy). ORMs are convenient for simple CRUD but hide a deadly trap.</p> <p><strong>The N+1 query problem:</strong> Suppose you fetch 50 students in a class, then iterate over them in Python to get each student’s name. The ORM’s default lazy-loading strategy executes one query to fetch the 50 IDs, then makes <em>50 more separate database round-trips</em> — one per student. On a remote database, the network latency alone makes the page take 10 seconds to load.</p> <p>The fix is <strong>eager loading</strong>: tell the ORM to perform a single JOIN up-front and load all data in one trip. But to do this correctly, you need to understand the SQL the ORM generates — the abstraction is leaky.</p> <hr/> <h2 id="4-normalisation-vs-denormalisation">4. Normalisation vs. Denormalisation</h2> <h3 id="why-normalise">Why Normalise?</h3> <p>Storing the city name “Washington, D.C.” as a foreign key integer (e.g., <code class="language-plaintext highlighter-rouge">region_id = 91</code>) instead of a raw string provides:</p> <ol> <li><strong>Consistency</strong>: all 10 million users in that city reference the same row, so there are no spelling variants (“DC”, “D.C.”, “Wash. D.C.”).</li> <li><strong>Cheap updates</strong>: if the city is renamed, update one row; all 10 million user records reflect the change instantly.</li> </ol> <p>Normalisation rule: meaningful human-readable data lives exactly once; everything else is a foreign key.</p> <h3 id="why-denormalise">Why Denormalise?</h3> <p>Joins are expensive. If millions of users request their profiles every second and each profile requires joining 5 tables, the CPU overhead becomes a bottleneck.</p> <p>Denormalisation duplicates data to pre-compute joins, making reads instant at the cost of write complexity. The application code is now responsible for keeping all copies consistent.</p> <p><strong>Twitter’s hybrid strategy:</strong> The materialised timeline cache stores only tweet IDs (the structural layout). The live like counts and profile pictures are fetched and merged in the application layer at request time. This normalises the volatile, frequently updated data while denormalising the structural outline.</p> <hr/> <h2 id="5-the-star-schema-and-data-warehouses">5. The Star Schema and Data Warehouses</h2> <p>For analytical workloads (OLAP), the dominant model is the <strong>star schema</strong>:</p> <ul> <li><strong>Fact table</strong> (centre): one row per event — a sale, a page view, a transaction. Contains foreign keys and numeric measures.</li> <li><strong>Dimension tables</strong> (arms): the who, what, where, when, why — products, stores, dates, customers.</li> </ul> <pre><code class="language-mermaid">graph TD
    Fact[Fact: Sales\n sale_id, date_key, product_key, store_key, amount]
    DimDate[Dim: Date\n date_key, day, month, year, is_holiday]
    DimProduct[Dim: Product\n product_key, name, category, brand]
    DimStore[Dim: Store\n store_key, city, region, sq_ft]

    Fact --&gt; DimDate
    Fact --&gt; DimProduct
    Fact --&gt; DimStore

    classDef default fill:#1a1a1a,stroke:#888,stroke-width:2px,color:#fff;
    classDef fact fill:#2196F3,stroke:#fff,stroke-width:2px,color:#fff;
    class Fact fact;
</code></pre> <p>The <strong>One Big Table (OBT)</strong> pattern takes denormalisation to the extreme: fold all dimension data directly into the fact table during ETL. Zero joins at query time. The massive write penalty (duplication) doesn’t matter in a warehouse because analytical data is largely append-only and historical — no one is updating last Tuesday’s sales record.</p> <hr/> <h2 id="6-schema-on-write-vs-schema-on-read">6. Schema-on-Write vs. Schema-on-Read</h2> <table> <thead> <tr> <th> </th> <th>Schema-on-Write (Relational)</th> <th>Schema-on-Read (Document)</th> </tr> </thead> <tbody> <tr> <td><strong>Enforcement</strong></td> <td>Database rejects non-conforming writes</td> <td>Application code interprets on read</td> </tr> <tr> <td><strong>Migration</strong></td> <td>ALTER TABLE — expensive on large tables</td> <td>Deploy new code; handle old/new format in app logic</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Homogeneous, well-understood data</td> <td>Heterogeneous or rapidly evolving data</td> </tr> <tr> <td><strong>Analogy</strong></td> <td>Static typing (compiler checks)</td> <td>Dynamic typing (runtime checks)</td> </tr> </tbody> </table> <hr/> <h2 id="7-graph-data-models">7. Graph Data Models</h2> <p>When your data has <strong>high-density many-to-many relationships</strong> — social networks, knowledge graphs, routing networks, fraud rings — relational tables become unwieldy. Finding “all paths of length ≤ 4 between Alice and Bob” requires a recursive SQL query of agonising complexity.</p> <p>A <strong>property graph</strong> (used by Neo4j, Amazon Neptune) represents:</p> <ul> <li><strong>Vertices (nodes)</strong>: entities (people, places, products).</li> <li><strong>Edges</strong>: relationships between entities, carrying their own properties (e.g., <code class="language-plaintext highlighter-rouge">{since: 2019, type: "follows"}</code>).</li> </ul> <p>The key optimisation is <strong>index-free adjacency</strong>: each vertex stores direct memory pointers to its neighbouring vertices. Traversing a relationship means following a pointer — O(1) — not scanning a foreign-key index.</p> <p><strong>Cypher query</strong> (Neo4j) — find all people who emigrated from the US to Europe:</p> <div class="language-cypher highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">MATCH</span><span class="w"> </span><span class="ss">(</span><span class="py">person:</span><span class="n">Person</span><span class="ss">)</span><span class="o">-</span><span class="ss">[</span><span class="nc">:BORN_IN</span><span class="ss">]</span><span class="o">-&gt;</span><span class="ss">(</span><span class="n">birthplace</span><span class="ss">)</span><span class="o">-</span><span class="ss">[</span><span class="nc">:WITHIN</span><span class="o">*</span><span class="mf">0.</span><span class="n">.</span><span class="ss">]</span><span class="o">-&gt;</span><span class="ss">(</span><span class="py">us:</span><span class="n">Country</span> <span class="ss">{</span><span class="py">name:</span> <span class="s1">'United States'</span><span class="ss">}),</span>
      <span class="ss">(</span><span class="n">person</span><span class="ss">)</span><span class="o">-</span><span class="ss">[</span><span class="nc">:LIVES_IN</span><span class="ss">]</span><span class="o">-&gt;</span><span class="ss">(</span><span class="n">location</span><span class="ss">)</span><span class="o">-</span><span class="ss">[</span><span class="nc">:WITHIN</span><span class="o">*</span><span class="mf">0.</span><span class="n">.</span><span class="ss">]</span><span class="o">-&gt;</span><span class="ss">(</span><span class="py">eu:</span><span class="n">Continent</span> <span class="ss">{</span><span class="py">name:</span> <span class="s1">'Europe'</span><span class="ss">})</span>
<span class="k">RETURN</span> <span class="n">person.name</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">*0..</code> syntax traverses a <strong>variable number of hops</strong> — it might take 2 hops (city → country) or 4 hops (village → commune → département → country). This is natural in Cypher; the equivalent recursive SQL takes 30+ lines.</p> <blockquote> <p><strong>GraphQL ≠ graph databases</strong>: GraphQL is an API query language for REST-like services that lets front-end clients declare the exact JSON shape they need. It has no native graph traversal semantics and does not require a graph database.</p> </blockquote> <hr/> <h2 id="8-event-sourcing-and-cqrs">8. Event Sourcing and CQRS</h2> <p>Traditional databases are <strong>state-based</strong>: an <code class="language-plaintext highlighter-rouge">UPDATE</code> overwrites the previous value and the old state is gone forever. This makes it impossible to answer “how did we get here?” or to replay history.</p> <p><strong>Event sourcing</strong> flips the model: instead of storing current state, you store an immutable, append-only log of every event that ever happened.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Event 1: AccountCreated { user_id: 42, balance: 0 }
Event 2: Deposited { user_id: 42, amount: 100 }
Event 3: Withdrew { user_id: 42, amount: 95 }
</code></pre></div></div> <p>The current balance ($5) is derived by replaying the log. This gives you:</p> <ul> <li>A complete audit trail.</li> <li>The ability to rebuild derived state from scratch after a bug.</li> <li>Multiple, independent <strong>read models</strong> computed from the same log.</li> </ul> <p><strong>CQRS (Command Query Responsibility Segregation)</strong> separates write path from read path:</p> <ul> <li><strong>Write model</strong>: the append-only event log (Kafka, EventStoreDB).</li> <li><strong>Read model(s)</strong>: materialised views derived from the log — one relational table for dashboards, one document store for customer receipts, one search index for full-text lookup — all fed by the same event stream.</li> </ul> <p><strong>The GDPR tension</strong>: immutable logs can’t comply with the right to erasure. The solution is <strong>crypto-shredding</strong>: encrypt each user’s personal payload with a unique key. To “delete” the user, destroy the key. The bytes remain in the log but are permanently unreadable garbage.</p> <hr/> <h2 id="9-data-frames-and-ml">9. Data Frames and ML</h2> <p>Machine learning models are fundamentally linear algebra over matrices of numbers. A relational table with text columns (“genre: Drama”, “region: EU”) cannot be fed directly to a neural network.</p> <p><strong>Data frames</strong> (pandas, Polars, Apache Spark) are the translation layer. Common transformations:</p> <ul> <li><strong>One-hot encoding</strong>: convert <code class="language-plaintext highlighter-rouge">genre = "Drama"</code> into a sparse numeric vector: <code class="language-plaintext highlighter-rouge">[is_drama=1, is_comedy=0, is_action=0, ...]</code>.</li> <li><strong>Normalisation</strong>: rescale numeric columns to \([0,1]\) or zero-mean unit variance.</li> <li><strong>Pivoting/aggregation</strong>: reduce event streams into feature vectors.</li> </ul> <p>Data frames are notably <strong>imperative</strong> (pipeline of transformations), unlike the declarative SQL model. This is intentional: data scientists explore data incrementally, inspecting intermediate results. They need granular control over each transformation step.</p> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li>The data model you choose defines not just how you store data, but which queries are natural, which are possible, and which are catastrophically expensive.</li> <li>Document models excel at self-contained hierarchies; relational models excel at many-to-many relationships; graph models excel at variable-depth traversals.</li> <li>ORMs are leaky abstractions — you must understand SQL joins to use them correctly (N+1 problem).</li> <li>Event sourcing provides a perfect audit trail and replay capability, at the cost of GDPR complexity (mitigated by crypto-shredding).</li> <li>The “schema-less” label for document databases is misleading — the schema still exists, it just lives in application code rather than the database.</li> </ul> <p><em>Next: Chapter 4 — Storage and Retrieval.</em></p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="data-models"/><category term="sql"/><category term="nosql"/><category term="graph"/><category term="relational"/><category term="document"/><category term="event-sourcing"/><summary type="html"><![CDATA[Ludwig Wittgenstein wrote: “The limits of my language mean the limits of my world.” In software, the data model you choose to represent your domain defines the questions you can even ask about it — and the queries you can run efficiently. Choose wrong, and performance limits become architectural limits.]]></summary></entry><entry><title type="html">Part 4: Storage and Retrieval</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-4-Storage-and-Retrieval/" rel="alternate" type="text/html" title="Part 4: Storage and Retrieval"/><published>2026-06-04T00:00:00+00:00</published><updated>2026-06-04T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%204%20-%20Storage%20and%20Retrieval</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-4-Storage-and-Retrieval/"><![CDATA[<p>As Richard Feynman observed in a 1985 seminar: <em>“Computers are primarily filing systems.”</em> Almost every web application is, at its core, a sophisticated system for putting data into boxes and taking it out again later. Chapter 4 of DDIA pulls back the curtain on exactly how those boxes work.</p> <p>You will likely never write a custom storage engine from scratch. But you absolutely need to understand them to: choose the right database for your workload, configure it correctly, and diagnose why it’s running queries in 10 seconds instead of 10 milliseconds.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 4 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-two-line-database">1. The Two-Line Database</h2> <p>The simplest possible key-value store is a pair of bash functions:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>db_set<span class="o">()</span> <span class="o">{</span> <span class="nb">echo</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">,</span><span class="nv">$2</span><span class="s2">"</span> <span class="o">&gt;&gt;</span> database<span class="p">;</span> <span class="o">}</span>
db_get<span class="o">()</span> <span class="o">{</span> <span class="nb">grep</span> <span class="s2">"^</span><span class="nv">$1</span><span class="s2">,"</span> database | <span class="nb">tail</span> <span class="nt">-n</span> 1 | <span class="nb">cut</span> <span class="nt">-d</span><span class="s1">','</span> <span class="nt">-f2</span><span class="p">;</span> <span class="o">}</span>
</code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">db_set</code></strong> appends a new line to a plain text file. Appending is O(1) — the fastest write operation possible. No searching, no seeking; the disk just streams data sequentially.</p> <p><strong><code class="language-plaintext highlighter-rouge">db_get</code></strong> scans the file from the end to find the most recent value. This is O(n) — as the file grows, reads slow proportionally. At a billion rows, a single lookup takes ~17 minutes. Unacceptable.</p> <p>This gap between O(1) writes and O(n) reads motivates every index structure in existence.</p> <hr/> <h2 id="2-indexes-the-fundamental-trade-off">2. Indexes: The Fundamental Trade-Off</h2> <p>An index is a separate data structure derived from the primary data. It acts as a signpost: “key X lives at byte offset Y.” Indexes speed up reads dramatically but <strong>slow down writes</strong> — every write must also update every index.</p> <p>This is why databases don’t index every column automatically. If you index 20 columns, every insert or update touches 20 separate data structures. You must choose indexes based on the specific queries your application actually runs.</p> <hr/> <h2 id="3-hash-indexes-bitcask-model">3. Hash Indexes (BitCask Model)</h2> <p>The simplest index is a <strong>HashMap in RAM</strong> mapping each key to its byte offset in the append-only log file.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HashMap (in RAM):
  user_42 → offset 6400
  user_89 → offset 9120
  ...
</code></pre></div></div> <p><strong>Read path:</strong> Look up the key in the HashMap (O(1)), jump directly to that byte offset on disk, read the value.</p> <p><strong>Write path:</strong> Append to the log, update the HashMap.</p> <p><strong>Compaction:</strong> Over time, the same key accumulates many historical values (old updates). A background process periodically scans the log, keeps only the most recent value per key, and writes a new compact file. The old file is deleted after the swap.</p> <p><strong>Fatal limitation:</strong> All keys must fit in RAM. If you have billions of unique keys, RAM runs out. And HashMaps can’t answer range queries like “give me all users with IDs between 10,000 and 10,999” — keys are scattered randomly by the hash function.</p> <hr/> <h2 id="4-sstables-and-lsm-trees">4. SSTables and LSM Trees</h2> <h3 id="the-sorted-string-table-sstable">The Sorted String Table (SSTable)</h3> <p>The breakthrough idea: what if the keys in the log file were <strong>sorted alphabetically</strong>? We call this a Sorted String Table (SSTable).</p> <p>Sorting enables a <strong>sparse index</strong>: instead of storing every key in RAM, you only store one key per ~4 KB block. To find <code class="language-plaintext highlighter-rouge">handiwork</code>, you look up the index, see that <code class="language-plaintext highlighter-rouge">handbag</code> starts at offset 10,000 and <code class="language-plaintext highlighter-rouge">handsome</code> starts at offset 14,000, and scan the 4 KB segment between them. The RAM footprint drops by orders of magnitude.</p> <p>Sorting also makes range queries trivial: all keys between <code class="language-plaintext highlighter-rouge">user_10000</code> and <code class="language-plaintext highlighter-rouge">user_10999</code> are stored contiguously on disk. One sequential read suffices.</p> <h3 id="the-write-problem-memtables">The Write Problem: MemTables</h3> <p>You can’t sort on disk efficiently — inserting in the middle of a file requires rewriting the entire right half. The solution: buffer incoming writes in a <strong>MemTable</strong> (a sorted in-memory data structure, typically a red-black tree or AVL tree).</p> <ol> <li>Writes go into the MemTable (fast, in-memory sort).</li> <li>When the MemTable reaches ~4 MB, it is flushed to disk as a new, immutable SSTable file.</li> <li>A background process <strong>merges</strong> multiple SSTable files using a merge sort pass, discarding stale values and producing one clean, compact file.</li> </ol> <p>This full architecture — MemTable + SSTable + background merge — is the <strong>Log-Structured Merge Tree (LSM Tree)</strong>, used by Cassandra, RocksDB, LevelDB, HBase, and others.</p> <pre><code class="language-mermaid">graph TD
    Write[Incoming Write] --&gt; MemTable[MemTable\nin RAM]
    MemTable --&gt;|flush when full| SSTable1[SSTable 1\non disk]
    SSTable1 --&gt;|background merge| SSTable_merged[Merged SSTable]
    Read[Read Query] --&gt; MemTable
    MemTable --&gt;|miss| SSTable1
    SSTable1 --&gt;|miss| SSTable_merged

    classDef default fill:#1a1a1a,stroke:#888,stroke-width:2px,color:#fff;
    classDef highlight fill:#2196F3,stroke:#fff,stroke-width:2px,color:#fff;
    class Write,Read highlight;
</code></pre> <h3 id="bloom-filters">Bloom Filters</h3> <p><strong>The missing-key problem:</strong> If you query for a key that doesn’t exist, the database must check every SSTable before giving up. This is expensive for write-heavy workloads where many keys are queried that were never written.</p> <p>A <strong>Bloom filter</strong> is a compact bit array that answers: “was this key <em>definitely not</em> written to this SSTable?” It never produces false negatives (if it says “not here”, the key is guaranteed to be absent) but can produce false positives (it might say “maybe here” when the key isn’t). A false positive just causes one unnecessary disk read — harmless. A false negative would mean silently returning “not found” for data that exists — catastrophic.</p> <p>In production, Bloom filters reduce unnecessary SSTable reads by ~99%, making LSM trees viable for read-heavy workloads too.</p> <hr/> <h2 id="5-b-trees-the-50-year-reigning-champion">5. B-Trees: The 50-Year Reigning Champion</h2> <p>While LSM trees append new files and merge them, <strong>B-trees</strong> update data <strong>in place</strong>. They are the storage engine behind PostgreSQL, MySQL (InnoDB), SQLite, and most traditional relational databases.</p> <h3 id="structure">Structure</h3> <p>The database is divided into <strong>fixed-size pages</strong> (typically 4 KB, matching hardware page size). Pages are organised as a balanced tree:</p> <ul> <li><strong>Root page</strong>: contains references to child pages, partitioned by key range.</li> <li><strong>Internal pages</strong>: further subdivide key ranges.</li> <li><strong>Leaf pages</strong>: contain the actual data (or pointers to it).</li> </ul> <p>Because the tree is balanced, every lookup traverses the same number of levels. For a 1 TB database, it typically takes <strong>3–4 page reads</strong> to find any record — remarkably fast.</p> <h3 id="splits-and-write-ahead-log">Splits and Write-Ahead Log</h3> <p>When a leaf page is full and a new key must be inserted, the page <strong>splits</strong> into two half-full pages, and the parent is updated. If the parent is also full, the split cascades upward.</p> <p>Danger: if the server crashes mid-split, pages are left in an inconsistent half-written state. The fix is a <strong>Write-Ahead Log (WAL)</strong>: before touching any page, the database writes the intended operation to an append-only journal. On crash recovery, the database replays the WAL to finish any incomplete operations.</p> <hr/> <h2 id="6-lsm-trees-vs-b-trees">6. LSM Trees vs. B-Trees</h2> <h3 id="write-amplification">Write Amplification</h3> <p>A B-tree must write every change twice (WAL + page), and may trigger cascading parent updates. Changing a single byte conceptually requires writing an entire 4 KB page.</p> <p>LSM trees accumulate writes in the MemTable and flush them sequentially — far lower write amplification for individual mutations.</p> <h3 id="ssd-friendliness">SSD Friendliness</h3> <p>SSDs erase data in large <strong>blocks</strong> (~512 KB) but write in small <strong>pages</strong> (4 KB). Random small writes (B-tree’s pattern) cause the SSD’s internal garbage collector to shuffle data constantly, wearing out flash cells. Sequential large writes (LSM’s flush pattern) respect the hardware’s natural boundaries and extend SSD lifetime significantly.</p> <h3 id="read-performance">Read Performance</h3> <p>B-trees: follow the tree, read 3–4 pages. Predictable, fast, optimal for point lookups.</p> <p>LSM trees: check MemTable, then SSTable 1, then SSTable 2, … Bloom filters make this fast in practice, but with many SSTables, read amplification can grow. Background compaction keeps it bounded.</p> <table> <thead> <tr> <th> </th> <th>LSM Tree</th> <th>B-Tree</th> </tr> </thead> <tbody> <tr> <td><strong>Write throughput</strong></td> <td>Higher</td> <td>Lower (write amplification)</td> </tr> <tr> <td><strong>Read throughput</strong></td> <td>Slightly lower</td> <td>Higher (predictable tree walk)</td> </tr> <tr> <td><strong>SSD wear</strong></td> <td>Lower</td> <td>Higher</td> </tr> <tr> <td><strong>Worst-case read latency</strong></td> <td>Higher (compaction I/O spikes)</td> <td>Lower (predictable)</td> </tr> <tr> <td><strong>Use case sweet spot</strong></td> <td>Write-heavy (logging, IoT, metrics)</td> <td>Read-heavy (OLTP, general web apps)</td> </tr> </tbody> </table> <hr/> <h2 id="7-columnar-storage-for-analytics">7. Columnar Storage for Analytics</h2> <p>OLTP databases store data <strong>row by row</strong> — all columns of one row are stored together. This is optimal for retrieving a single record (one disk read, all columns available).</p> <p>Analytical queries are the opposite: they scan millions of rows but only need 2–3 columns. Row-oriented storage wastes enormous I/O loading irrelevant columns just to throw them away.</p> <p><strong>Column-oriented storage</strong> stores all values of one column together:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>price column:  [12.99, 8.50, 24.99, 3.00, ...]
product column: [69, 42, 69, 17, ...]
date column:   [2024-01-01, 2024-01-01, 2024-01-02, ...]
</code></pre></div></div> <p>For a query like <code class="language-plaintext highlighter-rouge">SELECT AVG(price) FROM sales WHERE year = 2024</code>:</p> <ul> <li>Load only the <code class="language-plaintext highlighter-rouge">price</code> and <code class="language-plaintext highlighter-rouge">date</code> columns.</li> <li>Never touch the customer name, address, or any other column.</li> </ul> <h3 id="compression">Compression</h3> <p>Because a single column contains one data type (e.g., all integers), it compresses extremely well. The <code class="language-plaintext highlighter-rouge">product_id</code> column might have only 100 distinct values across a billion rows. <strong>Bitmap encoding</strong> creates one bit-array per distinct value: for product 69, the array is <code class="language-plaintext highlighter-rouge">0 0 0 1 0 0 1 0 ...</code>. <strong>Run-length encoding</strong> then compresses long runs of zeros: instead of storing 10,000 zeros, store the instruction “10,000 zeros”. A billion-row column can compress to a few kilobytes.</p> <p>Compressed column data fits in CPU cache lines, and <strong>vectorised processing</strong> — bitwise AND/OR operations on compressed bitmaps — lets modern CPUs evaluate <code class="language-plaintext highlighter-rouge">product = 69 AND region = 'EU'</code> across millions of records in a single instruction cycle.</p> <h3 id="data-cubes-and-materialised-views">Data Cubes and Materialised Views</h3> <p>Some analytical queries are run so frequently that it’s worth pre-computing and storing their results. A <strong>data cube</strong> (or materialised aggregate view) pre-computes sums and counts across common dimensions (product × date × region). Dashboard queries become instant lookups into the pre-computed grid.</p> <p>Trade-off: if a user wants to filter by a dimension not included in the cube (e.g., weather), they must fall back to scanning raw data.</p> <hr/> <h2 id="8-multi-dimensional-and-vector-indexes">8. Multi-Dimensional and Vector Indexes</h2> <h3 id="r-trees-for-geospatial-queries">R-Trees for Geospatial Queries</h3> <p>A standard B-tree can efficiently answer “find all users with ID between 10,000 and 10,999” (one dimension). But “find all coffee shops within 2 km with rating ≥ 4 stars” is two-dimensional — a B-tree can only use one index at a time.</p> <p><strong>R-trees</strong> partition the data into nested rectangular bounding boxes. To find coffee shops nearby, the database prunes entire bounding boxes that don’t overlap with the search circle, then descends into overlapping boxes, simultaneously filtering on the rating dimension. Applications extend beyond geography: any multi-dimensional range query (date + temperature, user age + account balance) benefits from an R-tree.</p> <h3 id="vector-indexes-for-semantic-search">Vector Indexes for Semantic Search</h3> <p>Traditional indexes match exact values. Modern AI search requires <strong>semantic</strong> matching: the query “cancel my subscription” should match a document titled “How to terminate your contract” — zero shared words.</p> <p>Large language models translate text into <strong>embedding vectors</strong> (arrays of ~1,000 floats) representing semantic meaning. Semantically similar texts map to nearby points in this high-dimensional space. The similarity metric is typically <strong>cosine similarity</strong> — the angle between two vectors.</p> <p>Finding the nearest vectors to a query vector is the k-Nearest Neighbours problem. Exact search requires comparing the query to every stored vector (O(n)) — too slow at scale.</p> <p>The <strong>HNSW (Hierarchical Navigable Small World)</strong> index solves this with a layered graph structure:</p> <ul> <li>Top layer: sparse graph of a few nodes with long-range connections.</li> <li>Lower layers: progressively denser graphs.</li> <li>Search: enter the top layer, greedily navigate toward the query point, then drop to the next layer for finer resolution.</li> </ul> <p>Like descending from a satellite view to street view — quickly narrowing from continent to neighbourhood to building.</p> <p>The result is <strong>approximate nearest neighbour search</strong>: not guaranteed to find the exact closest vector, but finds a very close one in O(log n) time.</p> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li>Indexes always trade write performance for read performance. Choose them based on your actual query patterns.</li> <li>LSM trees (append-then-merge) excel at write-heavy workloads and are kinder to SSD hardware.</li> <li>B-trees (in-place update with WAL) remain the gold standard for read-heavy OLTP workloads.</li> <li>Columnar storage, bitmap compression, and vectorised processing enable analytical queries that would be impossibly slow in row-oriented OLTP databases.</li> <li>Vector indexes (HNSW) represent a fundamental shift: databases are moving from exact retrieval of stored facts to approximate navigation through semantic meaning.</li> </ul> <p><em>Next: Chapter 5 — Encoding and Evolution.</em></p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="storage"/><category term="lsm-tree"/><category term="b-tree"/><category term="sstable"/><category term="bloom-filter"/><category term="columnar-storage"/><summary type="html"><![CDATA[As Richard Feynman observed in a 1985 seminar: “Computers are primarily filing systems.” Almost every web application is, at its core, a sophisticated system for putting data into boxes and taking it out again later. Chapter 4 of DDIA pulls back the curtain on exactly how those boxes work.]]></summary></entry><entry><title type="html">Part 5: Encoding and Evolution</title><link href="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-5-Encoding-and-Evolution/" rel="alternate" type="text/html" title="Part 5: Encoding and Evolution"/><published>2026-06-04T00:00:00+00:00</published><updated>2026-06-04T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/DDIA%20Part%205%20-%20Encoding%20and%20Evolution</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/DDIA-Part-5-Encoding-and-Evolution/"><![CDATA[<p>Data doesn’t just sit in databases — it travels. Between services. From memory to disk. Across continents. And it must remain readable as your application evolves over months and years. Chapter 5 of DDIA covers the engineering of that travel: how to translate in-memory objects into portable bytes, how to make those bytes survive schema changes, and how to design communication patterns that are robust under distributed system realities.</p> <blockquote> <h5 id="source">Source</h5> <p class="block-tip">Notes drawn from Chapter 5 of <em>Designing Data-Intensive Applications</em> (2nd ed.) by Martin Kleppmann &amp; Chris Riccomini.</p> </blockquote> <blockquote> <h5 id="created-with">Created With</h5> <p class="block-tip">These notes were structured with the help of <a href="https://notebooklm.google.com">NotebookLM</a>, using podcast-style audio overviews generated from the book chapters.</p> </blockquote> <hr/> <h2 id="1-the-rolling-upgrade-problem">1. The Rolling Upgrade Problem</h2> <p>You cannot upgrade a production service by shutting down all instances simultaneously. The standard technique is a <strong>rolling upgrade</strong>: deploy new code to a small batch of servers, verify them, then proceed to the next batch.</p> <p>During this window, <strong>old and new code run simultaneously</strong>, both reading from and writing to the same database. This means:</p> <ul> <li><strong>Backward compatibility</strong>: new code must handle data written by old code (missing fields, old formats).</li> <li><strong>Forward compatibility</strong>: old code must gracefully handle data written by new code (unknown fields it has never seen).</li> </ul> <p>Backward compatibility is manageable — the new code’s author knows the old format. Forward compatibility is harder — old code must somehow survive encountering fields it was never compiled to understand, without crashing and without accidentally deleting them.</p> <p>These two requirements shape every format and protocol decision that follows.</p> <hr/> <h2 id="2-from-objects-to-bytes">2. From Objects to Bytes</h2> <p>Application objects (hash tables, linked lists, trees) are optimised for CPU access via <strong>memory pointers</strong> — addresses that are local to one machine’s RAM. A pointer on server A means nothing to server B.</p> <p>To send data over a network or write it to disk, you must <strong>encode</strong> (serialise) it into a flat byte sequence that carries no machine-specific pointers. At the destination, you <strong>decode</strong> (deserialise) those bytes back into in-memory objects.</p> <p>The key engineering question: in what format should those bytes be arranged?</p> <hr/> <h2 id="3-language-native-formats-never-use-in-production">3. Language-Native Formats: Never Use in Production</h2> <p>Every mainstream language ships a built-in serialisation library (Java Serialisation, Python <code class="language-plaintext highlighter-rouge">pickle</code>, Ruby <code class="language-plaintext highlighter-rouge">Marshal</code>). These are irresistible for beginners — one line of code and the object is on the wire.</p> <p><strong>They are architectural traps:</strong></p> <ol> <li><strong>Ecosystem lock-in</strong>: Java Serialisation produces bytes only a JVM can read. When your company rewrites an analytics service in Go, the data is locked inside a Java-specific format.</li> <li><strong>Remote code execution vulnerabilities</strong>: Python’s <code class="language-plaintext highlighter-rouge">pickle</code> allows the byte stream to embed instructions like “instantiate this class and call this initialiser”. A malicious payload can instruct your server to open a network backdoor or delete files. Java Serialisation has been the root cause of some of the most devastating enterprise breaches in history.</li> <li><strong>No schema evolution</strong>: these formats treat forward/backward compatibility as an afterthought.</li> </ol> <p>Rule: never use language-native serialisation for any data that crosses a service boundary or persists to storage.</p> <hr/> <h2 id="4-textual-formats-json-xml-csv">4. Textual Formats: JSON, XML, CSV</h2> <p>Universal and human-readable — every language can parse them. But each carries dangerous ambiguities.</p> <h3 id="csv">CSV</h3> <p>No schema attached. Is column 3 a zip code, a phone number, or a product ID? Any comma inside a field must be escaped; escaping rules vary across parsers, causing silent corruption when moving files between systems.</p> <h3 id="xml">XML</h3> <p>Highly structured but verbose. Widely used in enterprise systems; largely replaced by JSON for APIs.</p> <h3 id="json">JSON</h3> <p>Lightweight and ubiquitous. But:</p> <ul> <li><strong>No native binary support</strong>: raw image bytes must be Base64-encoded, inflating size by ~33%.</li> <li><strong>No integer/float distinction</strong>: JSON just has “number.” Different runtimes interpret large numbers differently.</li> </ul> <p><strong>The 64-bit integer trap:</strong> JavaScript represents all numbers as 64-bit IEEE 754 doubles. The mantissa provides only 53 bits of integer precision, meaning integers larger than \(2^{53} \approx 9 \times 10^{15}\) are silently rounded.</p> <p>This is not hypothetical. When Twitter introduced Snowflake IDs (64-bit integers encoding timestamp + machine ID), browsers were silently rounding tweet IDs, making it impossible to reference or delete specific tweets. Their fix: send every ID twice in the JSON — once as a raw number (for back-end languages that handle 64-bit integers correctly) and once as a string (which JavaScript preserves exactly because it doesn’t do arithmetic on strings).</p> <h3 id="json-schema">JSON Schema</h3> <p>The ambiguity problem can be controlled with <strong>JSON Schema</strong> — a declarative blueprint defining the exact types, formats, and constraints of valid JSON:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"object"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"properties"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"user_id"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"integer"</span><span class="w"> </span><span class="p">},</span><span class="w">
    </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"string"</span><span class="p">,</span><span class="w"> </span><span class="nl">"format"</span><span class="p">:</span><span class="w"> </span><span class="s2">"email"</span><span class="w"> </span><span class="p">}</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"required"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"user_id"</span><span class="p">,</span><span class="w"> </span><span class="s2">"email"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"additionalProperties"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">additionalProperties: false</code> is a <strong>closed content model</strong>: any field not explicitly listed is rejected. This protects against schema drift and injection attempts.</p> <hr/> <h2 id="5-protocol-buffers-tagged-binary-encoding">5. Protocol Buffers: Tagged Binary Encoding</h2> <p>Textual formats carry every field name in every message. If you send a million user records, the string <code class="language-plaintext highlighter-rouge">"username"</code> appears a million times. Protocol Buffers (protobuf, developed at Google) eliminates this by using <strong>integer tags</strong> instead of string field names.</p> <h3 id="schema-definition">Schema Definition</h3> <div class="language-protobuf highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">message</span> <span class="nc">User</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">username</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="kt">int64</span>  <span class="na">favorite_number</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
  <span class="kt">string</span> <span class="na">photo_url</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="encoding">Encoding</h3> <p>Instead of <code class="language-plaintext highlighter-rouge">{"username": "Alice", "favorite_number": 42}</code>, the wire format is:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[tag=1, type=string, length=5] Alice
[tag=2, type=varint]           42
</code></pre></div></div> <p>The strings <code class="language-plaintext highlighter-rouge">"username"</code> and <code class="language-plaintext highlighter-rouge">"favorite_number"</code> are completely absent. The decoder looks up tag 1 in its local schema file to know this means <code class="language-plaintext highlighter-rouge">username</code>. A typical protobuf message is <strong>half the size</strong> of the equivalent MessagePack, and a small fraction of the JSON.</p> <h3 id="schema-evolution-rules">Schema Evolution Rules</h3> <p>The system’s robustness relies on immutable tag numbers:</p> <ul> <li><strong>Adding a field</strong>: assign a new, never-used tag number. Old code encountering this unknown tag knows the byte length (encoded in the wire format) and skips it safely. New code reading old data that lacks this field receives a language-default value.</li> <li><strong>Never change a tag number</strong>: old data on disk and old code both rely on tag 1 meaning <code class="language-plaintext highlighter-rouge">username</code>. Reassigning it is permanently destructive.</li> <li><strong>Removing a field</strong>: mark it <code class="language-plaintext highlighter-rouge">reserved</code> so no future field accidentally reuses that tag.</li> </ul> <h3 id="variable-length-integers-varints">Variable-Length Integers (Varints)</h3> <p>Standard <code class="language-plaintext highlighter-rouge">int64</code> always uses 8 bytes. Most integers in practice are small. Protobuf uses <strong>varints</strong> — a variable-length encoding where each byte uses 7 bits for data and 1 bit as a continuation flag:</p> <ul> <li>The number <code class="language-plaintext highlighter-rouge">5</code> encodes in <strong>1 byte</strong> (7 bits easily covers it; continuation bit = 0).</li> <li>The number <code class="language-plaintext highlighter-rouge">300</code> encodes in <strong>2 bytes</strong> (needs 9 bits; split across two bytes).</li> <li>The number <code class="language-plaintext highlighter-rouge">9,000,000,000,000</code> encodes in <strong>7 bytes</strong>.</li> </ul> <p>For a typical user record with small IDs and short strings, this can halve the payload again compared to fixed-width encoding.</p> <hr/> <h2 id="6-apache-avro-schema-driven-binary-without-tags">6. Apache Avro: Schema-Driven Binary Without Tags</h2> <p>Protobuf requires a human to manually assign integer tags to every field. For a data lake with thousands of constantly-evolving tables, this becomes an administrative nightmare. Apache Avro (created for the Hadoop ecosystem) solves this differently.</p> <h3 id="tag-free-encoding">Tag-Free Encoding</h3> <p>Avro’s wire format contains <strong>no field names and no tags</strong> — just the raw values, concatenated in declaration order:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[varint length=5] Alice [varint] 42
</code></pre></div></div> <p>Without a schema, this is unreadable. That’s intentional.</p> <h3 id="schema-resolution">Schema Resolution</h3> <p>Avro decoding requires two schemas simultaneously:</p> <ul> <li><strong>Writer’s schema</strong>: the exact schema used when the data was encoded.</li> <li><strong>Reader’s schema</strong>: the schema the current application code expects.</li> </ul> <p>The Avro library matches fields by <strong>name</strong> (not by tag number) and reconciles differences:</p> <table> <thead> <tr> <th>Scenario</th> <th>Resolution</th> </tr> </thead> <tbody> <tr> <td>Writer has a field the reader doesn’t know</td> <td>Skip the bytes for that field</td> </tr> <tr> <td>Reader expects a field the writer didn’t include</td> <td>Use the declared default value</td> </tr> <tr> <td>Writer type differs from reader type</td> <td>Apply defined type promotion rules (e.g., <code class="language-plaintext highlighter-rouge">int</code> → <code class="language-plaintext highlighter-rouge">long</code>)</td> </tr> </tbody> </table> <p>This active translation happens without any human assigning tags. You can write a script that reads a relational database schema, generates an Avro schema dynamically, and dumps any table without manual intervention.</p> <h3 id="schema-delivery">Schema Delivery</h3> <p>The schema itself must travel alongside the data:</p> <ol> <li><strong>Large files</strong> (Hadoop, Parquet): embed the writer’s schema once at the start of the file. Amortised over millions of records, the overhead is negligible.</li> <li><strong>Message streams</strong> (Kafka): use a centralised <strong>schema registry</strong> (Confluent Schema Registry). Each schema gets an integer version ID. Prepend that tiny integer to each message; the consumer fetches the full schema once from the registry and caches it.</li> <li><strong>Network connections</strong>: negotiate the schema during the handshake when establishing the connection, then use it for all messages on that connection.</li> </ol> <hr/> <h2 id="7-rest-rpc-and-the-location-transparency-fallacy">7. REST, RPC, and the Location Transparency Fallacy</h2> <h3 id="rest">REST</h3> <p>REST uses HTTP verbs (<code class="language-plaintext highlighter-rouge">GET</code>, <code class="language-plaintext highlighter-rouge">POST</code>, <code class="language-plaintext highlighter-rouge">PUT</code>, <code class="language-plaintext highlighter-rouge">DELETE</code>) against URL endpoints. Universally understood, cache-friendly, easy to inspect with curl or a browser. JSON with OpenAPI specifications is the dominant combination for external APIs.</p> <h3 id="rpc-and-its-fatal-flaw">RPC and Its Fatal Flaw</h3> <p>RPC (Remote Procedure Call) tries to make a call to a remote service look identical to a local function call — “location transparency.” The problem: this is a lie.</p> <p>A local function call is:</p> <ul> <li><strong>Instantaneous</strong> (nanoseconds)</li> <li><strong>Binary</strong> — succeeds with a return value, or throws an exception</li> </ul> <p>A network call is:</p> <ul> <li><strong>Slow</strong> (milliseconds)</li> <li><strong>Has a third state</strong> — the <strong>timeout/void</strong>: the request was sent, but you don’t know if it arrived, succeeded, or failed</li> </ul> <p><strong>The idempotence problem in finance:</strong></p> <p>You call <code class="language-plaintext highlighter-rouge">charge_card($100)</code>. Timeout. Did the charge succeed? If you retry, you may double-charge the customer. If you don’t retry, you may have given away goods for free. Location transparency hid the network, and now you’re in an impossible state.</p> <p>Modern gRPC is better — it forces you to handle deadlines and contexts — but the fundamental problem of the unreliable network cannot be abstracted away.</p> <hr/> <h2 id="8-service-discovery">8. Service Discovery</h2> <p>In a cloud environment, the IP addresses of service instances change constantly as containers start and stop. Hard-coding an IP in a config file means your calls hit a void when that instance restarts.</p> <p><strong>Service discovery</strong> (Consul, ZooKeeper, Kubernetes DNS) acts as a real-time registry:</p> <ol> <li>On startup, every service instance registers its current IP and health status.</li> <li>Instances send heartbeats every few seconds.</li> <li>When a client needs to call <code class="language-plaintext highlighter-rouge">payment-service</code>, it queries the registry for a healthy instance’s current IP.</li> <li>When an instance fails (heartbeat stops), it’s removed from the registry within seconds.</li> </ol> <p>A <strong>service mesh</strong> (Istio, Linkerd) extends this with a sidecar proxy alongside every service container, handling load balancing, TLS encryption, and retries transparently — the application code calls <code class="language-plaintext highlighter-rouge">http://payment-service</code> and the proxy handles everything.</p> <hr/> <h2 id="9-durable-execution">9. Durable Execution</h2> <p>Multi-step workflows (fraud check → charge card → reserve inventory → generate shipping label) are fragile under standard RPC. If the server crashes after step 3, where do you restart? Retrying from the beginning may double-charge the customer; giving up leaves the order incomplete.</p> <p><strong>Durable execution</strong> frameworks (Temporal, Restate) solve this with a <strong>persistent event log</strong>. Every time a workflow step completes, its result is written to a durable journal. On crash recovery, the workflow replays its code against the journal:</p> <ol> <li>“Run fraud check” → journal says “already done: approved” → inject the result, skip the network call.</li> <li>“Charge card” → journal says “already done: txn-id 8823” → inject the result.</li> <li>“Generate shipping label” → not in journal → actually execute.</li> </ol> <p>The workflow resumes exactly where it crashed, without repeating any successful work.</p> <p><strong>Critical constraint</strong>: workflow code must be <strong>deterministic</strong> — identical inputs produce identical outputs every run. Calls to <code class="language-plaintext highlighter-rouge">random.random()</code>, <code class="language-plaintext highlighter-rouge">datetime.now()</code>, or <code class="language-plaintext highlighter-rouge">uuid4()</code> will produce different values during replay, corrupting the journal match. Use the framework’s provided deterministic equivalents.</p> <hr/> <h2 id="10-event-driven-architecture">10. Event-Driven Architecture</h2> <p>Instead of synchronous RPC chains, <strong>message brokers</strong> (Kafka, RabbitMQ, AWS SQS) decouple services via asynchronous events:</p> <pre><code class="language-mermaid">graph LR
    Checkout[Checkout Service] --&gt;|order.paid| Broker[(Message Broker)]
    Broker --&gt; Shipping[Shipping Service]
    Broker --&gt; Analytics[Analytics Service]
    Broker --&gt; Email[Email Service]

    classDef default fill:#1a1a1a,stroke:#888,stroke-width:2px,color:#fff;
    classDef highlight fill:#2196F3,stroke:#fff,stroke-width:2px,color:#fff;
    class Checkout highlight;
</code></pre> <p>The checkout service fires an <code class="language-plaintext highlighter-rouge">order.paid</code> event and moves on. It doesn’t know or care which downstream services are listening. If the shipping service goes down for three hours, events pile up safely in the broker’s durable queue. When it recovers, it drains the backlog without missing a single order.</p> <p>The broker acts as a <strong>shock absorber</strong> that prevents cascading failures. It also simplifies service discovery — producers only need the broker’s address, not the address of every consumer.</p> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li>Rolling upgrades require both backward compatibility (new code reads old data) and forward compatibility (old code handles new fields without deleting them).</li> <li>Language-native serialisation is a security and portability trap. Never use it across service boundaries.</li> <li>JSON’s number ambiguity (especially the 64-bit integer problem) can cause silent data corruption. Track this at service boundaries.</li> <li>Protocol Buffers use immutable integer tags to achieve compact binary encoding with clean schema evolution.</li> <li>Avro uses active schema resolution (writer schema + reader schema) to support fully dynamic schemas — ideal for database dumps and large-scale data pipelines.</li> <li>RPC’s “location transparency” is a dangerous fiction. Network calls have a third state (timeout) that local calls don’t. Design explicitly for it.</li> <li>Event-driven architectures with durable message brokers provide fault isolation that synchronous RPC chains cannot.</li> </ul> <p><em>“Data outlives code.”</em> The serialisation format you choose today may be read by systems and engineers who don’t yet exist. Design for the future reader.</p>]]></content><author><name></name></author><category term="ddia"/><category term="ddia"/><category term="encoding"/><category term="protobuf"/><category term="avro"/><category term="schema-evolution"/><category term="serialization"/><category term="rpc"/><category term="microservices"/><summary type="html"><![CDATA[Data doesn’t just sit in databases — it travels. Between services. From memory to disk. Across continents. And it must remain readable as your application evolves over months and years. Chapter 5 of DDIA covers the engineering of that travel: how to translate in-memory objects into portable bytes, how to make those bytes survive schema changes, and how to design communication patterns that are robust under distributed system realities.]]></summary></entry><entry><title type="html">Part 6: Cooperation and Division – Bargaining Theory and Coalitional Games</title><link href="https://smasoudrezvani.github.io/blog/2026/Bargaining-and-Coalitions/" rel="alternate" type="text/html" title="Part 6: Cooperation and Division – Bargaining Theory and Coalitional Games"/><published>2026-04-25T00:00:00+00:00</published><updated>2026-04-25T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/Bargaining%20and%20Coalitions</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/Bargaining-and-Coalitions/"><![CDATA[<p><em>If you missed the previous chapter, start here: <a href="https://smasoudrezvani.github.io/blog/2026/Bayesian-Games/">Part 5: Bayesian Games and Incomplete Information</a></em></p> <p>Welcome to the final chapter of our game theory series! In Parts 2–5, we studied <strong>non-cooperative games</strong>: players make decisions independently, and agreements can’t be enforced. Today we shift to <strong>cooperative game theory</strong>, where players can form binding agreements, communicate freely, and jointly decide how to split the gains from cooperation.</p> <p>Two central questions arise: <strong>How much will the whole group cooperate?</strong> and <strong>How should the gains be distributed?</strong></p> <blockquote> <h5 id="reference-note">REFERENCE NOTE</h5> <p class="block-tip">The concepts and mathematics in this post are drawn from Chapters 7 and 8 of <em>An Introduction to Game Theory</em> by Martin J. Osborne (Oxford University Press, 2003).</p> </blockquote> <hr/> <h2 id="1-bargaining-two-players-one-pie">1. Bargaining: Two Players, One Pie</h2> <p>The simplest cooperative situation: two players must agree on how to split a <strong>surplus</strong> of size 1. If they agree on \((u_1, u_2)\) with \(u_1 + u_2 \leq 1\), they each get their respective share. If they <strong>disagree</strong>, they each receive a <strong>disagreement payoff</strong> \((d_1, d_2)\) (what they can guarantee themselves outside the negotiation).</p> <p>The <strong>feasible set</strong> \(S \subset \mathbb{R}^2\) is the set of all achievable utility pairs (often a convex, compact region). The <strong>disagreement point</strong> is \(d = (d_1, d_2) \in S\).</p> <p>A <strong>bargaining solution</strong> is a function \(f: (S, d) \mapsto (u_1^*, u_2^*)\) selecting one point from \(S\).</p> <hr/> <h2 id="2-the-nash-bargaining-solution">2. The Nash Bargaining Solution</h2> <p>Rather than modeling the negotiation process explicitly, John Nash (1950) proposed an <strong>axiomatic approach</strong>: list the properties any reasonable solution should satisfy, then show these uniquely determine a solution.</p> <p><strong>The Four Nash Axioms:</strong></p> <ol> <li><strong>Efficiency (PAR):</strong> \(f(S,d)\) lies on the Pareto frontier of \(S\). There’s no outcome in \(S\) that makes both players strictly better off.</li> <li><strong>Symmetry (SYM):</strong> If the game is symmetric (swapping the players produces the same game), then both players receive the same payoff: \(u_1^* = u_2^*\).</li> <li><strong>Scale Invariance (SCI):</strong> The solution is unaffected by positive affine transformations of utility. Rescaling payoffs doesn’t change the outcome.</li> <li><strong>Independence of Irrelevant Alternatives (IIA):</strong> If \(f(S,d) \in S' \subseteq S\), then \(f(S',d) = f(S,d)\). Shrinking the feasible set doesn’t change the solution as long as the solution stays feasible.</li> </ol> <p><strong>Nash’s Theorem:</strong> These four axioms uniquely characterize the <strong>Nash Bargaining Solution</strong>:</p> \[f^{NB}(S, d) = \underset{(u_1, u_2) \in S,\; u_i \geq d_i}{\text{argmax}} \; (u_1 - d_1)(u_2 - d_2)\] <blockquote> <h5 id="the-core-concept">THE CORE CONCEPT</h5> <p class="block-tip">The Nash Bargaining Solution maximizes the <strong>product of utility gains</strong> over the disagreement point. Players cooperate to find the point that jointly maximizes the multiplicative “pie” above their outside options.</p> </blockquote> <p><strong>Geometric Intuition:</strong> The Nash product \((u_1 - d_1)(u_2 - d_2) = k\) is a hyperbola in the \((u_1, u_2)\) plane. The Nash solution is the point on the Pareto frontier of \(S\) that is tangent to the highest such hyperbola.</p> <hr/> <h2 id="3-the-rubinstein-alternating-offers-game">3. The Rubinstein Alternating Offers Game</h2> <p>The Nash axiomatic approach tells us <em>what</em> the outcome should be, but not <em>why</em> — the process of negotiation is a black box. Ariel Rubinstein (1982) filled this gap with an explicit sequential bargaining model.</p> <p><strong>Setup:</strong> Two players alternately propose how to split a pie of size 1. Player 1 proposes first.</p> <ul> <li>If the opponent <strong>accepts</strong>, the game ends and that split is implemented.</li> <li>If the opponent <strong>rejects</strong>, they make a counter-offer in the next period.</li> <li>Each period of delay costs: Player \(i\)’s payoff in period \(t\) is discounted by \(\delta_i^t \in (0,1)\).</li> </ul> <p><strong>The Unique SPE:</strong> By backward induction on the infinite horizon (the game ends in finite time in equilibrium), the unique Subgame Perfect Equilibrium has Player 1 offering and Player 2 <strong>immediately accepting</strong>:</p> \[u_1^* = \frac{1 - \delta_2}{1 - \delta_1 \delta_2}, \quad u_2^* = \frac{\delta_2(1 - \delta_1)}{1 - \delta_1 \delta_2}\] <p><strong>Key Implications:</strong></p> <ul> <li><strong>Patience pays:</strong> A more patient player (higher \(\delta_i\)) receives a larger share.</li> <li><strong>No delay:</strong> Rational players agree immediately — delay is costly and gains nothing.</li> <li><strong>Equal patience → equal split:</strong> If \(\delta_1 = \delta_2 = \delta\), as \(\delta \to 1\), \(u_1^* \to \frac{1}{2}\) — the 50/50 split.</li> </ul> <blockquote> <h5 id="the-core-concept-1">THE CORE CONCEPT</h5> <p class="block-tip">The Rubinstein solution <strong>converges to the Nash Bargaining Solution</strong> with equal disagreement payoffs as discount factors approach 1. The axiomatic approach and the strategic approach reach the same destination from opposite directions.</p> </blockquote> <hr/> <h2 id="4-coalitional-games-beyond-two-players">4. Coalitional Games: Beyond Two Players</h2> <p>With more than two players, coalitions can form and split off. A <strong>coalitional game</strong> (in characteristic function form) is defined by:</p> <ul> <li>A player set \(N = \{1, 2, \ldots, n\}\).</li> <li>A <strong>characteristic function</strong> \(v : 2^N \rightarrow \mathbb{R}\), assigning a real value \(v(S)\) to every coalition \(S \subseteq N\), with \(v(\emptyset) = 0\).</li> </ul> <p>\(v(S)\) is the <strong>total payoff</strong> that coalition \(S\) can guarantee its members by cooperating, regardless of what the other players do.</p> <p>A game is <strong>superadditive</strong> if \(v(S \cup T) \geq v(S) + v(T)\) for all disjoint \(S, T\) — bigger coalitions are (weakly) more productive. Superadditivity makes formation of the grand coalition \(N\) efficient.</p> <hr/> <h2 id="5-the-core">5. The Core</h2> <p>Given that the grand coalition forms, how should the total value \(v(N)\) be split? A <strong>payoff allocation</strong> is a vector \(x = (x_1, \ldots, x_n)\) with \(\sum_{i \in N} x_i = v(N)\) (efficiency).</p> <p>An allocation is <strong>blocked</strong> by coalition \(S\) if the members of \(S\) can all do better by splitting off: \(\sum_{i \in S} x_i &lt; v(S)\)</p> <p><strong>Definition (Core):</strong> The <strong>core</strong> of a coalitional game is the set of efficient allocations that are not blocked by any coalition:</p> \[\text{Core}(v) = \left\{ x \in \mathbb{R}^n : \sum_{i \in N} x_i = v(N), \; \sum_{i \in S} x_i \geq v(S) \; \forall S \subseteq N \right\}\] <p><strong>Properties:</strong></p> <ul> <li>The core may be <strong>empty</strong> — no allocation is stable against all possible defections.</li> <li>The core may be a <strong>large set</strong> — many allocations are stable.</li> <li>Balancedness conditions (Bondareva–Shapley theorem) characterize when the core is non-empty.</li> </ul> <hr/> <h2 id="6-the-shapley-value-a-unique-fair-allocation">6. The Shapley Value: A Unique Fair Allocation</h2> <p>The core describes <em>stable</em> allocations but doesn’t uniquely prescribe how to split the pie. Lloyd Shapley (1953) took the axiomatic approach, asking: what is the <strong>uniquely fair</strong> way to distribute the grand coalition’s value?</p> <p><strong>The Four Shapley Axioms:</strong></p> <ol> <li><strong>Efficiency:</strong> \(\sum_{i \in N} \phi_i(v) = v(N)\).</li> <li><strong>Symmetry:</strong> If players \(i\) and \(j\) are interchangeable (swapping them doesn’t change \(v\)), then \(\phi_i = \phi_j\).</li> <li><strong>Null Player:</strong> If player \(i\) contributes nothing to any coalition — \(v(S \cup \{i\}) = v(S)\) for all \(S\) — then \(\phi_i = 0\).</li> <li><strong>Additivity:</strong> \(\phi_i(v + w) = \phi_i(v) + \phi_i(w)\) for any two games \(v\) and \(w\).</li> </ol> <p><strong>Shapley’s Theorem:</strong> These axioms uniquely determine the <strong>Shapley Value</strong>:</p> \[\phi_i(v) = \sum_{S \subseteq N \setminus \{i\}} \frac{|S|!\,(|N| - |S| - 1)!}{|N|!} \left[ v(S \cup \{i\}) - v(S) \right]\] <table> <tbody> <tr> <td><strong>Interpretation:</strong> Consider all $$</td> <td>N</td> <td>!\(orderings in which players could arrive to form the grand coalition. For each ordering, player\)i\('s **marginal contribution** is\)v(S \cup {i}) - v(S)\(, where\)S\(is the set of players who arrived before\)i\(. The Shapley Value is player\)i$$’s <strong>average marginal contribution</strong> across all orderings.</td> </tr> </tbody> </table> <table> <tbody> <tr> <td>The weight $$\frac{</td> <td>S</td> <td>!(</td> <td>N</td> <td>-</td> <td>S</td> <td>-1)!}{</td> <td>N</td> <td>!}\(is exactly the probability that, in a uniformly random ordering of all\)n\(players, the set of players arriving before\)i\(is exactly\)S$$.</td> </tr> </tbody> </table> <hr/> <h2 id="7-a-worked-example">7. A Worked Example</h2> <p>Three players: \(N = \{1, 2, 3\}\). The characteristic function:</p> <p>\(v(\{1\}) = 0, \; v(\{2\}) = 0, \; v(\{3\}) = 0\) \(v(\{1,2\}) = 6, \; v(\{1,3\}) = 4, \; v(\{2,3\}) = 2, \; v(\{1,2,3\}) = 9\)</p> <p><strong>Computing \(\phi_1\):</strong> We need the marginal contribution of Player 1 in each of the \(3! = 6\) orderings:</p> <table> <thead> <tr> <th style="text-align: left">Ordering</th> <th style="text-align: center">Coalition before 1</th> <th style="text-align: center">Marginal contribution</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">(1, 2, 3)</td> <td style="text-align: center">\(\emptyset\)</td> <td style="text-align: center">\(v(\{1\}) - v(\emptyset) = 0\)</td> </tr> <tr> <td style="text-align: left">(1, 3, 2)</td> <td style="text-align: center">\(\emptyset\)</td> <td style="text-align: center">\(0\)</td> </tr> <tr> <td style="text-align: left">(2, 1, 3)</td> <td style="text-align: center">\(\{2\}\)</td> <td style="text-align: center">\(v(\{1,2\}) - v(\{2\}) = 6\)</td> </tr> <tr> <td style="text-align: left">(3, 1, 2)</td> <td style="text-align: center">\(\{3\}\)</td> <td style="text-align: center">\(v(\{1,3\}) - v(\{3\}) = 4\)</td> </tr> <tr> <td style="text-align: left">(2, 3, 1)</td> <td style="text-align: center">\(\{2,3\}\)</td> <td style="text-align: center">\(v(\{1,2,3\}) - v(\{2,3\}) = 7\)</td> </tr> <tr> <td style="text-align: left">(3, 2, 1)</td> <td style="text-align: center">\(\{2,3\}\)</td> <td style="text-align: center">\(7\)</td> </tr> </tbody> </table> \[\phi_1 = \frac{1}{6}(0 + 0 + 6 + 4 + 7 + 7) = \frac{24}{6} = 4\] <p>Similarly: \(\phi_2 = \frac{1}{6}(6+6+0+0+2+2) \cdot \frac{1}{6}\) … working through all orderings yields \(\phi_2 = \frac{17}{6} \approx 2.83\) and \(\phi_3 = \frac{13}{6} \approx 2.17\). Check: \(4 + \frac{17}{6} + \frac{13}{6} = 4 + 5 = 9 = v(N)\) ✓</p> <hr/> <h2 id="8-visualizing-shapley-values">8. Visualizing Shapley Values</h2> <pre><code class="language-echarts">{
  "title": { "text": "Shapley Value Distribution (3-Player Example)", "left": "center" },
  "tooltip": { "trigger": "item", "formatter": "{b}: {c} ({d}%)" },
  "legend": { "orient": "vertical", "left": "left", "data": ["Player 1 (φ=4.00)", "Player 2 (φ=2.83)", "Player 3 (φ=2.17)"] },
  "series": [{
    "name": "Shapley Value",
    "type": "pie",
    "radius": "60%",
    "data": [
      { "value": 4.00, "name": "Player 1 (φ=4.00)", "itemStyle": {"color": "#2196F3"} },
      { "value": 2.83, "name": "Player 2 (φ=2.83)", "itemStyle": {"color": "#4CAF50"} },
      { "value": 2.17, "name": "Player 3 (φ=2.17)", "itemStyle": {"color": "#FF9800"} }
    ],
    "emphasis": { "itemStyle": { "shadowBlur": 10, "shadowColor": "rgba(0,0,0,0.5)" } }
  }]
}
</code></pre> <hr/> <h2 id="series-summary">Series Summary</h2> <p>We have now traveled through the full landscape of game theory — from two players choosing simultaneously without a word spoken, all the way to large coalitions distributing joint gains by fairness axioms.</p> <table> <thead> <tr> <th style="text-align: left">Post</th> <th style="text-align: left">Topic</th> <th style="text-align: left">Core Concept</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Part 1</td> <td style="text-align: left">Introduction</td> <td style="text-align: left">The strategic interaction framework</td> </tr> <tr> <td style="text-align: left">Part 2</td> <td style="text-align: left">Nash Equilibrium</td> <td style="text-align: left">Self-enforcing unilateral stability</td> </tr> <tr> <td style="text-align: left">Part 3</td> <td style="text-align: left">Mixed Strategies</td> <td style="text-align: left">Randomization and the indifference principle</td> </tr> <tr> <td style="text-align: left">Part 4</td> <td style="text-align: left">Extensive Games</td> <td style="text-align: left">Backward induction and credible threats (SPE)</td> </tr> <tr> <td style="text-align: left">Part 5</td> <td style="text-align: left">Bayesian Games</td> <td style="text-align: left">Private types, BNE, and auction theory</td> </tr> <tr> <td style="text-align: left">Part 6</td> <td style="text-align: left">Bargaining &amp; Coalitions</td> <td style="text-align: left">Nash solution, Rubinstein, core, and Shapley value</td> </tr> </tbody> </table> <p>Game theory is not just an abstract mathematical exercise. Its ideas appear in AI (multi-agent reinforcement learning, mechanism design for LLMs, RLHF), economics (auction design, contract theory), political science (voting theory, international relations), and evolutionary biology (evolutionarily stable strategies). The language of rational strategic interaction is universal.</p>]]></content><author><name></name></author><category term="game-theory"/><category term="game-theory"/><category term="economics"/><category term="mathematics"/><category term="bargaining"/><category term="shapley-value"/><category term="cooperative-games"/><summary type="html"><![CDATA[If you missed the previous chapter, start here: Part 5: Bayesian Games and Incomplete Information]]></summary></entry><entry><title type="html">Part 5: Playing in the Dark – Bayesian Games and Incomplete Information</title><link href="https://smasoudrezvani.github.io/blog/2026/Bayesian-Games/" rel="alternate" type="text/html" title="Part 5: Playing in the Dark – Bayesian Games and Incomplete Information"/><published>2026-04-21T00:00:00+00:00</published><updated>2026-04-21T00:00:00+00:00</updated><id>https://smasoudrezvani.github.io/blog/2026/Bayesian%20Games</id><content type="html" xml:base="https://smasoudrezvani.github.io/blog/2026/Bayesian-Games/"><![CDATA[<p><em>If you missed the previous chapter, start here: <a href="https://smasoudrezvani.github.io/blog/2026/Extensive-Games/">Part 4: Extensive Form Games and Backward Induction</a></em></p> <p>Welcome back! In Parts 2–4, we assumed players know each other’s payoffs — they understand exactly what motivates their opponents. In reality, this is rarely true. A firm doesn’t know its competitor’s costs. A bidder doesn’t know how much a painting is worth to others. A general doesn’t know the enemy’s strength.</p> <p>This <strong>private information</strong> is the defining feature of <strong>Bayesian games</strong>, and it leads to a fundamentally different kind of strategic reasoning.</p> <blockquote> <h5 id="reference-note">REFERENCE NOTE</h5> <p class="block-tip">The concepts and mathematics in this post are drawn from Chapters 9–10 of <em>An Introduction to Game Theory</em> by Martin J. Osborne (Oxford University Press, 2003).</p> </blockquote> <hr/> <h2 id="1-the-problem-of-incomplete-information">1. The Problem of Incomplete Information</h2> <p><strong>Incomplete information</strong> means players do not know the payoff functions of their opponents (as opposed to <strong>imperfect information</strong>, which means they don’t observe past moves).</p> <p>The key insight, due to John Harsanyi (1967), is that incomplete information can be modeled as a game where each player has a privately known <strong>type</strong> that determines their payoffs. All players share a common prior belief about the distribution of types.</p> <p><strong>Example:</strong> In a bilateral trade, the buyer’s private valuation \(v_b\) is drawn from \([0,1]\). The seller’s private cost \(c_s\) is also drawn from \([0,1]\). Neither knows the other’s number. Their types determine whether trade is mutually beneficial.</p> <hr/> <h2 id="2-formal-definition-bayesian-game">2. Formal Definition: Bayesian Game</h2> <p>A <strong>Bayesian game</strong> (or game of incomplete information) is defined by:</p> <ul> <li>A set of players \(N = \{1, \ldots, n\}\).</li> <li>For each player \(i\): an action set \(A_i\), and a <strong>type space</strong> \(T_i\).</li> <li>A <strong>joint prior distribution</strong> \(p \in \Delta(T_1 \times \cdots \times T_n)\) over type profiles, assumed to be <strong>common knowledge</strong>.</li> <li>For each player \(i\): a <strong>payoff function</strong> \(u_i : A \times T \rightarrow \mathbb{R}\), which depends on all actions and all types.</li> </ul> <p>The timing of a Bayesian game:</p> <ol> <li>Nature draws a type profile \((t_1, \ldots, t_n)\) from \(p\).</li> <li>Each player \(i\) privately observes their own type \(t_i\) only.</li> <li>Players simultaneously choose actions based on their types.</li> </ol> <style>.mermaid{display:flex;justify-content:center}</style> <pre><code class="language-mermaid">graph LR
    N[Nature] --&gt;|Draws types| T1[Player 1 observes t_1]
    N --&gt;|Draws types| T2[Player 2 observes t_2]
    T1 --&gt;|Chooses a_1| G((Payoffs))
    T2 --&gt;|Chooses a_2| G

    classDef default fill:#1a1a1a,stroke:#888,stroke-width:2px,color:#fff;
    classDef nature fill:#9C27B0,stroke:#fff,stroke-width:2px,color:#fff;
    classDef player fill:#2196F3,stroke:#fff,stroke-width:2px,color:#fff;
    class N nature;
    class T1,T2 player;
</code></pre> <hr/> <h2 id="3-strategies-and-bayesian-nash-equilibrium">3. Strategies and Bayesian Nash Equilibrium</h2> <p>In a Bayesian game, a <strong>strategy</strong> for player \(i\) is a function \(\sigma_i : T_i \rightarrow A_i\) (or \(\Delta(A_i)\) for mixed strategies) — a mapping from each possible type to an action.</p> <p>Player \(i\) of type \(t_i\) evaluates strategy profile \(\sigma\) by taking the expectation over the other players’ types:</p> \[U_i(\sigma \mid t_i) = \mathbb{E}_{t_{-i} \mid t_i}\left[ u_i(\sigma_i(t_i), \sigma_{-i}(t_{-i}), t_i, t_{-i}) \right]\] <p><strong>Definition (Bayesian Nash Equilibrium):</strong> A strategy profile \(\sigma^* = (\sigma_1^*, \ldots, \sigma_n^*)\) is a <strong>Bayesian Nash Equilibrium (BNE)</strong> if, for every player \(i\) and every type \(t_i \in T_i\):</p> \[U_i(\sigma^* \mid t_i) \geq U_i(\sigma_i', \sigma_{-i}^* \mid t_i) \quad \forall \sigma_i'\] <blockquote> <h5 id="the-core-concept">THE CORE CONCEPT</h5> <p class="block-tip">In a BNE, each player’s strategy is a <strong>best response</strong> to the other players’ strategies, <strong>type-by-type</strong>. A player of type \(t_i\) acts optimally given their type, knowing the type distribution and the equilibrium strategies of others.</p> </blockquote> <hr/> <h2 id="4-example-a-simple-two-type-game">4. Example: A Simple Two-Type Game</h2> <p>Consider a game where Player 2 can be one of two types: <strong>Strong (S)</strong> with probability \(p\), or <strong>Weak (W)</strong> with probability \(1-p\). Player 1 does not know the type. The payoff matrix (Player 2 is either type S or type W):</p> <p><strong>If Player 2 is type S:</strong></p> <table> <thead> <tr> <th style="text-align: left"> </th> <th style="text-align: center"><strong>L</strong></th> <th style="text-align: center"><strong>R</strong></th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>U</strong></td> <td style="text-align: center">(1, 2)</td> <td style="text-align: center">(0, 0)</td> </tr> <tr> <td style="text-align: left"><strong>D</strong></td> <td style="text-align: center">(0, 0)</td> <td style="text-align: center">(2, 1)</td> </tr> </tbody> </table> <p><strong>If Player 2 is type W:</strong></p> <table> <thead> <tr> <th style="text-align: left"> </th> <th style="text-align: center"><strong>L</strong></th> <th style="text-align: center"><strong>R</strong></th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>U</strong></td> <td style="text-align: center">(1, 0)</td> <td style="text-align: center">(0, 1)</td> </tr> <tr> <td style="text-align: left"><strong>D</strong></td> <td style="text-align: center">(0, 2)</td> <td style="text-align: center">(2, 0)</td> </tr> </tbody> </table> <p>Player 2 knows their type and picks accordingly. Player 1 must pick U or D <strong>before</strong> knowing the type. Player 1’s expected payoff of choosing U:</p> \[\mathbb{E}[U|U, \sigma_2] = p \cdot u_1(U, \sigma_2^S) + (1-p) \cdot u_1(U, \sigma_2^W)\] <p>A BNE specifies: what Player 2 does for each type <em>and</em> what Player 1 does — such that no one wants to deviate given correct beliefs.</p> <hr/> <h2 id="5-auctions-the-canonical-bayesian-game">5. Auctions: The Canonical Bayesian Game</h2> <p>Auctions are the richest application of Bayesian game theory. In a <strong>first-price sealed-bid auction</strong>:</p> <ul> <li>\(n\) bidders each have a private <strong>valuation</strong> \(v_i\) drawn independently from a common distribution \(F\) on \([0, \bar{v}]\).</li> <li>Each bidder simultaneously submits a bid \(b_i\).</li> <li>The highest bidder wins and pays their own bid. If bidder \(i\) wins, their payoff is \(v_i - b_i\); all others get 0.</li> </ul> <p><strong>Strategy:</strong> \(b_i(v_i)\) — a mapping from valuation to bid.</p> <p><strong>BNE Analysis (symmetric, \(n\) bidders, uniform \(v_i \sim U[0,1]\)):</strong></p> <p>In a symmetric BNE, all bidders use the same bidding function \(b(v)\). Bidder \(i\) with valuation \(v\) solves:</p> \[\max_{b} \; (v - b) \cdot P(\text{win} \mid b)\] \[P(\text{win} \mid b) = P(b_j(v_j) &lt; b \; \forall j \neq i) = \left[F(b^{-1}(b))\right]^{n-1}\] <p>With \(F = U[0,1]\), letting \(x = b^{-1}(b)\) (the valuation that bids exactly \(b\)), \(P(\text{win}) = x^{n-1}\). The first-order condition and boundary condition \(b(0) = 0\) yield:</p> \[b^*(v) = \frac{n-1}{n} \cdot v\] <blockquote> <h5 id="the-core-concept-1">THE CORE CONCEPT</h5> <p class="block-tip">In the symmetric BNE of a first-price auction with \(n\) bidders, each bidder <strong>shades their bid below their valuation</strong> by a factor of \(\frac{n-1}{n}\). With more competition (larger \(n\)), the shade factor approaches 1 — bids approach valuations.</p> </blockquote> <p><strong>Revenue Equivalence Theorem:</strong> Under mild conditions (independent private values, symmetric bidders, risk neutrality), <strong>all standard auction formats</strong> (first-price, second-price, English, Dutch) yield the <strong>same expected revenue</strong> for the seller in their BNE. This is one of the most elegant and counterintuitive results in economics.</p> <hr/> <h2 id="6-second-price-auctions-and-dominant-strategies">6. Second-Price Auctions and Dominant Strategies</h2> <p>In a <strong>second-price (Vickrey) auction</strong>, the highest bidder wins but pays the <em>second-highest</em> bid. Remarkably, bidding your true valuation is a <strong>weakly dominant strategy</strong>:</p> <p><strong>Proof:</strong> Fix any bids of the other players, and let \(m = \max_{j \neq i} b_j\) be the highest competitor bid.</p> <ul> <li><strong>If \(b_i &gt; v_i\):</strong> If \(b_i &gt; m &gt; v_i\), you win but pay \(m &gt; v_i\) — a negative payoff. Bidding \(v_i\) would have lost (better, as \(m &gt; v_i\)). Overbidding can only hurt.</li> <li><strong>If \(b_i &lt; v_i\):</strong> If \(v_i &gt; m &gt; b_i\), you lose. Bidding \(v_i\) would have won with payoff \(v_i - m &gt; 0\). Underbidding can only hurt.</li> <li><strong>Bidding \(b_i = v_i\):</strong> Never wins when \(v_i &lt; m\), wins and earns \(v_i - m &gt; 0\) when \(v_i &gt; m\).</li> </ul> <p>This makes the second-price auction <strong>strategy-proof</strong> — a highly desirable property for mechanism design.</p> <hr/> <h2 id="summary">Summary</h2> <table> <thead> <tr> <th style="text-align: left">Concept</th> <th style="text-align: left">Key Idea</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>Incomplete Information</strong></td> <td style="text-align: left">Players have private types unknown to others</td> </tr> <tr> <td style="text-align: left"><strong>Bayesian Game</strong></td> <td style="text-align: left">Models incomplete info via type spaces and a common prior</td> </tr> <tr> <td style="text-align: left"><strong>BNE</strong></td> <td style="text-align: left">Each type plays a best response given others’ type-contingent strategies</td> </tr> <tr> <td style="text-align: left"><strong>Bid Shading</strong></td> <td style="text-align: left">First-price equilibrium bids strictly below valuations</td> </tr> <tr> <td style="text-align: left"><strong>Revenue Equivalence</strong></td> <td style="text-align: left">Standard auction formats yield equal expected revenue in BNE</td> </tr> <tr> <td style="text-align: left"><strong>Second-Price Auction</strong></td> <td style="text-align: left">Dominant strategy to bid true valuation (strategy-proof)</td> </tr> </tbody> </table> <hr/> <h2 id="whats-next">What’s Next?</h2> <p>We’ve now mastered strategic interaction with private information. In Part 6, the final chapter, we turn to a different kind of game: <strong>Cooperative Game Theory</strong>. When players can make binding agreements, how should they split the gains? We’ll derive the <strong>Nash Bargaining Solution</strong> and the <strong>Shapley Value</strong> — two of the most beautiful results in all of game theory.</p>]]></content><author><name></name></author><category term="game-theory"/><category term="game-theory"/><category term="economics"/><category term="mathematics"/><category term="bayesian-games"/><category term="incomplete-information"/><category term="auctions"/><summary type="html"><![CDATA[If you missed the previous chapter, start here: Part 4: Extensive Form Games and Backward Induction]]></summary></entry></feed>